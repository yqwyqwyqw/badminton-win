# 变动报告：C++/ONNX 推理改造（里程碑 A）

> 分支 `deepseekflash`　日期 2026-09-15
> 结论：**里程碑 A 达成**。应用在**无 Python、无 .venv、无工程目录**依赖下可完成"导入→分析→审核→导出"，且回合切分与拍数与 Python 基线**完全一致**。
> 相关文档：`onnx-refactor-plan.md`（方案）、`onnx-m1-parity-report.md`（对齐数据）、`决策记录.md`（决策过程）。

---

## 1. 一句话摘要

把"Qt UI + Python/PyTorch/CUDA 子进程 + 全套 Python 依赖"换成"Qt UI + **进程内 C++ + ONNX Runtime(DirectML)**"，在**保持回合切分结果不变**的前提下，去掉了 Python 运行时、CUDA 运行时与工程目录依赖；对唯一无法位级复刻的部分（OpenCV resize 的 8 位定点路径）做了量化评估并以产品门禁验收。

---

## 2. 变更清单

### 新增（产品代码）

| 文件 | 作用 |
|---|---|
| `app/src/inference/tracknet_types.h` | 常量与轨迹类型（512×288、阈值 0.5） |
| `app/src/inference/cv_resize.{h,cpp}` | 双线性缩放（与 `cv2.resize` 浮点路径位级一致） |
| `app/src/inference/preprocess.{h,cpp}` | BGR→RGB → resize → CHW → 背景帧置于通道 0 → ÷255 |
| `app/src/inference/postprocess.{h,cpp}` | 阈值 0.5 → 8 邻接连通域取最大 → 外接框中心 → 坐标缩放 |
| `app/src/inference/windowing.{h,cpp}` | 窗口枚举（步长、末尾重复帧补齐） |
| `app/src/inference/rally_state_machine.{h,cpp}` | 回合切分与拍数判定（含出画瞬时/终局规则） |
| `app/src/inference/video_decoder.{h,cpp}` | ffprobe 元数据 + ffmpeg 子进程解码（BGR24）+ 通用命令执行 |
| `app/src/inference/tracknet_engine.{h,cpp}` | ONNX Runtime 会话（DML/CPU）+ 窗口推理与解码 |
| `app/src/inference/job_runner.{h,cpp}` | 背景中值图 + 分块推理 + 轨迹组装 + 断点续跑 + 进度回调 |
| `app/src/inference/inference_job.{h,cpp}` | Qt 封装：工作线程 + 文件契约（progress.json / rally-feed.json / 短片） |

### 修改

| 文件 | 变更 |
|---|---|
| `app/src/TrialAnalysisController.{h,cpp}` | 用进程内 `InferenceJob` 取代 Python `QProcess`；新增 `resolveResource()`（打包目录优先、源码树兜底）；缓存目录移到 `%LOCALAPPDATA%`；删除 `m_pythonPath`/`m_runnerPath`/`m_vendorPath`；`running()` 改为同时反映代理与推理任务 |
| `app/CMakeLists.txt` | 编译内核源码、查找并链接 ONNX Runtime（`ORT_ROOT`/`DML_ROOT`）、构建后自动拷贝 `onnxruntime.dll`/`DirectML.dll` |
| `README.md`（根/`app/`） | 待更新（见第 6 节遗留项） |

### 新增（开发/验证工具，均**不进发布包**）

`tools/`：`capture_golden.py`、`export_onnx.py`、`compare_onnx_pipeline.py`、`dump_golden_bin.py`、`dump_parity_fixtures.py`、`dump_resize_probe.py`、`resize_probe/`、`resize_calibrate.py`、`resize_sweep.py`、`parity_harness/`、`pipeline_e2e/`、`integration_check/`。

### 未改动

`app/qml/**`（全部 QML）、`VideoImportController`、`main.cpp`、`scripts/release-windows.ps1`、`validation/**`（原 Python 链路保留，仅作开发期"预言机"）。

---

## 3. 行为保真度证据

### 3.1 逐层对齐（`tools/parity_harness`，编译的是产品源码）

| 测试 | 结果 |
|---|---|
| T1 前处理 vs PyTorch 张量 | max **1.0000 LSB**，超 1 LSB 的元素 **0 / 3185 万** → PASS |
| T2 后处理 vs Python 解码坐标 | **0 / 64 不一致**（完全精确）→ PASS |
| T3 端到端（前处理+ONNX+解码） | 可见性 **0 / 64**、坐标 P50/P95 **0.0 px** → PASS |
| T4 回合判定 vs `rallies-provisional.csv` | **3 vs 3，边界/拍数/观测/推断逐项一致** → PASS |

### 3.2 全链路（`tools/pipeline_e2e`，2823 帧 720p 代理）

```
中值图   differing=0/2764800 max=0                      （与 cv2 解码逐字节一致）
轨迹     golden=2823 mine=2823 可见性差异 1 (0.035%)
         jointVisible=1994 P50=0.000 P95=0.000 max=5.000  → PASS
回合     mine=3 golden=3，边界与拍数逐项一致              → PASS
```

### 3.3 成品应用（`tools/integration_check`，隔离目录 + 最小 PATH）

在 `D:\Desktop\app-portable-test`（exe 同级 `resources/`，PATH 仅 system32）驱动真实控制器：

```
final state: complete   第 2 次运行 11.5s（代理与分块命中缓存）
rallies in rally-feed.json: 3
  rally 1  0.48s – 23.26s  hits=17
  rally 2 25.28s – 30.86s  hits=3
  rally 3 31.32s – 56.44s  hits=23       ← 与 Python 基线逐项相同
result(analysis): PASS
export 720p:     ok  3247 KB
export 1080p:    ok  5175 KB
result(export):  PASS
```

冷启动（含 app 自己从 4K 原片生成 720p 代理 + 全片分析 + 3 段短片）**85.5 秒**；推理吞吐 **42.2 fps（0.85× 实时）**。

---

## 4. 有意的行为差异（逐条）

| # | 差异 | 原因 | 影响 | 风险与边界 |
|---|---|---|---|---|
| D1 | 模型输入缩放与 `cv2.resize(INTER_LINEAR)` 有 **≤1 LSB** 差异（5.5% 像素） | 本实现与 cv2 **浮点路径位级一致（max\|d\|=0）**；差异来自 OpenCV **8 位定点路径自身的量化**（cv2 的浮点路径与 8 位路径亦差 34521 像素；扫过 60+ 种定点公式无一命中） | 轨迹可见性差异 **1/2823（0.035%）**、坐标 P95 **0 px**、回合切分不变 | 门禁为"可见性 ≤0.5%、P95 ≤1.5px"；若换素材后出现可见性差异，第一嫌疑在此。补救选项：改用 `INTER_LINEAR_EXACT` 语义或内嵌 OpenCV |
| D2 | 模型文件由 `TrackNet_best.pt`（129.9 MiB）改为 **`tracknet-8f-concat-288x512-sizes-fp16.onnx`（21.6 MiB）** | 剥离 Adam 优化器状态；fp16 计算、fp32 输入输出 | 体积减少 108 MiB；数值差异 ≤5e-3（进入 D1 的同一量级） | 对齐数据见 §3 |
| D3 | 帧解码由 `cv2.VideoCapture` 改为 **`ffmpeg.exe` 子进程**（BGR24 原始管道） | 去掉 OpenCV 依赖；保持子进程以规避 GPL 传染（不链接 libav\*） | **中值图与 cv2 逐字节一致（0/2764800）**，无额外偏差 | 需要发布包内含 ffmpeg.exe（GPLv3，需附许可与源码指引） |
| D4 | 缓存目录由 `<工程根>/validation-output/ui-trials/` 改为 **`%LOCALAPPDATA%/BadmintonAnalyzer/cache/`** | 安装后的应用不应写源码树（原方案的 D-4 缺陷） | 旧缓存不再被复用（首次运行会重新生成代理与轨迹） | 用户如需保留旧结果，可手工拷贝到新目录 |
| D5 | 资源定位改为 **`<exe>/resources/` 优先，源码树兜底** | 去掉"向上找工作区"的隐式依赖（原 D-6 缺陷） | 打包布局可用；开发期仍可直接运行 | 缺失时错误信息指向打包路径 |
| D6 | 推理改为**进程内 + 工作线程**，stdout 协议（`PROGRESS`/`RALLY_READY`）不再产生 | 无子进程 | UI 仍通过 `progress.json`/`rally-feed.json` 轮询（契约未变），已实测 | 分析期间"已恢复完整分析结果"文案可能瞬时闪现一次（状态由文件驱动），无功能影响 |
| D7 | 回合短片改由 C++ 生成（ffmpeg 参数与 Python 完全一致：`-ss start-0.5 -to end+0.8 -an -c:v libx264 -crf 18`） | 无 Python | 短片仍生成、仍无音轨（**与改造前一致**，D-3 缺陷未在本次修） | 若后续要修"720p 快速版导出无声"，见 §6 |
| D8 | `PredictChunk` 改为**直通解码**（不再按帧累加热图） | 非重叠模式下每帧只属一个窗口；原累加方式多占 ≈1.5 GB 并导致崩溃 | 内存 ~2.6 GB → **~1.1 GB**；输出不变 | 仅在非重叠模式成立；重叠模式仍保留累加路径 |
| D9 | `JobRunner` 支持**断点续跑**：`chunks/chunk-*.csv` 存在即复用 | 与原 Python 行为对齐 | 第二次运行 11.5s（命中缓存） | 指纹校验（视频/模型/参数）尚未实现，换素材但目录名相同时可能误复用——**建议下一轮补 `run-fingerprint.json`** |

**没有差异的项目**：回合边界与拍数、可见性判定、出画处理规则、坐标映射、导出画质档位与缩放策略、UI 文案与交互流程。

---

## 5. 原有功能保持情况

| 功能 | 状态 |
|---|---|
| 本地视频导入、元数据读取、原片预览 | 未改动（Qt Multimedia + `VideoImportController`） |
| 720p 代理生成（CUDA 优先、软件回退） | 未改动（ffmpeg QProcess） |
| TrackNet 增量回合分析、停止/恢复 | 等价（进程内任务 + 分块续跑） |
| 回合列表预览、缩略/短片播放 | 未改动；`rally-feed.json` 契约一致，短片照常生成 |
| 往返筛选、拖动排序、边界微调、备注 | 未改动（QML + 内存态） |
| 单回合导出（720p/1080p/原始画质） | 未改动；已实测 720p 与 1080p 两条路径 PASS |
| 合集拼接导出（filter_complex trim/concat/scale） | 未改动（代码路径与单回合共用 ffmpeg） |
| 缓存隔离（按文件路径+大小+时间取键） | 保留，仅目录位置变更（D4） |

**未做的验证**：QML 界面未做点击式回归（无 GUI 自动化）；本次通过真实控制器驱动同一代码路径完成"分析+导出"，并确认 QML 依赖的契约文件与字段完全一致。

---

## 6. 已知遗留项（建议后续）

1. **打包（原 M4）**：`windeployqt` 精简（当前实测便携目录 347.8 MB，其中 Qt 拉入了 Pdf/Network/多余插件）、FFmpeg 精简构建、7z 自解压单 exe、`THIRD-PARTY-NOTICES.md`。
2. **性能与内存**：中值图用 41 次 ffmpeg 子进程采样（实测单帧 seek 0.11–0.19 s，整段约 6 s；而全片流式解码只要 4 s，仍值得合并为单次采样）；每个 6 秒分块一次性读入 ≈900 MB 帧缓冲（可改子块流式，降到 ~170 MB）。
3. **断点续跑的指纹校验**（D9 备注）。
4. **回合短片无音轨**（原 D-3 缺陷，改造前即存在，本次未动）。
5. **InpaintNet / BounceNet**：按决策 D2/D4 未纳入（二期可选）。
6. `README.md` / `RELEASE.md` 需更新为"C++/ONNX 版本 + 需要 DX12 显卡"的说明。

---

## 7. 复现与验收命令

```powershell
$root = 'D:\Desktop\badminton-win'
$qt   = 'D:/Qt/6.11.2/msvc2022_64'
$ort  = "$root/.tools/onnxruntime-cpp/ort-dml"
$dml  = "$root/.tools/onnxruntime-cpp/directml"

# 1) 编译应用（含内核）
cmake -S "$root\app" -B "$root\build\app" -G "Visual Studio 17 2022" -A x64 `
  -DCMAKE_PREFIX_PATH="$qt" -DORT_ROOT="$ort" -DDML_ROOT="$dml"
cmake --build "$root\build\app" --config Release

# 2) 逐层对齐（T1-T4）
cmake -S "$root\tools\parity_harness" -B "$root\build\parity_harness" -G "Visual Studio 17 2022" -A x64 `
  -DORT_ROOT="$ort" -DDML_ROOT="$dml"
cmake --build "$root\build\parity_harness" --config Release
& "$root\build\parity_harness\Release\parity_harness.exe" "$root\validation-output\golden\fixtures" `
  "$root\validation-output\golden\models\tracknet-8f-concat-288x512-sizes-fp16.onnx" dml 8 1280 720 8 `
  "$root\validation-output\golden\VID20260828212318_1\app-run\trajectory-partial.csv" `
  "$root\validation-output\golden\VID20260828212318_1\app-run\analysis\rallies-provisional.csv" 50

# 3) 成品应用全流程（隔离目录 + 最小 PATH）
cmake -S "$root\tools\integration_check" -B "$root\build\integration_check" -G "Visual Studio 17 2022" -A x64 `
  -DCMAKE_PREFIX_PATH="$qt" -DORT_ROOT="$ort" -DDML_ROOT="$dml"
cmake --build "$root\build\integration_check" --config Release
# 部署到 D:\Desktop\app-portable-test（exe + Qt DLL + resources/ffmpeg + resources/models）后：
& "D:\Desktop\app-portable-test\integration_check.exe" "F:\羽毛球视频\20260828\VID20260828212318_1.mp4" 56460 3840 2160
```

---

## 8. 验收后修复：非 ASCII 路径导致"进度条不动"（2026-09-15）

### 8.1 现象

GUI 里选一部长视频开始分析：GPU 明显在跑、显存占用高，但**进度条长时间停在 0%**、已完成回合数不增加。界面里显示的 ETA 是代理生成阶段的残值——`TrialAnalysisController` 在 ffmpeg 生成代理时用 `-progress` 估算进度并写入 ETA；分析真正开始时 `m_progress` 归零，此后只由 `progress.json` 驱动，而那份文件当时写到了别处。

### 8.2 根因：同一份缓存被写成了两个目录

磁盘上同时出现两个 app 目录：

| 目录 | 谁在写 | 内容 |
|---|---|---|
| `%LOCALAPPDATA%\BadmintonAnalyzer\羽毛球回合分析器\` | Qt 侧（UI 也读这里） | `proxy-720p.mp4` |
| `%LOCALAPPDATA%\BadmintonAnalyzer\缇芥瘺鐞冨洖鍚堝垎鏋愬櫒\` | C++ 内核 | `chunks/*.csv`、`progress.json`、`rally-feed.json`、`rallies/*.mp4` |

`缇芥瘺鐞冨洖鍚堝垎鏋愬櫒` 就是 `羽毛球回合分析器` 的 UTF-8 字节被当成 GBK 解读的结果。原因：内核（`job_runner.cpp`、`inference_job.cpp`）把 Qt 传来的 **UTF-8 `std::string`** 直接喂给 `std::filesystem::path`；Windows 上 `fs::path` 的窄字符构造按**系统 ANSI 码页（936）**解码，于是 `create_directories()` 建出的是另一个目录，内核全程写在"影子目录"里，UI 盯着的目录始终是空的——分析其实跑完了，只是没人看得见。

同类问题还有两处（当时尚未显形）：

- `tracknet_engine.cpp` 的 `Widen()` 写成 `std::wstring(text.begin(), text.end())`，是**逐字节膨胀**而不是 UTF-8 解码：模型路径一旦含中文就会打不开模型；
- `inference_job.cpp` 的 `WriteTextFile()` 用 `path.string() + ".tmp"` 拼临时文件，同样要过一遍 ANSI 码页。

`video_decoder.cpp` 的 ffmpeg 调用本来就是 `CreateProcessW` + `MultiByteToWideChar(CP_UTF8)`，所以中文路径的读取与 ffmpeg 参数一直是正确的——这也解释了"为什么分析确实在跑"。

### 8.3 修复

- 新增 `app/src/inference/path_utf8.h`：`Utf8Path()` / `Utf8String()` / `Utf8ToWide()`，显式按 UTF-8 转换（Windows 走 `MultiByteToWideChar` / `WideCharToMultiByte`，`CP_UTF8`）；
- 内核所有路径构造统一走它：
  - `job_runner.cpp`：`ChunkCsvPath` / `TrajectoryCsvPath` 改为返回 `std::filesystem::path`，`fs::exists` / `create_directories` / 分块 CSV 读写全部改用原生路径；
  - `inference_job.cpp`：`progress.json`、`rally-feed.json`、`analysis/*`、`rallies/*`、原子写临时文件、ffmpeg 短片输出路径；
  - `tracknet_engine.cpp`：ONNX 模型路径改用真正的 UTF-8 → wide。
- 对外契约不变：文件名、目录层级、JSON 字段名与取值都没有变，只是终于落到了正确的目录。

### 8.4 为什么之前没测出来

`tools/integration_check` 原来把 `applicationName` 设成 ASCII 的 `BadmintonAnalyzer`，整条缓存路径都是 ASCII，恰好绕过了这个 bug——**自动化验证用的目录布局和产品不一致**。现在它默认使用产品同名（`羽毛球回合分析器`，可用环境变量 `BADMINTON_APP_NAME` 覆盖），并新增 `path preflight` 断言：让内核在临时目录里写一个中文目录，断言"只多出这一个目录、且目标文件存在"，一旦再出现乱码目录会立刻 FAIL。

### 8.5 复测证据（冷跑，产品同名目录）

- `path preflight: PASS  created=羽毛球路径检查  file=ok`
- 进度真实推进：代理阶段 6.3% → 98.1%，推理阶段 0% → 95.6% → 100%（冷跑 78.5 s，推理 43.0 fps）；该 `progress` 正是 QML 进度条绑定的属性
- 8 个契约文件全部落在 `…\BadmintonAnalyzer\羽毛球回合分析器\cache\VID20260828212318_1-c1fe12c0ab\`
- 3 个回合与黄金集边界一致（0.48–23.26 s / 17 拍、25.28–30.86 s / 3 拍、31.32–56.44 s / 23 拍），720p 与 1080p 导出 PASS
- `%LOCALAPPDATA%\BadmintonAnalyzer` 下**只有一个**目录，字符码 `7FBD 6BDB 7403 56DE 5408 5206 6790 5668`，无乱码目录

### 8.6 同一轮一并修掉的两个相关问题

1. **续跑分支吞掉发布与进度**：`job_runner.cpp` 命中分块缓存时用 `continue` 直接进入下一块，跳过了 `on_trajectory` / `on_progress`。后果是"续跑"这一次的进度条从第一块开始就不动、回合也要等第一块新算完才出现（而缓存里的分块往往本来就是完整的）。现在续跑块与全新块走同一段发布/进度代码。
2. **改名被读端锁住，最后一次进度丢失**：`WriteTextFile()` 用"写临时文件 + 改名"替换 `progress.json`，而 UI 每 0.5 s 读一次该文件；Windows 上 `QFile` 打开时不含 `FILE_SHARE_DELETE`，读者持句柄期间 `MoveFileEx` 会失败，旧代码失败即放弃——磁盘上留下 `progress.json.tmp`，UI 永远停在 `99.54% / running`。现在改名最多重试 20 次（每次 15 ms，足以覆盖读端打开的间隙），仍失败才退回原地覆盖并清掉临时文件。

实测：用一个刻意以 `FileShare.Read` 反复打开 `progress.json` 的抢锁读者持续干扰，续跑一次（13.3 s，10 个分块全部命中缓存）后 `progress.json` 为 `status=complete / percent=100.00`，且无 `.tmp` 残留。

---

## 9. UI 交互调整（按使用反馈）

| # | 位置 | 调整 |
|---|---|---|
| 1 | 回合分析页（`RallyLibraryPanel`） | 「上一回合 / 下一回合」按钮加宽到 82 px 并收紧内边距，标题不再被截断 |
| 2 | 回合分析页 | 切换回合后**立即播放**，并设 `loops: MediaPlayer.Infinite` 做**单回合循环** |
| 3 | 回合拼接页（`RallyAssemblyPanel`） | 「上一段 / 下一段」的可用性只看索引位置（原逻辑是"该方向存在已显示的回合才可用"），点击即跳转并播放，不受隐藏/筛选影响 |
| 4 | 回合拼接页 | 微调边界（±0.5、手填时间、恢复原始边界）后**回到该段起点继续播放** |
| 5 | 回合拼接页 | 播放到段尾不再自动跳到下一段，改为**回到段首循环** |
| 6 | 回合拼接页 | 底部微调行的留白处新增「隐藏本回合 / 显示本回合」按钮，直接读写同一个 model 字段，与右侧列表的隐藏状态天然同步 |
| 7 | 导入页（`VideoImportPanel`） | 按钮「进入切分试验」改名为「**开始回合分析**」；同步把状态文案改为「等待开始回合分析 / 回合分析完成 / 已恢复回合分析结果 / 回合分析未完成」 |
| 8 | 顶栏「新建项目」（`AppHeader` + `Main.qml`） | 新建项目时**自动清理上一个项目的分析缓存**（`<cache>/<视频名-hash>`：720p 代理、分块轨迹、回合短片、报告）。取消是异步的，因此清理走 300 ms 延迟重试（等分析线程/代理进程退出，最多约 30 s），完成后顶栏提示「已清理上次分析的缓存（X MB）」。同目录被重新导入时不删 |
