# 羽毛球视频自动回合切割与计分系统 — 技术方案（v1.6）

> **⚠️ 复核提示**：本版包含三轮复核 —— 第 17 节（技术压力复核，**推翻 v1.2 的时间预算**、降级 4K-ROI 卖点）与**第 18 节（素材条件确定后的整体复核，全 4K60）**。**以第 17、18 节为准。**
> **⚠️ 导出提示**：本版按 D13/D14 纠正了导出主次 —— **主产物是"一条成片"，不是"每回合一个 MP4"**；三种导出方式的完整交互设计见 **第 9.6 节**。
> **⚠️ 环境提示**：素材为**全 4K60**，因此**"4K60 硬件解码可用性"（A6）是当前最高优先级的环境风险**，建议开工第一步就验证（第 18.1 节）。

> 状态：方案设计稿（未编码）。**本次会话交付范围为方案本身，不含代码。**
> 目标场景：**业余娱乐、单视频单局、6–10 分钟**。
> 素材条件（D10 + **D14**）：**三脚架手机拍摄为主；单打与双打都有；全部 4K 60fps；球可能出画**。**开发前会提供典型素材。**
> 目标平台：**Windows 本地 + Android 本地（原生双端，共享同一 C++ 核心）；全部本地处理，不做服务器，不做浏览器端**。
> 本文档回答四件事：**能不能做**（技术要点与可行性）、**怎么做**（程序执行逻辑）、**多大**（程序体积与算力预算）、**怎么落地**（设计方向、导出方式、双端分工）。
>
> **已确认决策**：D1 人工三态确认（`A方得分`/`B方得分`/`非回合`）；D2 全部本地处理、不做服务器；D3 Windows + Android 双端；D5 业余娱乐单局 6–10 分钟；D6 排除浏览器方案；D7 粗切片/预览/导出三层分离；**D8 `非回合`= 不计分作废（三态）**；**D9 音画偏移 → 以轨迹为主证据，音频降为可选增强**；**D10 素材条件（三脚架 / 单双打 / 1080p–4K / 可能出画）**；**D11 比分非核心、默认不显示**；**D12 典型帧率 60fps**；**D13 切片三大用途 → 导出主产物是"成片"**。
> **下一步**：先看 **第 16 节（方案审阅结论）** —— 含开工前必须完成的 **M-1 预研清单**、术语表、验收测试集。**待确认项只剩 1 项：20 段基准素材由谁提供。**

---

## 0. 结论先行（TL;DR）

| 议题 | 结论 |
|---|---|
| **场景（最关键）** | **业余娱乐、单视频单局、6–10 分钟**；素材为**三脚架手机拍摄 + 60fps**（D10/D12）。这个组合把原先基于"手持 + 30fps + 60 分钟比赛"的三大难点（算力、体积、平台）基本解除，**帧率与抖动两大杀手已被素材条件消除**。剩余头号矛盾是**光线**与**横向高速杀球的轨迹断裂**（第 17.5 节）。 |
| **性能口径（已按第 17 节复核修正）** | ⚠️ **原"1.5–3 分钟"的乐观数字已作废** —— 那是把第二遍处理代价低估一个数量级得出的。修正后：到"可审核" **Windows ≈5–12 min（GPU）**、**手机 ≈17–33 min（仅第一遍）**；且第一遍吞吐口径存在 6 倍分歧，**必须实测（A1）后才能给承诺值**。在此之前对外只写"分钟级"。 |
| 可行性 | **可行，但必须改一处核心假设**。仅靠"场地识别 + 双方姿态 → 判断击球/未击球"在业余手持视频上误检率会很高；必须改为**以球轨迹为唯一主证据 + 静止期为首道锚**（音频与姿态仅作可选增强）。 |
| **主证据链（D9）** | 🆕 **球轨迹运动学 = 唯一主证据**（回合边界、拍数、发球时刻、死球全由它给出）；**静止期/画面稳定 = 第一道锚**（三脚架拍摄下极可靠）。**击球音因音画偏移问题降为可选增强**，v1 默认不使用（第 2.2 ④-补二）。 |
| **回合边界（关键定义）** | 🆕 **回合起点 ≠ 首拍事件**，而是"**发球时刻往回推 1.5–2.0 s**"（覆盖发球准备动作），并用"**静止期结束点 + 0.2 s**"做下界保护，避免把捡球包进来（第 2.2 ④-补）。 |
| **全局校验** | 🆕 主校验改为**静止期结构校验 + 发球节奏自洽性**（不需要用户输入任何信息）；"终局比分硬约束"降为可选辅助 —— 因为**比分非核心、甚至可不显示**（D11，第 12.4 节）。 |
| 计分 | ✅ **D1/D8/D11**：人工三态 `A方得分` / `B方得分` / `非回合`。**"非回合" = 不计分、回合作废**（不推进比分、不换发球权）。**比分列默认隐藏、烧字默认不烧比分**；三态标注仍保留，因为它是切分质量的校验工具与训练数据来源。 |
| 切片 | **三层分离（D7，第 3.3 节）**：① **粗切片 = 纯时间索引，零编码、零文件**；② **预览 = 按时间 seek 播放**（可选生成一支全片代理保证弱机流畅）；③ **导出 = 唯一一次真正编码**。顺序调整/删除/合并/改边界全部是纯数据操作，零编码成本。 |
| **导出的主产物（D13）** | ⚠️ **不是"每回合一个 MP4"，而是"一条成片"**。三种用途 = 一个"成片生成器"的三个预设：**① 单球导出**、**② 精彩集锦**（主力）、**③ 垃圾时间压缩**（第 9.5 节）。拼接点必须做 **80–150 ms 音频交叉淡化**，否则会听到爆音。 |
| 导出 | **唯一一次编码，且是项目数据的纯函数**（`导出结果 = f(回合表, 顺序, 设置)`）→ 可重复、可增量、可中断恢复。默认 MP4 片段 + 可选合并长片；**竖屏/方形模板 + 一键分享到微信/抖音**为重心；**FCPXML/EDL 降级为二期可选**（第 9 节、第 12.6 节）。 |
| 运行平台 | ✅ **全部本地处理 + 原生双端**（D2/D6）。Windows 为完整客户端，Android 为移动端；共享平台无关 C++ 核心、数据模型与参数档位。**服务器不算力**；**浏览器端不做**。 |
| **素材与规格（D10/D12/D14）** | 三脚架手机拍摄；**全部 4K 60fps**；单打与双打都有；球可能出画 → 出画即标 UNKNOWN。模型走"**V2/V4 第一遍 720p 粗筛 + V3 仅事件窗口精算**"；⚠️ **4K-ROI 提精度仍是待验证的可选增强**（第 17.1 节 T2）；⚠️ **4K60 硬解可用性是新的最高优先级环境风险**（第 18.1 节 A6）。 |
| 安装包体积 | Windows ≈ **200–400 MB**；Android ≈ **90–150 MB**。 |
| 总工作量（单人） | **Windows 单线约 6–9 周**；双端并行约 **12–18 周**（原生双端，Android 需单独实现 UI）。**另需前置 M-1 预研 1–2 周**。 |
| 明确砍掉 | 批量队列、库管理与跨场次统计、比分板 OCR、FCPXML/EDL（降级）、**音频作主判据**、**双打空间判定**、手持稳定化相关工作量（因三脚架拍摄而省）。 |
| **开工前必做** | 🆕 **M-1 预研（1–2 周，纯算法无 UI）**：验证 TrackNet 在真实素材上的轨迹召回（**P1 是唯一硬门，目标 ≥90%**）、半场标定可行性、手机温控、两遍处理收益（第 16.2 节）。 |
| **开工第一件事** | 🆕 拿到素材后**先跑 A6（4K60 硬解探测）**，再跑 P1（轨迹召回 ≥90%）。理由：硬解不可用会改变整个时间预算的前提，P1 的测试环境也须相应改为"代理模式"（第 18.4 节）。 |
| **待确认** | ① C 类 5 项（精彩球筛选 / 球员姓名 / 烧字内容 / 双端研发顺序 / 检查更新）；② **素材的数量与条件覆盖**（单打/双打比例、光线条件、是否含球出画）—— 第 16.4 节的验收指标依赖覆盖度。**已无阻塞项。** |

---

## 1. 需求澄清

> **状态更新**：本节的 6 个变量中，**第 1、2、4、5 项已由 D5（业余娱乐单局）与 D1（人工三态计分）确定**；**第 3 项（比分从哪来）已由 D1 决定为"人工点选"**。**仅第 6 项（烧字内容与语言）仍待确认**。保留本节用于记录当初的判断依据。

方案的所有技术选型都取决于这 6 个变量：

1. **视频来源与机位**（决定难度，差别是数量级的）
   - A. 电视/直播转播源：比分板可见、镜头会切、导播会跟球 → 镜头切换会打断轨迹，**本场景不适用**。
   - B. **固定单机位**（三脚架/放在椅子或地上，最好全场入画）→ 最理想；**若只拍到半场，走"半场标定"（第 12.3 节）**。
   - C. **手持/手机随手拍 → 业余娱乐场景的常态**，抖动、变焦、球经常出画 → 需稳定化 + 滑动重标定，精度预期下调。
2. **单打还是双打**：双打 4 人姿态匹配、站位重叠、责任区判定复杂度显著上升。业余双打很常见，但建议 v1 的**空间判定类功能**只支持**单打**（双打可先只做切分 + 三态确认）。
3. **比分从哪来**：画面内有比分板？还是需要程序自己数？还是发球方口播？
4. **视频规格**：分辨率（720p 可，1080p 最佳，4K 走两级 ROI）、**帧率（已确认为 60fps —— 这是本方案最重要的有利条件，见 D12）**、快门（60fps 下单帧曝光时间天然减半，运动模糊显著改善；但仍受光线影响）、**时长（本场景为 6–10 分钟单局，见 D5）**。
5. **"切片"的最终用途**：自己复盘看 / 发给学员 / 剪成集锦发社交平台 / 进 Premiere 精剪？——这直接决定导出方案（第 9 节）。
6. **输出语言与字幕**：是否要烧入比分、拍数、时长、回合序号、球员名。

---

## 2. 技术要点评估（核心章节）

### 2.1 关键技术现实：为什么不能只靠姿态

用户原始设想是"识别姿态 → 判断是击球/发球/未击球"。实际工程中的问题是：

- 姿态模型输出的是关节点，**"挥拍动作"与"挥空动作"在关节轨迹上差异很小**；业余选手的假动作、挥拍练习、捡球、擦汗都会命中同一模式。
- 单人机位下**球员经常背对镜头、被球网/球柱/另一名球员遮挡**，关键点置信度骤降。
- 羽毛球是全世界最快的球类（杀球初速 300–490 km/h）。**本场景已确认为 60fps（D12），杀球在相邻两帧间的位移约 1.4–2.3 m，仍在可关联范围内**；若素材是 30fps，位移会翻倍到 2.8–4.5 m，轨迹关联与击球时刻定位都会明显退化 —— 这也是 60fps 成为本方案重要有利条件的原因。
- 结论：姿态是**辅助证据**，不是主证据。可靠的做法是"球在哪里、什么时候改变方向"。

### 2.2 五级流水线的技术要点

#### ① 场地识别与标定（Court Calibration）

- 目标：得到单应矩阵 `H`，把画面像素坐标映射到真实球场坐标（13.4 m × 6.1 m，单打边线 5.18 m 宽，网高 1.524 m）。
- 方法：按键点检测 12–16 个场线交点（四角、发球线、中线、双打/单打边线），再最小二乘拟合单应。可选两条路线：
  - 轻量几何路线：Canny + Hough 直线 → 直线交点聚类 → 与球场模板匹配。CPU 上 5–20 ms，无需 GPU、无模型体积。
  - 学习路线：小型关键点 CNN（输入 512×288，输出 12–16 个热图），对遮挡和模糊更鲁棒，模型 2–8 MB。
- 工程要点：
  - **标定策略分两种**：固定机位可"前 N 秒取中值、全片复用"；**手持/手机素材必须滑动窗口重标定**（每 30–60 秒或检测到漂移时重标，见第 12.3 节）。若只拍到半场，走**半场标定**。
  - 标定后要算**重投影误差**，把误差当"置信度"上报 UI；标定不可信时该视频自动降级为"仅切片、不计分"。
  - 场景边缘的鱼眼/透视畸变在业余拍摄里很明显 → 加一步径向畸变粗校正能显著提升落点判定。
- 产出：`H`、球场 ROI、以及"网前/后场/左右半区/单双打边线内"的空间判定函数。

#### ② 球员检测、跟踪与身份归属（Player Tracking & Identity）

- 检测：YOLO11n-pose / RTMPose-tiny（person + 17 COCO 关键点），约 3–6 M 参数。
- 跟踪：ByteTrack 或轻量 IoU+卡尔曼关联，得到 `player_id` 轨迹。
- **使用范围（按实际需求收敛）**：用户既打单打也打双打，而双打下"球员身份归属"与"站位分析"都不可靠（4 人重叠、遮挡频繁）。因此姿态在本方案中**只承担两个轻量职责**：
  1. **活动量检测**（第 2.2 ④-补）：判断球员是否在走动 —— 这是"静止期"三条件之一，**只需粗粒度**；
  2. **击球位置校验**（可选）：判断候选击球点附近是否有人在做挥拍动作。
  - **不做**：单/双打的身份归属、站位热图、责任区判定（第 16.5 节列为明确不做）。
- 姿态只在候选窗口跑（不必全帧全时），算力占比小。

#### ③ 羽毛球检测与轨迹重建（Shuttlecock Tracking）— 系统的主心骨

这是全场最难、也是决定成败的一环。备选方案对比：

| 方案 | 原理 | 优点 | 缺点 | 建议 |
|---|---|---|---|---|
| 帧差 + 背景建模 + 形态学 | 运动目标提取 | 极轻量，CPU 可跑，0 模型 | 相机一动就废；球贴近球员/球场线时丢失严重 | 作为 B 计划的降级路径 |
| 通用小目标检测（YOLO 微调） | 单帧检测 | 易实现、可量化 | 羽毛球 3–8 px、高速运动模糊 → 单帧召回率低 | 只用于"球出画/落点"粗判 |
| **TrackNet / TrackNetV2 / TrackNetV3** | 连续 3–8 帧热图回归 + 时序融合 | 该任务 SOTA 系列，公开数据集与预训练权重，文献报告 test split 上 **Accuracy 97.51% / F1 98.56% / 25.1 FPS**（TrackNetV3） | 输入固定小尺寸（512×288）、需背景中值图、需 GPU/NPU 才快 | **主选**（TrackNetV3） |
| TrackNet + InpaintNet 轨迹修复 | 二阶段补全遮挡/漏检 | 轨迹连续性大幅提升，后续事件检测稳定 | 多一个模型、多一步推理 | **推荐启用** |

- 工程要点：
  - 输入统一缩到 512×288（TrackNet 惯例），**坐标保持原始像素并由 `H` 转球场坐标**。
  - 每段视频开场先采样若干帧算**背景中值图**（固定机位前提）；手持素材机位会变，需按滑动窗口重算，或退化为逐回合中值图。
  - 输出 `(frame, visibility, x, y)` 序列 + 可见性置信度；**不可见的点绝不当成"球停在原地"**（这是把回合结束判错的头号原因）。
  - 一杆过：TrackNet 检测 → InpaintNet 补全 → 运动学事件候选 → BounceNet 分类过滤。
  - 参考实现（可复用其事件模块设计与半自动标注工具）：[BadmintonTrackNet](https://github.com/ZSHYC/BadmintonTrackNet)、[TrackNetV3](https://github.com/qaz812345/TrackNetV3)。

#### ③-补 球轨迹识别的技术细节与实际效果（重点）

**技术路线：热图回归（heatmap regression），不是通用目标检测**

TrackNet 系列与 YOLO 那类"画框检测"思路完全不同，这是它能追上羽毛球的原因：

| 环节 | 做法 | 为什么这样做 |
|---|---|---|
| 输入 | **连续 3–9 帧**（V1: 3 帧 640×360；V2/V3/V4: 3 in 3 out，**512×288**） | 单帧无法区分"球"与"白色球场线上的高光/反光点"；多帧时序是唯一可靠线索 |
| 输出 | 每帧一张**单通道高斯热图**（峰值即球心），而非 bbox | 球只有 3–8 px，bbox 回归的 IoU 无意义；热图能给出亚像素中心 |
| 主干 | VGG-16 风格编码器 + 上采样解码器 + **skip connection** | 深网络会把小目标特征"吃掉"，skip 连接是为了保住微小目标的低层特征（TrackNetV2 的关键改进） |
| 辅助输入（V3） | **背景中值图**：球帧 − 背景帧的差分作为额外通道 | 显式压掉静止背景，让运动目标跳出来（V4 论文称之为"隐式利用了运动信息"） |
| 损失 | **加权交叉熵**（球位置像素权重高） | 正负样本极度不均（1 px vs 14 万 px） |
| 后处理（V3） | **InpaintNet 轨迹修复**：对遮挡/漏检段做图像修补式插值 | 轨迹连通性比单帧精度更影响后续事件判定 |
| 后处理（V4） | **可学习运动注意力图**（帧差 + Power Normalization 两参数层）与高层视觉特征逐元素相乘后拼接 | 插件式模块，2 个可学习参数，可挂到 V2/V3 上 |

**实测效果数据（这是文献里的真实数字，含分歧）**

TrackNetV3 作者自报（Shuttlecock Trajectory Dataset test split）：

| Model | Accuracy | Precision | Recall | F1 | FPS |
|---|---:|---:|---:|---:|---:|
| YOLOv7 | 57.82% | 78.53% | 59.96% | 68.00% | 34.77 |
| TrackNetV2 | 94.98% | 99.64% | 94.56% | 97.03% | 27.70 |
| TrackNetV3 | **97.51%** | 97.79% | **99.33%** | **98.56%** | 25.11 |

但 **TrackNetV4 论文的独立复现给出了明显更保守的数字**（同一 shuttlecock 数据集，按标准协议，其测试子集 10,836 帧）：

| Method | Acc. | Prec. | Rec. | F1 | FPS |
|---|---:|---:|---:|---:|---:|
| TrackNetV2 (3 in 1 out) | 83.9% | 90.7% | 89.2% | 89.9% | 12.9 |
| TrackNetV2 (3 in 3 out) | 85.2% | 97.2% | 85.4% | 90.9% | 31.8 |
| TrackNetV2（其复现基线） | 85.6% | 96.6% | 85.3% | 90.6% | **163.3**（含数据加载的整脚本速度不可比） |
| TrackNetV2 + Motion（V4） | 86.6% | 94.9% | 88.1% | 91.4% | 161.1 |
| **TrackNetV3** | 95.7% | **99.7%** | 95.4% | 97.5% | 15.1* |
| TrackNetV3 + Motion（V4） | **96.4%** | 99.5% | **96.3%** | **97.9%** | 15.1* |

**怎么读这两组数字（很重要）**

1. **准确率定义**：预测点与真值距离 **≤4 px 才算对**（在 512×288 尺度上），且"该出现没出现/不该出现却出现"都算错。这是严格的定位指标，不是"大概找到球了"。
2. **两组数字差 1.8–12 个百分点**（V3 97.5% vs 95.7%；V2 95.0% vs 85.6%），原因是测试子集构成不同、ground truth 质量不同、是否重训。**说明这类指标高度依赖测试集**，任何"单点精度承诺"都不可信。
3. **FPS 差距极大**：TrackNetV3 + InpaintNet 二阶段属于**重模型**（表中 15.1 FPS 是含数据加载与落盘的整脚本速度），而 TrackNetV2 轻量（同口径 160+ FPS）。**这是"精度 vs 算力"的核心取舍点**，也是方案里把 V3/InpaintNet 列为主选、但保留 V2 降级路径的原因。
4. **Precision 普遍 > Recall 的版本差异**：V3 高召回（99%+）低误报，适合"先捞全再过滤"；V2 高精确（96–99%）低召回（85%），会**丢拍**。回合切分要的是"不漏事件"，因此**宁可高召回 + 后置过滤**（这正是"两遍处理"要解决的问题：V2/V4 粗筛圈范围，V3 精算保召回）。

**真实场景下不能指望这些数字**：以上都在"画面清晰、球可见"的广播/标准数据集片段上测得。业余固定机位视频会遇到运动模糊、球贴球场白线伪装、球员/球网遮挡、光照突变、机位微动 —— **实测应预期在公开指标基础上下降 10–25 个百分点**。所以方案里的验收标准不写成"单帧精度"，而写成**"回合切分召回率 ≥90% / 精确率 ≥85%"**（第 12 节按业余素材再下调后的目标），因为这才是用户能感知的指标。

**关键认知：盯错指标会做错产品**

"逐帧球定位准确率"高 **≠** 能正确切分回合。真正决定成败的是：

- **最长连续轨迹段是否覆盖该回合的关键节点**（首拍、每次方向突变、落地）；
- **不可见段是否被正确标记为 UNKNOWN**，而不是被插值成"球停在原地"（这是把回合结束判错的头号原因）；
- **球在画面外的时段**（业余机位常见）能否靠音频证据补上。

因此工程上要额外维护三个自测指标：`轨迹覆盖率`（可见帧占回合帧比例）、`最长断裂时长`、`落地事件召回率`。

#### ③-补二 分辨率与 720p–4K 支持策略（另一重点）

**结论先说：导入支持 480p–4K（含竖屏），导出支持 720p–4K；但"分析分辨率"与"视频分辨率"必须解耦 —— 直接喂 4K 全帧反而会降低球轨迹精度。**

**为什么 4K 全帧直接缩放会变差（数学原因）**

TrackNet 系输入固定 **512×288**。把整帧等比缩放时，画面信息被压缩的倍率差异巨大：

| 源分辨率 | 缩放面积压缩比 | 720p/1080p/4K 相对彼此的"每单位模型输入的原始像素量" |
|---|---|---|
| 1280×720 | ≈ 1.46× | 1.0×（基准） |
| 1920×1080 | ≈ 2.42× | 1.66× |
| 3840×2160 | ≈ 7.70× | **5.28×** |

也就是说：**4K 源在送进模型前丢了 92% 的原始像素**，球与其周围像素（球场白线、观众席高光、地板反光）被平均到一起，热图上的峰会被背景"糊掉"。反直觉但成立：**不是分辨率越高识别越好，而是"球在模型输入尺度上占几个像素"才决定成败**。

**推荐方案：分层 + ROI 二级推理（这是 4K 的正确打开方式）**

```
第一级（低延迟、全场覆盖）
  4K 帧 ──► 缩放 512×288 全帧 ──► TrackNet（轻量版 TrackNetV2/V4 主干）
           目的：拿到候选区域 + 大致的球运动轨迹；允许漏检，只要求"圈出可能的位置"
                │
                ▼
第二级（高精度、按需触发）
  在 4K 原始帧上按候选开 640×640 或 512×512 的 ROI（滑窗/预测位置跟随）
            ──► 同一 TrackNet 权重，但输入采样率从 7.7× 降到 1.7×~1.9×
            ──► 热图峰值更尖锐、定位误差更小、对模糊更鲁棒
                │
                ▼
  轨迹级融合：两级结果按置信度与一致性合并；ROI 内命中 → 采纳；只在全帧命中 → 降级标记
```

- ROI 跟随策略：用上一帧位置 + 卡尔曼/轨迹外推预测下一帧 ROI 中心，**只在"连续 N 帧都没命中"时才回到全帧搜索**。这样 4K 的算力开销从"全场 7.7× 信息量"降到"ROI 1.8× 信息量"，**总耗时增加约 30–60%，而不是 4–7 倍**。
- 更省钱的替代：ROI 内用 **KCF/MOSSE 这类经典相关滤波跟踪器**（CPU 上 <2 ms/帧）做帧间跟随，只在间歇用 CNN 校正漂移。适合算力紧张的 Windows 笔记本与安卓端。
- 如果只支持 1080p，则**全帧 512×288 + 单级推理即可**，无需 ROI（1080p 的 2.42× 压缩比还在可接受范围）。

**导入侧策略**

| 源 | 处理方式 |
|---|---|
| 480p–720p | 直接全帧推理；无需 ROI；精度预期中等（球在 720p 上仅约 3 px，靠多帧时序补足） |
| 1080p | **主目标规格之一**：全帧推理 + 可选 ROI 精修。球约 5–8 px |
| **4K（主目标规格之一）** | ⚠️ **默认必须走两级 ROI 流水线**（用户素材 1080p–4K 都有，4K 是常态而非边缘情况）。强烈建议硬件解码（NVDEC/QSV/AMF/MediaCodec）与硬件编码 |
| >4K / 8K | 不承诺性能；建议预处理降到 4K 以下再导入，UI 明确提示 |
| 竖屏/非 16:9 | 支持。保持等比 + letterbox 到 512×288，ROI 用原始宽高比，坐标反变换时按 padding 偏移还原 |
| VFR（可变帧率） | **必须先转 CFR** 并重建帧号索引，否则"帧号 ↔ 时间戳"会漂移，切片边界全错 |
| HDR（HLG/PQ） | 支持导入，但分析前必须 tone-map 到 SDR，否则对比度异常导致热图失效 |

**导出侧策略（三种模式）**

| 模式 | 规格 | 用途 | 代价 |
|---|---|---|---|
| A 无损流复制 | 保持源分辨率/编码/码率（`-c copy`） | 存档、进 PR 精剪前的素材 | **切点必须落在关键帧**，头尾可能多 0.5–3 s |
| B 重编码片段（默认） | 源分辨率或指定到 720p/1080p/1440p/4K；H.264 High 或 HEVC；可烧字 | 日常交付、发学员、社交平台 | 需转码；4K 片段较慢 |
| C 统一批次 | 全部导出 1080p / 8–12 Mbps | 一次导出上百个回合、体积可控（1 个 20 s 回合 ≈ 20–30 MB） | 清晰度低于源 |

- 编码器与硬件加速：Windows 走 NVENC / QSV / AMF（4K60 实时编码可行），无硬件时回退 x264 `slow` 预设（4K 约 0.2–0.5× 实时）；Android 走 MediaCodec。
- 音频：AAC 192 kbps 立体声（或直接复制源音轨）。**v1 分析链路默认不使用音频**（第 2.2 ④-补二），音频只随片段复制，不做处理。
- **烧字在 4K 下代价显著**（`drawtext` + 编码叠加）。由于**比分为非核心、甚至可不显示**，建议：**v1 默认只烧"拍数 / 时长 / 序号"，不烧比分**；4K 场景优先出"烧字版 1080p + 不烧字版 4K 原分辨率"双产物。
- **磁盘预算提示（必须算给用户看，按单局 10 分钟口径）**：4K60 HEVC 源约 2–4 GB、4K60 H.264 约 4–8 GB；导出全部选中片段（约 12–20 个球）另需 0.5–2 GB；代理（720p）约 20–60 MB。→ **单局总占用 3–10 GB，普通电脑完全可行**（这与第 8 节"60 分钟长视频写满盘"的结论不同，因为素材短了 6 倍）。
- **性能红线（按单局口径重算）**：4K 两级 ROI 分析在消费级独显上约 **3–8 分钟**（10 分钟素材），纯 CPU 不现实（约 40–90 分钟）。因此 UI 应在导入时按本机能力给预估时间，并让用户选档：**快速 720p 粗筛 / 标准 1080p / 精细 4K-ROI**。
- **对 4K 的算力优化建议**：第一遍**直接在 720p 上全帧跑**（不含 4K 解码之外的额外代价），第二遍只对候选段回原 4K 帧做 ROI —— 这样 4K 的额外成本只落在"候选段的少数帧"上，而不是整段视频。

#### ④ 击球 / 发球 / 未击球 / 回合结束的事件判定（轨迹为主 + 静止期锚点）

**证据链 1：球轨迹运动学（主证据）**

从 `(x, y, t)` 序列计算速度 `v`、加速度 `a`、方向角 `θ`、曲率。典型模式：

- `vy 反转 + 速度局部极小→突增` → 球被击打（方向突变）；
- `速度骤降 + vy 趋零 + 空间位置落在场地内/外` → **落地（回合结束）**；
- 🔴 `visibility 持续为 0 且轨迹到达画面边缘` → **出画**。⚠️ **出画绝不等于回合结束** —— 见下方"出画的两种类型"。

**🔴 出画的两种类型（本次修复的致命错误之一）**

原方案把"球出画"直接当成回合结束触发条件，这是**错误的**：羽毛球最高频的两条球路（高远球、挑后场）**必然经过画面上方出画再入画**，而本方案机位是"三脚架、略高于网带"，画面顶部通常就在底线附近 —— 于是**每一条正常的高远球都会被切成两段**，后半个回合还可能被标成 `非回合`，拍数腰斩。

必须把出画拆成两类，且**判定标准是"是否在无落地、无静止期的前提下重新入画"**：

| 类型 | 判定 | 处置 |
|---|---|---|
| **`OUT_OF_FRAME_TRANSIENT`（瞬时出画）** | 出画后 **≤ T 内重新入画**，且中间**无落地事件、无静止期**（T 默认 **3.0 s**，可调 2.5–3.5 s） | ✅ **回合继续**。该段轨迹记为 UNKNOWN，拍数用"入画前后方向一致性"做补 |
| **`OUT_OF_FRAME_TERMINAL`（终局出画）** | 出画后超过 T 未回，**或**伴随静止期 / 落地事件 | 回合结束，标 `UNKNOWN`，交人工确认 |

- **T 的取值依据**：高远球滞空约 2–3 s，过顶段占大半，故 3.0 s 能覆盖绝大多数过顶球；若素材机位更高（画面顶部离底线更远），过顶段更短，T 可下调；
- **为什么"无落地、无静止期"是关键**：出画期间"球不可见"恒成立，若不校验这两点，一个正在飞行的回合会被误判成回合间（见 ④-补 的静止期判据）；
- **与 P1 的关系（重要）**：`TRANSIENT` 出画造成的断裂是**物理上合法且必然发生**的（1–2.5 s），所以 **P1 的"最长断裂"指标必须改为分类指标** —— 分别统计"可解释断裂（瞬时出画/遮挡）"与"不可解释断裂（漏检导致的轨迹断开）"，**只对后者设门槛**（见第 16.2 节 P1）。原方案"最长断裂 <0.5 s"的单值门槛会让 P1 在**最正常的素材上判失败**。

- **发球识别**：发球 = 回合第一拍，特征为"球自静止点启动、初始高度低、此前有就位停顿 + 发球准备姿态"。所以"发球"不靠单帧姿态分类，而靠**回合起点 + 静止期结束后的首次轨迹出现**联合判定（见 ④-补）。

**证据链 2：击球音（v1 保留为"零成本校验通道"）**

> ⚠️ **本次修复（原方案在此处走向了另一个极端）**：先前因顾虑**音画偏移**，一度把音频整体降为"可选增强、v1 不使用"。这是**矫枉过正**：
> - **音画偏移是"每素材一次性可估的全局常量"**（互相关估一次 + 人工确认一次），不是持续开销；
> - **音频是唯一能在"球出画 / 被遮挡"时提供独立模态的证据** —— 而这恰好是本方案最薄弱的场景（D10：球会出画；双打遮挡频繁）。

**因此 v1 的正确处置是：保留"零模型成本"的音频通道，但只用于校验与提示，不承担主判据职责。**

| 用途 | v1 是否启用 | 说明 |
|---|---|---|
| 断裂提示（辅助判断出画是 TRANSIENT 还是 TERMINAL） | ✅ **启用** | 出画期间若听到击球音 → 说明是飞行中，倾向 `TRANSIENT` |
| 拍数校验 / 补断裂处拍数 | ✅ 启用（仅提示，不自动改数） | 与轨迹事件不一致时在审核页高亮 |
| 标记"镜头外击球" | ✅ 启用 | 轨迹断裂 + 有击球音 → 提示"可能是出画期间的击球" |
| 作为回合边界的主判据 | ❌ **不启用** | 主判据仍是轨迹 + 静止期 |
| 自动音画偏移估计 | 🔻 增强档（v2） | v1 提供手动微调滑杆 |

- 做法：音频 16 kHz 单声道 → 4 kHz 以上带通/能量包络 → 峰值检测 → 得到候选击球时间戳；
- **必须先用上述滑杆把音频对齐到轨迹突变上**，再启用校验；未对齐时不参与任何判定；
- 这条链**不需要任何模型**，成本极低，是"性价比最高的校验手段"。

**证据链 3：姿态（辅助证据）**

- 只在"候选击球前后 ±0.5 s 窗口"内跑姿态，**不必全帧全时跑**，可省 60–80% 算力。
- 提取手腕速度峰值、肘肩角度变化、重心位移、双脚起跳/跨步 —— 用于判定"人是否在击球位置/是否只是捡球"。
- 用轻量时序分类器（1D-CNN / GRU / ST-GCN，<5 MB）把"击球/发球/挥空/非动作"四分类，输入是骨架序列而非 RGB。
- 文献参考：[BST: Badminton Stroke-type Transformer](https://huggingface.co/buckets/huggingchat/papers-content/tree/2502/2502.21085.md)（骨架+Transformer 做挥拍类型分类）、[ShuttleSet](https://arxiv.org/abs/2306.04948)（KDD'23，104 局 / 3685 回合 / 36492 拍的人工标注数据，含击球类型 18 类与双方站位，是本项目最有价值的公开标注来源）。

**回合（Rally）切分状态机**（已按 D9 + 出画修复 + 静止期加权判据重写）：

```
IDLE ──(静止期检出：球不可见 ∧ 球场ROI帧差低，≥2.5s)──▶ RALLY_ACTIVE(拍数=0，待首拍)
        （不再需要 SERVE_READY：原"球在发球区静止/被检出"与"静止期=球不可见"逻辑互斥，
          且 TrackNet 的背景中值差分恰恰会减掉静止物体，故删除该状态）
RALLY_ACTIVE ──(首拍事件：静止期后首次轨迹出现 + 方向突变)──▶ RALLY_ACTIVE(拍数=1)
RALLY_ACTIVE ──(击球事件：速度/方向突变)──▶ RALLY_ACTIVE(拍数+1)
RALLY_ACTIVE ──(落地事件：速度骤降 + vy 归零)──▶ RALLY_END(落点可判定)
RALLY_ACTIVE ──(出画 + ≤3.0s 内重新入画 且 无落地/无静止期)──▶ RALLY_ACTIVE  ← 瞬时出画，回合继续
RALLY_ACTIVE ──(出画 + >3.0s 未回 或 伴随静止期)──▶ RALLY_END(UNKNOWN，需人工)  ← 终局出画
RALLY_ACTIVE ──(≥2.5s 无轨迹 ∧ 静止期检出)──▶ RALLY_END(UNKNOWN)   ← 不再要求"无音"
RALLY_END ──(≥0.8s 冷却，防抖)──▶ IDLE
```

> ⚠️ **两处关键修复**：① 删除 `SERVE_READY` 状态（其进入条件与静止期定义互斥）；② **出画分两类**，只有"超时未回或伴随静止期"才结束回合（否则高远球过顶会被切成两段）。详见 ④ 的"出画的两种类型"。
> **注意**：状态机中**不再出现"无音"** —— v1 的音频只作校验通道，不作为状态转移条件。

**回合片段边界**（以 ④-补 的四个时间锚点为准）：

```
回合起点 = serve_contact − 1.5~2.0s   （覆盖发球准备；serve_contact = 静止期结束后首次轨迹出现）
         且 ≥ quiet_end + 0.2s        （下界保护：不越过静止期，避免把捡球包进来）
回合终点 = rally_end + 1.5s
```

> ❌ **已作废的旧公式**：`回合起点 = 最后一个击球事件 − 1.2s`。它有两处错误：① 用"最后一个击球"（应为"第一个/首拍"）；② 前推量 1.2 s 不足以覆盖业余发球准备。以 ④-补 的定义为准。

#### ④-补 回合边界与"发球窗口期"的精确定义（重点）

> 这一节回应一个关键点：**球的轨迹是从"球离开拍面"才开始的，但回合的起点应当往回覆盖发球准备动作**。若起点等于"首拍事件"，用户会看到"球凭空飞出来"的观感；若起点等于"上一回合结束"，又会把十几秒的捡球/走动包进来。这中间的窗口期必须用规则精确定义。

**四个时间锚点（必须区分开）**

| 锚点 | 定义 | 用途 |
|---|---|---|
| **静止期结束点（quiet_end）** | 双方停止明显移动、且**连续 ≥2.5 s 无球、无击球事件**的最后时刻 | 划分"回合间"与"回合内"的物理边界 |
| **发球准备起点（serve_prep）** | 发球方开始做发球动作（重心前移、抬拍、举球）的时刻；**这是"未击球"阶段的视觉起点** | **回合起点的基准** |
| **发球时刻（serve_contact）** | 球离开拍面的瞬间 —— 即**首拍事件** | 拍数计数从 1 开始的位置 |
| **死球时刻（rally_end）** | 落地 / 出界 / 下网 / 长时间不可见 + 静止 | 回合终点基准 |

**关键结论：回合起点 ≠ 首拍事件，而是"首拍事件往回推一个发球窗口期"。**

```
回合起点 = serve_prep - 0.3s               （给准备动作再留余量，避免动作被切掉）
         ≈ serve_contact - 1.5 ~ 2.0s      （业余发球动作通常 1.0–2.5 s）
         ≥ quiet_end + 0.2s                （下界保护：绝不能越过静止期结束点，否则会把捡球包进来）

回合终点 = rally_end + 1.5s                （保留落地后的反应/庆祝）
```

- **默认提前量 2.0 s**，设置里可调 1.0–3.0 s；
- **两个约束必须同时满足**：既覆盖发球准备，又不跨过 `quiet_end`。当发球动作特别长时（业余抛球反复很常见），以"`quiet_end + 0.2s`"为上限，多余部分宁可不要；
- **实现上不依赖"识别谁是发球方"**（视觉上很难判且无必要），而是以**"静止期结束后第一次轨迹出现"作为 `serve_contact` 的回推基准**，再往前推固定窗口。规则简单、鲁棒，单打双打都适用。

**"静止期"如何检测（切分质量的关键，也是固定机位场景白送的强信号）**

`quiet` 的判定**是加权的，不是三个条件并列**（原"三者同时成立"的写法不成立 —— 单打球员一直在小步调整、双打 4 人同时在动，"球员静止"根本不是可靠条件）：

| 信号 | 权重 | 说明 |
|---|---|---|
| **球不可见 / 近乎静止**（连续 ≥2.5 s 位移低于阈值） | 🟢 **主判据** | 轨迹层直接给出，最可靠 |
| **球场区域帧间差分低** | 🟢 **主判据** | 固定机位下极可靠，且**几乎零成本**；注意只统计球场 ROI 区域，避开观众席与顶灯闪烁 |
| 球员活动量低 | 🟡 **弱提示（不作门槛）** | 只用于消歧（例如"球不可见但人还在跑动"时降低置信度），**不参与硬判定** |

> 换言之：**`quiet = (球不可见) AND (球场帧差低)`，球员活动量只调置信度。** 双打场景下这条修正尤其重要。

**为什么强调这一条**：用户素材是**三脚架拍摄**，因此"球不在 + 球场帧差低"是极强的"回合间"判据。**它比"识别发球动作"简单得多，也比"检测死球"更可靠** —— 因为回合间的静止是结构性的，而落地判定容易受遮挡/出画影响。建议把**静止期检测作为第一道锚、轨迹事件作为第二道锚**，两者交替校正。

**与用户原始判断的对应**：用户指出"球发出来进入轨迹期间有一段时间" —— 正是上述 `serve_prep → serve_contact` 的窗口期。方案采纳：**回合起点往前推约 2 s**，并用 `quiet_end` 做下界保护。

#### ④-补二 音画偏移：主证据链改为"轨迹优先"（已确认决策）

**决策：本方案不以音频作为主判据，音画偏移不构成阻塞风险。**

理由（用户已确认的实际条件）：

- 手机录制的音画偏移可达**数十至数百毫秒**，且可能随录制过程漂移，逐素材估计代价高；
- 而**球轨迹本身就能同时给出"击球时刻"（方向突变）与"发球时刻"（轨迹起点）**，信息量足够；
- 因此**把音频降级为"可选增强 + 辅助校验"**：仅在音频偏移被验证过（或用户手动微调过）时才启用。

**证据权重重新分配**

| 证据 | 原定位 | **新定位** | 作用 |
|---|---|---|---|
| **球轨迹运动学** | 主证据 | ✅ **唯一主证据** | 回合边界、击球次数、发球时刻、死球判定**全部由它给出** |
| **静止期 / 画面稳定** | 未单列 | ✅ **第一道锚（并列主证据）** | 划出"回合间"，为轨迹提供起止保护；对固定机位极可靠 |
| 击球音 | 强证据 | 🔻 **可选增强 + 校验** | 仅在验证过偏移后启用：补断裂处的拍数、校验轨迹事件、标记"镜头外击球" |
| 姿态 | 辅助 | 🔻 辅助（不变） | 判断"是否在击球位置"，不用于计数 |

**音频可选的启用级别（建议做成三级）**

| 级别 | 条件 | 音频用途 |
|---|---|---|
| **默认（v1）** | 不做任何偏移估计 | ❌ 不使用音频，纯轨迹 + 静止期 |
| 增强 | 用户拖动滑杆把"击球音"对到轨迹突变上（或互相关估计 + 人工确认） | ✅ 补拍数、校验 |
| 自动 | 音画偏移估计置信度高（互相关峰值显著） | ✅ 同增强，且自动完成 |

**这条改动带来的连锁收益**

1. **架构简化**：不再需要"音频与视觉候选做 ±100 ms 容差对齐"这一整套逻辑及其背后的偏移估计，少一个易错环节；
2. **M-1 预研的 P2 从"阻塞项"降为"增强项"**：决策门只看 **P1（轨迹召回 ≥85%）** 与 **P3（半场标定）**（见第 16.2 节）；
3. **粗筛档更快**：第一遍可以完全不解码音频。

> ⚠️ 保留的注意点：将来若启用音频（增强档），**仍必须先估计并补偿全局音画偏移**（第 16.7 节），否则会静默降低准确率。此条不删除，只是不再阻塞 v1。

#### ⑤ 计分（Scoring）—— 已简化为人工三态（原双轨制作废）

> **状态（D1）**：原"轨道 A 自动推算 + 轨道 B 比分板 OCR"的**双轨制已作废**（比分非核心需求，用户甚至可选择不显示）。现行方案见 **第 9.4 节**：用户对每个回合点选 `A方得分` / `B方得分` / `非回合`，程序只负责规则状态机与（可选的）比分显示。
>
> 以下仅作历史记录，说明当初为何放弃自动计分：
>
> - 轨道 A 依赖**落点判定**（标定精度 + 轨迹末端精度），是整条链路最脆弱的一环，判错会连锁污染整局比分；
> - 轨道 B 依赖**比分板 OCR**，而业余单局素材**没有比分板**；
> - 结论：两者投入产出比都远低于"人工点三下 + 规则引擎"，故剥离。

**人工确认（保留，且是 v1 的核心）**：结果页每个回合三个按钮（A 得分 / B 得分 / 非回合），**连播中即可标记**，全操作可撤销（详见第 9.4 节）。

### 2.3 判据总表：在"识别什么"上的最终回答

| 要判定的类别 | 主判据 | 校验判据 |
|---|---|---|
| 要判定的类别 | 主判据（轨迹 + 静止期） | 可选增强（音频/姿态，需先验证偏移） | 置信度 |
|---|---|---|---|
| 是否击球 | **球速/方向突变**（唯一主判据） | 击球音时间戳（需先对齐） | 0.9（仅轨迹）；音频一致时 0.95 |
| 是否发球 | **是否为回合首拍 + 球自静止启动**（不依赖识别"谁是发球方"） | 发球准备姿态（重心下降、拍在身前） | 0.85 |
| 是否未击球/挥空 | **球轨迹无方向突变**且球可见 | 有挥拍姿态但无音频峰值（音频可选） | 0.7（低置信，不计入拍数） |
| **回合起点** | **`serve_contact` 往前推 1.5–2.0 s，且不越过 `quiet_end + 0.2s`** | 发球准备动作的时间范围 | — |
| **回合同隔（回合间）** | **静止期：球不可见/近乎静止 ≥2.5 s ∧ 球场 ROI 帧间差分低**（两项为主判据） | 球员活动量**仅调置信度、不作门槛** | 0.9（三脚架机位下最可靠） |
| **球出画（瞬时）** | **≤3.0 s 内重新入画 ∧ 无落地 ∧ 无静止期** → 回合继续 | 出画期间有击球音 → 倾向瞬时 | 0.8 |
| **球出画（终局）** | **>3.0 s 未回 或 伴随静止期** → 回合结束（UNKNOWN） | 无击球音 | 0.6（必须人工确认） |
| 回合结束（落地） | 落地（速度骤降 + vy 归零） | 双方进入静止状态 ≥2.5 s | 0.85 |

> **判据优先级说明（重要）**：本表按"**轨迹 + 静止期**"双主证据组织。**音频与姿态都是可选增强**，因为手机录制存在音画偏移（见 2.2 ④-补二）。这一改动同时简化了架构：v1 不需要音频-视觉对齐逻辑。

---

## 3. 最终交付程序的执行逻辑

### 3.1 用例主流程（用户视角）

```
导入视频(文件/相册)
   │
   ├─ 自动探测规格 → VFR 转 CFR / 旋转元数据解析 / HDR tone-map / 按本机能力给分析时长预估
   │
   ├─ 【预处理】采样背景中值图 / 场地标定（三脚架可一次复用，含手动微调）
   │     ──► UI 显示标定叠加图，"确认/手动微调"按钮
   │     （注：v1 默认不处理音频，见 D9）
   │
   ├─ 【分析·第一遍 粗筛】720p 全帧球轨迹（轻量模型）→ 静止期检测 → 候选回合段
   │
   ├─ 【分析·第二遍 精算】仅对候选段回原始分辨率（4K 走 ROI）用 V3/InpaintNet 复算
   │     ① 轨迹精算  ② 事件候选  ③ 姿态校验(可选)  ④ 回合状态机
   │     ⑤ 主校验：静止期结构 + 发球节奏自洽性
   │
   ├─ 【粗切片（不产生文件）】分析结果直接写出"时间索引表"
   │     每回合 = {start_ms, end_ms, shot_count, ...} —— 零编码、零磁盘占用
   │     可选：生成「一整支低码率代理视频」+ 每回合一张缩略图（见 3.3 节）
   │
   ├─ 【审核/预览】回合列表页：每回合 卡片(序号|拍数|时长|置信度|缩略图)
   │     （比分列默认隐藏，可在设置中开启，见 D11）
   │     低置信/结构异常回合高亮 → 一键"重判/合并/拆分/标记为非回合(捡球)"
   │
   ├─ 【回看】分段播放器：按 start_ms seek 播放到 end_ms 自动跳下一条
   │     连播 / 单回合循环 / 慢放(0.25x–2x) / 逐帧 / 轨迹叠加
   │
   └─ 【导出（唯一次真正编码）】按三个预设之一生成成片
         ① 单球导出   ② 精彩集锦（标记为精彩球者）   ③ 垃圾时间压缩（全部对抗回合）
         + 压缩力度三档 / 分辨率(含 4K) / 竖屏或方形模板
         + 烧字（默认不烧比分）/ 拼接点音频交叉淡化
```
```

**核心原则：分析与预览全程"非破坏性、不产生视频文件"；唯一的编码动作发生在最后一步导出。** 这与剪辑软件的"离线剪辑 → 最终渲染"完全一致，详见第 3.3 节。

### 3.2 内部分层与线程模型

```
┌─────────────────────── UI 层 (Qt6/QML 或 Compose) ───────────────────────┐
│  视频库 | 分析进度 | 回合列表(可拖拽排序) | 播放器 | 暂存观看列表 | 导出向导  │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │  命令 / 事件（不含阻塞调用）
┌───────────────────────────────▼─────────────────────────────────────────┐
│  应用服务层：ProjectService / AnalysisJob / PlaylistService / ExportJob   │
│  状态机：DRAFT → ANALYZING → REVIEW → FINALIZED → EXPORTING               │
└───────┬──────────────────────────────┬──────────────────────────────────┘
        │                              │
┌───────▼─────────────┐   ┌────────────▼──────────────────────────────────┐
│  推理引擎 (IPC/FFI)  │   │  媒体引擎 (FFmpeg)                             │
│  - Runtime 抽象层    │   │  - demux / decode / scale                     │
│    ONNX-RT / LiteRT  │   │  - 硬件编码 NVENC/QSV/AMF/MediaCodec           │
│    / NNAPI / DirectML│   │  - 音频解码 + 16k 单声道重采样                 │
│  - 模型池(姿态/球/事件│   │  - 关键帧索引 / 精确 seek / 流复制              │
│    /OCR)             │   │  - 烧字滤镜(drawtext) / 拼接 / 混流             │
│  - 批处理 + 帧缓存    │   └───────────────────────────────────────────────┘
└───────┬─────────────┘
        │
┌───────▼─────────────────────────────────────────────────────────────────┐
│  数据层：SQLite (project.db) + 文件系统                                   │
│  project(id,name,created_at,fingerprint,proxy_path,analysis_state)  ← 补  │
│  video(id,project_id,path,fps,w,h,duration,rotation,calib_json)          │
│  rally(id,project_id,video_id,idx,order,start_ms,end_ms,stroke_count,    │
│        outcome,outcome_source,outcome_confidence,serve_side,             │
│        confidence,review_state,thumbnail_path)                          │
│  stroke(id,rally_id,idx,frame,t_ms,type,confidence,visibility_raw,ball_x,ball_y)│
│  playlist(id,project_id,name) → playlist_item(playlist_id,rally_id,order) │
│  evidence(rally_id,kind,payload)  ← 轨迹/姿态窗口/静止期区间等可回溯证据    │
└─────────────────────────────────────────────────────────────────────────┘
```

> **说明**：
> - `project` 表是"一个项目 = 一个视频 + 一次分析 + N 个回合 + M 个播放列表"的根实体；`fingerprint` 用于原片被移动/重命名后重新关联项目（第 16.6 节）；`rotation` 记录手机竖拍的旋转元数据（第 16.7 节）。
> - ⚠️ **`stroke.visibility_raw` 必须与修复后的坐标分开存**（第 17.3 节 T6）：TrackNetV3 的 InpaintNet 会**修补漏检段**，修补出的点不代表真实可见。**判死球/回合结束必须用原始可见性**，轨迹平滑与拍数补齐才用修复后的坐标。混用会把"看不见"误判成"球在动"。

设计原则：

1. **UI 与分析完全解耦**：分析跑在独立进程/工作线程池，UI 只订阅进度事件；分析可取消、可断点续跑（中间结果落盘 CSV/DB，重跑时跳过）。
2. **引擎可替换**：`InferenceBackend` 接口抽象（CPU / CUDA / DirectML / NNAPI / CoreML / OpenVINO），换平台不改业务代码。
3. **原始视频零拷贝（改为强制）**：回合片段一律用 `video_id + start_ms + end_ms` 表达，**分析与预览阶段绝不产生视频文件**；物理文件只在"导出"时产生（详见第 3.3 节）。
4. **一切判定可回溯**：每个回合保留其证据（球轨迹片段、音频峰值、姿态窗口、置信度），UI 上可展开查看"为什么这样判"，否则用户无法信任自动结果。

### 3.3 粗切片与最终渲染分离（剪辑软件式工作流）

> **这是本方案的核心架构决策之一**，与剪辑软件的"离线剪辑（offline edit）→ 最终渲染（online render）"完全同构。

#### 3.3.1 三个层次，只有最后一层产生文件

| 层次 | 操作 | 是否编码 | 产物 | 耗时 | 磁盘 |
|---|---|---|---|---|---|
| **① 粗切片** | 分析结果直接写"时间索引表" | ❌ **零编码** | 数据库里 20–50 行记录 | 0 | 0 |
| **② 预览** | 播放时按 `start_ms` seek，播到 `end_ms` 自动跳下一条 | ❌ **零编码**（可选生成一支代理视频） | 屏幕上的画面 | 即时 | 可选 ~50–150 MB |
| **③ 导出** | 按当前设置批量渲染选中回合 | ✅ **唯一一次真正编码** | N 个 MP4 / 合并长片 / ZIP | 20–60 s（10 min 素材） | 视设置而定 |

**关键认知：粗切片根本不是"切"，它只是一个时间索引。** "切片"这个词容易让人以为要立刻生成文件，其实在审核阶段完全不需要——用户想看的只是"第 3 个球从 12.4 s 到 27.1 s"，播放器 seek 过去就行。

#### 3.3.2 为什么不要"每回合生成一个代理片段"

这是原方案（第 3.3 节旧版）的一个**错误直觉**，必须纠正。对比：

| | ❌ 每回合一个代理片段 | ✅ 一支全片代理 + 索引播放 |
|---|---|---|
| 编码次数 | N 次（N=20–50），每次都要重开编解码器 | **1 次** |
| 总耗时 | 编解码器反复启停，开销叠加；10 min 素材约 1–3 min | **10–30 s**（硬件编码） |
| 文件数 | 40+ 个 MP4 | **1 个** |
| 顺序调整 | 需要重排文件名/播放列表 | **改一个整数就完了** |
| 标记为"非回合" | 要删文件或留垃圾 | **改一个状态位** |
| 切分点调整 | 必须重新编码 | **改两个时间戳** |
| 磁盘 | 40 个文件的封装开销 | 一支连续文件，无浪费 |

**结论**：单视频单局的场景下，"一支代理 + 时间索引"在所有维度上都优于"每回合一个片段"。**顺序、删除、合并、拆分、改边界这些操作全部变成纯数据操作，零编码成本**——这正是"粗剪"应该有的手感。

#### 3.3.3 预览的技术实现

1. **播放源选择（自动）**：
   - 默认**直接播原片**（最省事、零额外磁盘）。MP4/MOV 自带关键帧与采样表索引，seek 是毫秒级。
   - 当满足任一条件时**自动生成一支代理**：**源为 4K60（D14 下这是常态，故实际等于"默认生成"）** / 机器较弱 / 源码率过高导致 seek 卡顿。代理规格建议 **720p / 4–8 Mbps**（10 分钟素材约 30–90 MB）。
   - 代理生成是**一条后台任务**，用户可在它生成完之前先用原片看（进度不阻塞）。
2. **精确 seek 的处理**：seek 到 `start_ms` 时解码器必须从**它之前最近的关键帧**开始解，再把多余的帧丢掉。这是播放器标准行为，但要保证：
   - 输出的第一帧**不早于** `start_ms`（否则用户会看到上一个回合的尾巴，尤其在 GOP 较长时明显）；
   - 导出时同样按此规则取帧，保证"预览所见 = 导出所得"。
3. **连播的衔接**：在当前回合结束前 **~0.5 s 预解码下一个回合的起手关键帧**，避免每次切换时出现黑帧或卡顿。这是分段播放体验好不好的分水岭。
4. **缩略图**：每个回合一张 160×90 / 320×180 JPEG，从代理（或原片）seek 到回合中点生成，**这一项值得做**——它是"秒级浏览 40 个球"的关键。
5. **音频**：seek 时音频同样从关键帧对齐；波形峰值（击球音）只作为刻度标记叠加在时间轴上，不需要额外文件。

#### 3.3.4 导出阶段（唯一一次编码）

导出是**项目数据的纯函数**：`导出结果 = f(回合表, 顺序, 导出设置)`。这个性质带来三个好处：

- **可重复、可撤销**：改了设置重新导出即可，源素材零损伤；
- **可增量**：只导出被修改过的回合（缓存已渲染结果，按 `rally_id + 设置哈希` 做键）；
- **可中断恢复**：一个个回合独立编码，某个失败不影响其他，失败项可单独重试。

导出设置（用户可保存为预设）：

| 设置项 | 选项 |
|---|---|
| 范围 | 全部 / 仅 A、B 得分回合 / 仅非回合 / 筛选结果 / 暂存列表 / 手动勾选 |
| 分辨率 | 源分辨率（**4K60，可选无损流复制**）/ 1080p / 720p / 竖屏 1080×1920 / 方形 1080×1080 |
| 编码 | H.264 High（兼容优先）/ HEVC（省空间）；**4K60 必须硬件编码**，无硬件时明确警告耗时；软件兜底 |
| 烧字 | 拍数 / 时长 / 序号（**默认开**）/ 比分（**默认关**，见第 16.9 节 D8）；逐项开关、位置、字号 |
| 模板 | 横屏 / 竖屏 / 方形；留白或模糊背景 |
| 产物 | 独立片段 / 合并长片 / ZIP 打包 / 两者都要 |

> **成本提示（必须让用户看到）**：导出前显示"将渲染 12 个片段，预计 25 秒，约 340 MB"，并把"源分辨率无损流复制"标注为**最快但切点受关键帧限制（可能多出 0.5–3 s）**。用户对"为什么这个模式快但不精确"必须知情。

#### 3.3.5 这个架构在业余单局场景下的额外收益

- **总编码量从"素材 2–3 倍"降到"素材 1 倍 + 导出量"**：10 分钟素材大约只编码 10 分钟（代理）+ 3–5 分钟（导出选中的片段）。
- **分析完立刻可看**：粗切片零编码，意味着分析一结束用户就能连播审核，**不需要等任何"切片中"的进度条**——这对"30 秒内看到这场有几个好球"的体验目标至关重要。
- **敢做两遍处理**（第 12.2 节）：既然预览不产生文件，第一遍粗筛 + 第二遍精算就可以放心做，没有中间产物堆积。

### 3.4 切片顺序管理与临时观看

- **回合表**是唯一真相：`order` 字段 + 拖拽重排；重排只改序号，**不涉及任何文件操作**（这也是 3.3.2 节"一支代理 + 索引"带来的直接好处）。
- **暂存观看列表（Playlist / 临时篮）**：任意把回合卡片拖入"临时列表"，支持：连播、随机、只看这个列表、只导出这个列表、清空。实现上就是 `playlist` + `playlist_item` 两张表，与"回合表"解耦 —— 这样"临时看一下这几个球"不会污染正式切片顺序。
- **代理视频是可选优化项**，不是必需品；它只影响预览流畅度，不影响任何功能正确性。
- 标注状态：每回合可打标签（`有效/捡球/擦汗/争议球/精彩球`、`我方得分/对方得分`），标签参与筛选与导出；**标签与顺序都只是数据库字段，随时可改、零成本**。

---

## 4. 性能与体积预算

### 4.1 模型清单（这是体积的主体）

| 模型 | 用途 | 量级 | 参数量参考 |
|---|---|---|---|
| YOLO11n-pose / RTMPose-tiny | 球员检测 + 17 关键点 | 5–15 MB | 3–6 M |
| 小关键点网（场地线） | 场地标定 | 2–8 MB | 1–3 M |
| TrackNetV2 / V4（+Motion） | **第一遍粗筛**（720p 全片） | 5–15 MB | — |
| TrackNetV3 (TrackNet) | **第二遍精算**（候选段 + 4K ROI） | 20–40 MB | — |
| TrackNetV3 (InpaintNet) | 轨迹修复（候选段） | 10–25 MB | — |
| BounceNet / 事件分类 | 落地/击球/none 过滤 | 1–5 MB | <1 M |
| 骨架时序分类器 | 击球/发球/挥空 四分类（**二期**） | 2–6 MB | <2 M |
| ~~比分牌数字 OCR~~ | **已砍掉**（第 12.6 节） | — | — |
| **合计（v1 实际需要）** | | **≈ 40–95 MB**（int8 量化后可再砍 50–65%） | |

> **v1 的模型选型建议（"两遍处理"的具体用法 —— 已按第 17.2 节的代价实算修正）**：
> - **第一遍（粗筛，全片 720p，可跳帧）**：TrackNetV2 或 TrackNetV4（V2 + 运动注意力）—— 轻量。作用不止"圈出候选回合"，还要**输出候选事件列表**（每次方向突变、落地候选、发球起点）。
> - **第二遍（精算）**：⚠️ **只对"事件窗口"跑 TrackNetV3 + InpaintNet（每事件 ±0.2 s ≈ 24 帧），绝不对整个回合跑**。按 V3 官方 25.11 FPS 实算，整个回合跑一遍会让单局耗时从分钟级恶化到十几分钟（第 17.1 节 T1）。
> - **4K-ROI 降级为可选增强**：把 4K 裁成球心 ROI 喂给模型会偏离训练分布（整幅球场 → 局部特写），**精度可能不升反降**，必须先做消融实验（第 17.4 A5）。在验证通过前，第二遍默认**整幅**推理。
> - **工期紧张时的降风险路径**：v1 只用 V2/V4 单遍跑通（720p 全帧），第二遍与 ROI 全部后置。官方数据集本身就含业余素材（match24–26），预训练权重对业余画质有适应性，冷启动风险不高（第 17.6 节）。

> 说明：以上为公开模型的常见量级估计，最终以实际选型 profiler 结果为准；本方案不承诺具体数值，只给预算区间。

### 4.2 安装包体积估算

**Windows（x64）**

| 组件 | 体积 |
|---|---|
| 应用二进制 + Qt6 运行库 + 资源 | 120–200 MB |
| FFmpeg (含 x264/x265/NVENC 等) DLL | 25–40 MB |
| ONNX Runtime (CPU + DirectML EP) | 30–60 MB |
| 可选 CUDA/cuDNN（若走 NVIDIA GPU 加速，可做成独立可选包） | +500 MB–1.5 GB（**默认不打包**） |
| 模型（v1 实际需要） | 30–80 MB |
| **总计（默认）** | **≈ 200–400 MB**，安装后占用 ≈ 350–600 MB |

**Android（arm64-v8a 单 ABI）**

| 组件 | 体积 |
|---|---|
| APK 本体 + Compose/资源 | 25–45 MB |
| LiteRT/TFLite + NNAPI delegate | 10–20 MB |
| ffmpeg-kit（或自编译精简 FFmpeg） | 15–30 MB |
| 模型（int8 量化版） | 30–60 MB |
| **总计** | **≈ 90–150 MB** |

### 4.3 算力与时间预算

**A. 原口径（60 分钟 1080p60 视频、固定机位）——保留作对比基线**

| 阶段 | Windows（RTX 级 GPU） | Windows（纯 CPU 8 核） | Android 中高端 SoC |
|---|---|---|---|
| 解码 + 静止期检测 | 1–2 min | 4–6 min | 4–8 min |
| 球轨迹（TrackNet，每帧 5–8 ms GPU） | 4–8 min | 40–70 min | 60–120 min |
| 姿态（仅候选窗口，约 5–8k 帧） | 1–3 min | 6–15 min | 10–25 min |
| 事件融合 + 状态机 + 比分 | <30 s | <1 min | 1–2 min |
| 生成整片代理（硬件编码，可选） | 1–2 min | 4–8 min | 5–10 min |
| **合计** | **≈ 7–16 min** | **≈ 55–100 min** | **≈ 80–165 min + 显著发热** |

**B. 实际口径（业余单局 10 分钟 **1080p60**，即 D5 + D12 场景）**

| 阶段 | Windows（RTX 级 GPU） | Windows（纯 CPU 8 核） | Android 中端机（720p 粗筛） |
|---|---|---|---|
| 解码 + 静止期检测 | 15–30 s | 50–80 s | 60–100 s |
| 球轨迹（第一遍，720p，**可跳帧到 30fps 等效**） | 4–7 min ⚠️ | 20–40 min ⚠️ | 15–30 min ⚠️ |
| 球轨迹（第二遍，**仅事件窗口**跑 V3） | 1–5 min | 5–15 min | ❌ 不做（见 17.2） |
| 姿态（仅候选窗口） | 10–20 s | 40–90 s | 1–2 min |
| 事件融合 + 状态机 + 校验 | <5 s | <10 s | <20 s |
| **粗切片（零编码）** | **0 s** | **0 s** | **0 s** |
| 整片代理（可选，硬编） | 10–30 s | 40–90 s | 1–2 min |
| 导出选中片段（约 12 个球） | 20–40 s | 1–2 min | 1–3 min |
| **合计（到"可审核"为止）** | **≈ 5–12 min** ⚠️ | **≈ 26–55 min** ⚠️ | **≈ 17–33 min** ⚠️ |
| **合计（含导出）** | **≈ 6–13 min** | **≈ 28–58 min** | **≈ 18–35 min** |

> ⚠️ **重要修正（第 17 节复核结果）**：上表已按第 17.2 节的代价实算更新，**与 v1.2 的乐观数字差异很大**。原因是：
> - **第一遍的吞吐口径存在 6 倍分歧**（官方表 V2 = 27.7 FPS，V4 论文 = 160+ FPS，口径不可比）→ 已按保守值估算，**必须实测确认（A1）**；若实测接近 160 FPS，第一遍可降到 4 min（GPU）以内。
> - **第二遍若覆盖整个回合会贵一个数量级**（T1）→ 已收窄为"只跑事件窗口"。
>
> **结论：时间预算的乐观值已作废，必须由 M-1 预研的 A1/A2 实测后才能给出承诺值。** 在此之前，对外口径应写"**分钟级**"，不写具体数字。

**C. 60fps 带来的算力影响与三条提速手段（D12 的连带影响，必须处理）**

⚠️ **60fps 让精度上限提高，但帧数翻倍（10 分钟 = 36,000 帧，而非 30fps 的 18,000 帧）**，粗筛耗时相应上升约 1.5–2×。这不是精度问题，是成本问题，必须用下面三条手段压回来：

| 手段 | 做法 | 收益 | 代价 |
|---|---|---|---|
| **① 第一遍跳帧（最有效）** | 粗筛时**隔帧取用（60fps → 等效 30fps）**。因为 60fps 下球在两帧间位移更小，**跳帧后仍比"原生 30fps"更连续**，细粒度时序信息没丢 | 粗筛算力直接减半 | 几乎无损（球轨迹在 30fps 等效下仍足够定位候选段） |
| **② 第二遍只跑"事件窗口"**（第 17.2 节修正） | 精算只覆盖 **落地/发球等关键事件前后 ±0.2 s**（≈24 帧），**绝不对整个回合跑** | 精算代价从"约 14 min/局"降到 **≈1–5 min** | ⚠️ 必须实测确认事件窗口够用（A2） |
| **③ 亚帧插值提精度** | 60fps 的富余帧用于**轨迹平滑 + 亚帧级击球时刻定位**（在相邻两帧之间插值方向突变点） | **回合边界误差可优于 1 帧**（对应 T2 的 ≤0.3 s 目标） | 轻微计算量 |

> **结论**：60fps 是净利好 —— **第一遍靠跳帧把成本压回 30fps 水平，第二遍靠"只跑关键事件窗口 + 亚帧插值"换来更高精度**。这也是"两遍处理"架构在 D12 条件下价值更大的原因。
> ⚠️ 注意跳帧的**例外**：横向高速杀球段**不能跳帧**（跳帧会让帧间位移翻倍，最易断裂），见第 17.3 节 T4。

> **注意"到可审核为止"这一行**：因为粗切片零编码（第 3.3 节），用户**不需要等任何切片进度**就能开始看和审核。这是本场景体验的核心。

**内存峰值**：Windows **2.5–4 GB**（4K60 硬解缓冲比 1080p 大，见第 18.2 节 C3）；Android **1.2–1.8 GB**（风险点，需严格限制窗口与位图缓存）。

**关键优化**：① 只分析"人物存在 & 有运动"的时段（静止/捡球时段跳过）；② 姿态只跑候选窗口；③ 有界滑窗流式读取，绝不把整段视频解到内存；④ 球轨迹用半分辨率；⑤ 多线程解耦解码/推理/编码，避免互相等待。

---

## 5. 设计方向与 UI 结构

**五个主界面，围绕"审核效率 + 成片导出"设计**（自动结果一定会有错，UI 的价值是把纠错成本压到最低；而用户最终要的是"一条成片"，见 D13）：

1. **库 / 项目页**：视频列表（时长、状态、回合数）、新建分析、分析参数模板（机位类型、单/双打、精度档）。
2. **回合列表页（审核核心）**：表格式卡片列表 —— `序号 | 拍数 | 时长 | 置信度 | 缩略图 | 标签(精彩球/我方得分/对方得分/非回合)`；支持多选、拖拽排序、筛选（**精彩球**/长回合/低置信）、拆分/合并、**键盘全流程操作**（↑↓ 选择、空格播放、`H` 标精彩球、`A`/`B`/`X` 三态）。
3. **时间线页** 🆕（**因 D13 升为 P0**）：全片时间轴，回合块 + **静止段（垃圾时间）可视化**，让用户一眼看到"压掉了多少、留下什么"。可在此直接拖拽调整回合边界。
4. **播放器页**：单回合播放 + 时间轴事件标记（击球点/落地点刻度）、可选叠加"球轨迹 + 球场半透明标定"；慢放 0.25x、逐帧步进；左右方向键跳上/下一回合；`H` 标精彩球、`P` 加入候选池。
5. **导出页**：**三个预设（单球 / 精彩集锦 / 垃圾时间压缩，见第 9.5 节）** + 压缩力度三档 + 竖屏/方形模板 + 烧字开关 + 分辨率与码率 + 进度与产物路径。

交互设计要点：
- **一切自动判定都要可视化其依据**（回合卡片可展开显示"落地帧 + 球轨迹末段 + 静止期区间"）。
- **不做隐藏的自动修改**：任何自动修正必须留痕、可一键撤销。
- 长任务必须可暂停/取消/续跑，且**分析完成后立即给出"低置信回合清单"**，引导用户只审这几条。
- 🆕 **"精彩球"标记必须能在连播中一键完成**（`H` 键 / 触屏大按钮）—— 它是集锦预设的主要筛选条件，标记效率直接决定集锦功能好不好用。

---

## 6. 运行平台方案：全部本地处理（已确认决策）

> **决策状态（D2）**：已确认**全部本地处理，不做服务器**。因此本节不再讨论服务器路线；曾评估过的 2 核/2 GiB 云服务器结论保留在第 8 节，仅作为"为什么不走服务器"的依据存档。

| 维度 | Windows 本地 | Android 本地 |
|---|---|---|
| 隐私（家庭/学员视频） | 优（不出本机） | 优（不出本机） |
| 速度（**10 min 单局**，D5 口径） | **≈5–12 min**（独显）/ 26–55 min（纯 CPU）⚠️ | **≈17–33 min**（仅第一遍 720p，需插电）⚠️ |
| 成本 | 一次性（用户已有 PC） | 一次性 |
| 装机难度 | 中（DLL/驱动/运行库） | 低 |
| 离线可用 | 是 | 是 |
| 适合场景 | **完整分析（含 4K 精算）、大屏精细审核、批量多局、成品导出** | **现场/就近处理、快速查看、审核标记、分享** |

**双端策略**

- **Windows = 全能端**：完整分析（含可选精算档）、大屏审核工作台、批量多局、多规格导出（含 4K）、归档。
- **Android = 轻量但完整闭环端**：拍摄与质量体检、**720p 粗筛 + 切分 + 三态标注 + 导出片段 + 分享**，全流程可在手机上独立完成。1080p 视机型可选；4K 精算仍建议交 Windows。
- **两端共享同一套算法核心与数据模型**（第 10.2 节），因此"项目管理 / 回合表 / 三态标注 / 导出设置"的行为完全一致，不存在"手机版少一半功能"的问题。
- **"工程包"是可选的双端衔接方式**（`project.json` + `rallies.csv` + `calib.json` + 可选代理视频），用于"手机上先粗筛、之后在 PC 上接着精算/精修"，**不是必须的中间步骤**（详见第 10.5 节）。
- **架构前提（关键）**：算法核心从第一天就写成**平台无关的 C++/Rust 核心 + 薄 UI 外壳**，两端共享数据模型与参数档位系统。否则安卓端会退化成重写（详见第 10.2 节）。
- **明确不建议**：纯安卓端做 **4K 全量高精度分析**（中端机耗时与发热不可接受），但**手机端做 720p 全流程是推荐路径**。

**一句话结论**：**本地处理，双端分工 —— Windows 负责"做得更精细、更快、更多"，Android 负责"就地就能出一版能发的成品"。**

---

## 6.5 手机端功能边界，以及"手机上用"与"电脑上用"的真实差别
这一节回答两个产品层面的问题：安卓端到底做哪些功能、以及同一套算法在两个平台上跑，用户实际体验会差在哪。**结论是差别不在"功能有无"，而在工作流分工**（先给结论，展开见 6.5.3）。

### 6.5.1 手机端（Android）应有的功能清单

**A. 采集与导入**

| 功能 | 说明 | 优先级 |
|---|---|---|
| 相册/文件导入 | 支持 480p–4K、H.264/HEVC/AV1、VFR 自动转 CFR | P0 |
| 应用内直接拍摄 | 绑定 50/60fps + 高速快门/手动曝光、曝光锁定、防抖提示、剩余空间校验 | P0（差异化能力） |
| 拍摄前质量体检 | 实时提示：**帧率不足 / 快门过慢（会糊）/ 场地未完整入画 / 光照不足 / 横竖屏方向** | P0（这一步能救掉一半烂素材） |
| 分享导入 | 从微信/相册"分享到本应用" | P1 |
| 外部大文件（U 盘/读卡器） | 通过 SAF 授权读取 | P2 |

**B. 分析（算力受限，需分档）**

| 功能 | 说明 | 优先级 |
|---|---|---|
| 快速粗筛档（480p/720p、降帧率） | 目标 0.5–1× 实时，半小时内出结果 | P0 |
| 标准档（720p 全帧 + 音频证据） | 1–1.5× 实时 | P0 |
| 精细档（1080p + 候选 ROI 二次推理） | 慢，仅在插电+散热良好时开放 | P1 |
| 断点续跑 | 前台服务被系统回收后能从最近落盘点继续 | P0（生存必需，见 6.5.2） |
| 温控降速 | 温度超阈值自动降档/暂停并提示，而不是硬跑到底把自己烧了 | P0 |

**C. 审核与回看（手机端真正的价值区）**

| 功能 | 说明 |
|---|---|
| 回合卡片列表 | 序号 / 比分 / 拍数 / 时长 / 置信度 / 缩略图 |
| 一键改比分、合并/拆分回合、标记非回合（捡球） | 触屏手势 + 大按钮 |
| 连播、慢放（0.25x–2x）、逐帧步进 | 手机端体验最好的部分 |
| 轨迹/骨架叠加开关 | 可选，性能允许时开 |
| 暂存观看列表（Playlist） | 现场"这几个球给学员看" |
| 离线可用 | 全程无网 |

**D. 导出与分享**

| 功能 | 说明 |
|---|---|
| 生成回合片段（1080p/720p，硬件编码） | 默认（这是唯一一次编码） |
| 预览用整片代理（720p/4–8 Mbps，可选，后台生成） | 保证弱机连播流畅；不生成也能用 |
| 烧入比分/拍数/时长/序号（可开关） | 默认开 |
| 导出到相册 / 分享到微信、钉钉、短信 | 手机端最高频的"最后一公里"，**电脑做不到** |
| 批量导出 + 后台进度通知 | 前台服务 + 通知栏进度（注意：60 min 时代的量级参考已作废，10 min 单局导出通常 20–60 s） |
| 生成"轻量工程包"（JSON + 元数据，可选代理视频） | **可选功能**：用于"手机先粗筛、PC 接着精算/精修"的跨设备衔接，不是必经步骤（第 10.5 节） |

**E. 明确不做（或只做降级版）**

- ❌ 4K 全片完整分析（温控 + 时长不可接受）
- ❌ 批量处理多场视频（手机的输入形态就不是批量）
- ❌ FCPXML/EDL 工程导出（手机上没有 PR/Resolve）
- ❌ 大屏精细审核工作台（屏幕物理限制）

### 6.5.2 安卓平台的硬约束（决定了"手机能做什么"）

这几条不是优化问题，是平台红线，必须在架构里预留：

1. **前台服务与后台执行限制**：长耗时分析不能靠普通后台线程。必须使用**前台服务**（通知栏常驻），并声明合适的 `foregroundServiceType`（媒体处理/数据同步类）。Android 14+ 对服务类型与用途有严格校验，类型声明不当会被系统拒绝启动；部分类型的长时间运行还有**每日累计时长上限**。→ 所以"分析必须可暂停、可分段落盘、可续跑"，不能假设"一次跑完"。
2. **温控降频是主要矛盾**：手机持续满载推理会触发降频（SoC 温度约束），实际吞吐会掉到峰值的一半甚至更低。**按单局 10 分钟 60fps 口径**，中端机 720p 第一遍约需 **15–30 min**（第 17 节复核修正后的保守值），且**无法边充边跑**（充电发热叠加，通常会直接停止充电或不充电）。→ 手机端必须做"快速粗筛优先 + 分块落盘"，精算交给电脑。
3. **存储访问不是普通文件路径**：安卓的分区存储/SAF 体系下，应用拿到的常常是**内容 URI 而不是文件路径**，随机 seek、大文件流式解码、直接交给 FFmpeg 都要额外封装；还要申请媒体读取权限（`READ_MEDIA_VIDEO` 类）。→ FFmpeg 接入层要写两种输入适配器（fd/URI 与普通路径）。
4. **硬件编解码器行为因厂商而异**：MediaCodec 的分辨率/码率/profile 支持、同屏实例数、是否支持 4K60 编码，各厂商差异很大，必须做**能力探测 + 多级回退**（HEVC → H.264 → 软编）。
5. **功耗与电量**：手机满载分析约吃掉 10–25% 电量（视机型与档位），分析前应给用户明确的"耗电/耗时/温度"三预估，并允许"仅插电时执行精细档"。
6. **端侧加速能力参差**：LiteRT/TFLite 的 GPU delegate 覆盖较好，NPU 路线则高度依赖芯片与厂商运行时，覆盖率与稳定性都不如桌面的 CUDA/DirectML。→ **不能把 NPU 当作必需路径**，必须以 CPU+GPU 为可用的保底配置。

### 6.5.3 电脑端（Windows）独有的功能

| 功能 | 说明 |
|---|---|
| 批量队列处理 | 一次拖入多局视频（如一周打的 5 局），夜里跑完 |
| GPU 加速（CUDA/DirectML）+ NVENC/QSV/AMF 硬编 | 10 min 单局约 **5–12 min**（对比手机 17–33 min）⚠️ 具体值待 A1 实测 |
| **4K 两级 ROI 精细分析** | 手机上因温控与耗时基本不做 |
| 大屏审核工作台 | 多列列表、键盘全流程、并排对比两个回合、轨迹/骨架同时看 |
| 比分板 OCR 交叉校验 | ~~仅当素材是转播源时才有意义~~ **已砍掉**（D11：比分非核心；业余素材无比分板） |
| FCPXML / EDL / CSV 工程导出 | 交给 Premiere / DaVinci Resolve 精修 |
| 多机位对齐 | 同场两个机位的时间轴同步 |
| 视频库管理 | 按球员/赛事/日期归档、跨场次统计（胜率、拍数分布、站位热图趋势） |
| 无损导出与归档 | `-c copy` 保原画质 + 外挂硬盘/NAS 归档 |
| 自定义模型与参数 | 换权重、调阈值、接自训模型 |

### 6.5.4 同一套功能，在两边"用起来"的 6 个真实差别

**差别 1：时效性——决定了两者的工作流根本不是一回事（最关键）**

| | 手机 | 电脑 |
|---|---|---|
| 60 min 场次 | 1.5–3 h（可能被迫分两次跑） | 8–18 min |
| 用户行为 | **只能"事后看"** —— 打完球回家、晚上充电时跑，第二天看 | **可以"当场看"** —— 打完一局坐下等 10 分钟就能复盘 |
| 典型用法 | 打完一场，第二天早上翻列表 | 训练现场，教练局间就能调出刚才那几分 |

这条差别会**反过来影响功能设计**：手机端必须把"分析结果持久化 + 通知提醒"做好（因为用户不会守着），电脑端则要优化"从导入到看见结果"的等待体验（进度条、可边分析边看已完成的回合）。

**差别 2：审核效率——决定了纠错成本，而纠错成本决定了产品是否可用**

自动结果的错都要人来改，改错的成本在两个平台上完全不同：

- 电脑：多列列表 + 键盘（↑↓ 选择、空格播放、数字键改分）+ 鼠标拖拽排序，**100 个回合约 5–10 min**。
- 手机：6 寸屏、触屏逐条点、拖拽排序要长按、改分要调出数字键盘，**100 个回合约 20–40 min**，且容易误触。

→ 所以**手机端必须少改、电脑端可以多改**：手机端应当**降低自动结果的粒度**（宁可粗一点，也不要给出大量需要人工修正的低置信回合），并把"待审核清单"压缩到最少条目；电脑端则可以放开精度、把审核做成高效率工作台。

**差别 3：导出与"最后一公里"**

- 手机：**导出到相册、直接发微信/钉钉给学员**——这是最高频的真实需求，电脑端完全没有这条通路（要发还得先传手机）。
- 电脑：FCPXML/EDL 进 PR/Resolve 精修、`-c copy` 无损归档、批量渲染上百个片段、外接硬盘管理。

→ 一个很自然的结论：**手机负责"发出去"，电脑负责"做出来"**。

**差别 4：画质与规格上限**

- 手机受**温控 + 存储 + 电量**三重约束，实际交付规格落在 **720p–1080p、6–12 Mbps**；4K 只能"逐段、插电、散热良好"时勉强做，不能作为承诺。
- 电脑可以 `-c copy` 保原画质、可以做 4K 烧字渲染、可以批量统一规格。

→ 所以**手机端的定位是"预览与分发质量"，电脑端是"成品质量"。** 如果用户的最终用途是发社交平台，手机的 1080p 完全够用，电脑那套 4K 流程反而是浪费。

**差别 5：精细诊断能力**

判断"这一拍到底算不算、落地在哪、比分是不是错了"，需要逐帧看、同时看轨迹 + 骨架 + 音频波形 + 落点标记。这在 27 寸屏 + 键鼠上是顺手的，在 6 寸屏 + 触屏上是折磨。→ **手机的自动结果必须更保守**（低置信就标 UNKNOWN 交给电脑），而不是更激进。

**差别 6：使用场景与网络**

- 球馆常在地下室/偏远场馆，网络差。手机的优势是**离线 + 随身**（现场就能粗筛出新回合并给学员看）；电脑需要带笔记本到现场或事后处理。
- 反过来，如果要**多机位/多场次批量**，手机完全不是对手。

### 6.5.5 一句话总结两个平台的分工

| | 手机（Android） | 电脑（Windows） |
|---|---|---|
| 定位 | **现场采集 + 快速粗筛 + 回看审核 + 发出去** | **精算 + 批量 + 精细审核 + 做出来 + 归档** |
| 交付规格 | 720p–1080p | 直到 4K 无损 |
| 时效 | 隔天看 | 当场看 |
| 审核量 | 尽量少（自动结果保守） | 可以多（工作台高效） |
| 唯一通路 | **直接分享到微信/相册**（电脑做不到） | **FCPXML/EDL 进专业软件**（手机做不到） |

**产品含义**：不要把两端做成"同一个功能的两个版本"（那等于用手机的短板去做电脑的长板，还多维护一套 UI）。正确的做法是 **共享一套算法核心 + 数据模型 + 轻量工程包格式，两端各自只做自己最优的那段流程**，中间用"工程包"（JSON + 元数据 + 可选代理片段）无缝接力：手机现场粗筛并标出候选回合 → 工程包传给电脑 → 电脑精算、精修、导出高清或工程文件。

---

## 7. 计分是否必做？—— 做成可选项的成本/收益分析

**结论：应当做成可选项，而且 v1 建议默认关闭自动计分。计分不是"加一个字段"，而是整条流水线里风险最高、依赖最深的一层。**

### 7.1 计分为什么"影响挺大"（它牵动了什么）

用户直觉是对的：加比分这件事的连锁反应远超表面。它把三件高难度的事绑进了主线：

| 计分层 | 依赖的东西 | 失败后果 |
|---|---|---|
| 落点判定（球落在界内还是界外） | 场地标定精度 + 球轨迹末端精度 | 判错 → 得分方判反 → 整局比分连锁错误 |
| 回合胜者判定 | 落点判定 + 下网/触网/连击等规则 | 一个回合错，后面全错 |
| 规则状态机（21 分制、20 平净胜 2 分、30 分封顶、发球权轮转、局间 11 分换边） | 纯逻辑，**本身没有技术风险** | 只要输入对就不会错 |

关键洞察：**第三层是纯状态逻辑（最简单、可单测），第一、二层才是难度所在。** 所以"要不要计分"的真正问题是"要不要做落点判定"——而落点判定恰好也是切片功能不需要的东西。这就是为什么它"影响挺大"：**它引入了一条与切片正交、但难度更高的技术支线。**

### 7.2 三档方案（推荐第 2 档）

| 档位 | 做法 | 开发成本 | 风险 | 适用 |
|---|---|---|---|---|
| **A. 完全不做** | 无比分列、无落点判定 | 省 3–4 周 | 无 | 只要切片和拍数 |
| **B. 只做"人工记分"（推荐 v1）** | 每个回合由用户点一下"我方/对方得分"，程序负责规则状态机与比分显示；落点判定不参与 | 约 1 周（状态机 + UI） | 极低（输入由人保证） | 绝大多数真实需求 |
| **C. 自动计分 + 人工确认** | 落点判定 → 自动提议得分方（带置信度）→ 用户一键确认；OCR 交叉校验 | 3–5 周 | 高（依赖标定与轨迹精度） | 事后确定要做"全自动复盘" |

**为什么推荐 B 而不是 A**：状态机是纯逻辑、几乎零风险，做出来以后"比分列"就有了；而**"人工点一下胜者"这个动作，恰好就是自动计分所需的训练标签和精度基准**——用户点了 50 个回合，你就免费得到了 50 条带真值的落点样本，将来做 C 档时准确率有据可依。所以 B 是"低成本 + 为 C 铺路"的最优解。

### 7.3 架构要求：计分必须从第一天就做成"可插拔的后处理层"

这是本节最重要的工程决定。把计分设计成**只消费一个契约字段**，就永远不会污染主链路：

```
回合主链路产出（v1 就完成，与计分完全解耦）：
  rally { id, start_ms, end_ms, stroke_count, duration_ms,
          winner: A | B | UNKNOWN,        ← 这个字段由"人"或"算法"填，来源可换
          winner_source: MANUAL | AUTO_LANDING | AUTO_OCR,
          winner_confidence: 0.0–1.0,
          landing_point: {x, y, in_court: true/false/UNKNOWN} | null }
```

- 计分引擎（21 分制状态机）**只读 `winner`**，不关心它是谁填的；
- v1 填 `MANUAL`，v2 换 `AUTO_LANDING` 时**主线一行不用改**；
- 关闭计分时，UI 只是不显示比分列，数据模型与代码路径完全一致 → **"可选项"落地成本接近零**；
- 副作用：不是每个回合都需要赢家（比如"这段不算回合，是捡球"），所以 `winner` 必须是三态（A/B/UNKNOWN），UNKNOWN 不参与比分推进，只在审核清单里排队等待。

### 7.4 计分关闭后连带省掉的东西

`落点判定` 一旦不做，下面这些也随之省掉或降级：

- 落点判定的精度需求和配套自测指标（原本是主要风险源之一）；
- 比分板 OCR 模块（模型、ROI 维护、交叉校验逻辑）；
- 烧字里的比分项（只剩拍数/时长/序号，烧字实现反而更简单）；
- 导出工程文件里的比分命名规则（可用序号+拍数替代）。

**代价**：失去"自动出比分"这个卖点。但如果最终用途是复盘与集锦，拍数与时长才是真正被高频使用的信息，比分往往用户自己心里清楚。

### 7.5 对工期的影响

| 范围 | 原本预估 | 关闭自动计分后 |
|---|---|---|
| MVP（回合切割 + 切片 + 回看 + 导出） | 10–14 周 | **6–9 周** |
| 增加自动计分 | +3–4 周 | 作为 v2 独立模块，主线不受影响 |

**建议**：M4 里程碑改为"人工记分 + 状态机"，自动落点判定挪到 v2 并作为独立可选模块。

---

## 8. 现有服务器（2 vCPU / 2GB RAM / 40GB 盘）能否跑这套服务？

实测配置（来自实例信息）：
**通用型，2 核 CPU，2 GiB 内存，系统盘 40 GiB（已用 27.43 GiB），峰值带宽 200 Mbps，Windows Server 2025 数据中心版，地域：美国（弗吉尼亚），到期 2027-06-01。**

**结论：这台机器不能作为"重算力分析节点"，但完全可以作为"控制面 + 轻活执行节点"。**

### 8.1 逐项可行性判断

| 资源 | 现状 | 判断 |
|---|---|---|
| **内存 2 GiB** | Windows Server + 图形界面空载通常已吃掉 1–1.5 GiB | ❌ **硬伤**。留给推理的内存不足 1 GiB，模型（数十 MB）本身没问题，但**解码缓冲 + 推理张量 + 帧缓存**在 1080p 下要 1.5–3 GB。这套服务跑起来会因内存不足（OOM）而失败或被系统杀掉 |
| **CPU 2 核** | 无独显 | ⚠️ 纯 CPU 推理可跑但慢：逐帧球检测在现代 CPU 上约 8–20 fps（512×288 输入），60 min@60fps 的 21.6 万帧 → **约 3–7 小时**；再叠加姿态与编码，单场**约 4–10 小时**。勉强能"过夜跑一场"，但 2 核同时跑解码+推理+编码会互相抢核，实际更慢 |
| **系统盘 40 GiB（剩 ~12.5 GiB）** | 已用 27.43 GiB | ❌ **第二个硬伤**。60 min 1080p60 的 H.264 源约 6–10 GB，加上代理片段 + 回合切片 + 临时文件，**单场就需要 12–20 GB**。剩余空间只够一场，且会把系统盘写满（系统盘写满会让 Windows 本身出问题）。**必须先挂云硬盘或扩容** |
| **带宽 200 Mbps** | 当前用量 0.01 Mbps | ✅ 尚可。上传 10 GB 约 7 分钟。但要**确认月流量上限/计费方式**，4K 源（单场 12–25 GB）反复传会很快吃满额度 |
| **地域：美国（弗吉尼亚）** | — | ⚠️ **合规与体验风险**。训练/学员视频属私人内容，跨境上传需用户知情同意；国内访问该地域延迟高，上传慢、看切片卡。若用户在国内，这条可能直接否决方案 |
| **Windows Server 2025** | — | ✅ 好消息：与本地 Windows 目标平台一致，**同一份二进制可直接部署**，无需为 Linux 重新打包（这是当前方案的一个隐性优势） |

### 8.2 这台机器实际适合干什么："控制面"而非"计算面"

| 能做好（轻 CPU / 低内存 / 常驻） | 做不了（重 CPU / 大内存 / 大磁盘） |
|---|---|
| 任务队列与调度（后台常驻，这正是"可以后台操作"的价值） | 视频解码与逐帧推理 |
| Web 管理端 / API（上传、进度、结果浏览） | 4K 分析与两级 ROI |
| 元数据与项目索引（SQLite/Postgres，体积极小） | 批量并发多场 |
| 音频轨提取与击球音分析（纯信号处理，**很轻**） | 高质量重编码（2 核做 1080p 软编约 0.5–1× 实时） |
| 缩略图 / 代理片段生成（**必须是 720p/1.5 Mbps**） | 大文件长期存储（盘不够） |
| 结果打包与下载分发 | ~~比分板 OCR 的高分辨率 ROI 推理~~（OCR 已砍掉，见 D11） |

**"后台操作"确实是这台机器最大的价值**：它 7×24 常驻、有公网 IP，适合当"任务入口 + 进度看板 + 结果仓库"，用户白天上传、夜里排队跑、第二天取结果。**但注意：即便只是排队，真正执行分析任务的那台机器仍需满足内存与磁盘要求。**

### 8.3 三个可行方案

**方案一：这台机器只做控制面（零额外成本，推荐起步）**

```
[手机] ──上传代理片段(720p)──► [此服务器] 任务队列 + Web UI + 元数据 + 音频分析
                                     │
                                     └── 把"重算力任务"下发给
                                          [本地 Windows PC / 有 GPU 的实例]
```
- 本地 PC 端跑一个"worker agent"，主动向服务器拉任务、下载代理片段、分析、回传结果与切片。
- 好处：服务器不吃力、隐私可控（可只传低码率代理）、用户仍获得"后台队列 + 远程看结果"的体验。
- 代价：需要写一个 worker agent（约 1–2 周）。

**方案二：给这台机器加内存与云盘，直接扛 720p 分析（需付费升级）**

- **内存 → 4 GiB 是绝对底线，8 GiB 才舒服**（多一个进程余量、避免 OOM）；
- **挂一块 100–200 GiB 云硬盘**专放视频，与系统盘分离；
- 分析档位固定为 **720p / 降帧率**，不承诺 4K；
- 预期吞吐：单场 720p 50 min 素材，2 核约 **2–5 小时**（过夜跑 1–3 场，取决于帧率降采样）。
- 注意：2 核同时跑三个 ffmpeg 线程池会互相拖慢，**并发度必须设为 1**。

**方案三：换/加一台 GPU 实例（要真效率就必须走这条）**

- 目标规格参考：**8 vCPU / 16 GiB 内存 / 100 GB+ SSD / 单卡 8–16 GiB 显存**；
- 单场 1080p60 分析降到 **10–20 分钟**，可并发 2–3 路；
- 代价是持续费用。若一年只处理几十场，方案一 + 本地 GPU PC 的总成本通常更低。

### 8.4 一句话回答"是不是效率很低、但可以后台操作"

- **是，效率很低**：以当前 2 核 / 2 GiB / 剩 12.5 GiB 盘，**连单场 720p 分析都处于"能跑但随时可能 OOM 或写满盘"的边缘**，1080p 与 4K 基本不可行；时间上单场要数小时到十几小时。
- **后台操作确实是真优势**：常驻 + 公网 + 队列，适合"提交后不用守着"。但这个优势的实现方式是**它当调度台，而不是它当计算器**。
- **最省钱的起步路径**：这台机器做控制面（方案一），重算力放本地 Windows PC；等确实出现"每天多场、需要无人值守批量"的需求，再按方案三升级。

### 8.5 若坚持直接用这台机器，必须做的工程约束

1. **串行执行，并发度=1**（2 核无法并行）；
2. **只接 720p 代理**（导入时即转码，源 4K 只作归档不参与分析）；
3. **分段落盘 + 断点续跑**（长任务 + 低内存 + 可能被回收，必须有中间状态持久化）；
4. **磁盘水位守卫**：剩余空间低于阈值时暂停任务并告警，绝不写满系统盘；
5. **分析完立即清理**源代理与中间帧，只保留回合切片与元数据（单场约 300–800 MB）；
6. **内存守卫**：限制 FFmpeg 解码线程数与有界窗口帧数（这是 2 GiB 下能否跑通的关键）；
7. **Windows 页面文件设置到云盘**（系统盘空间紧张时不要放在 C 盘）；
8. **明确告知用户跨境传输与数据留存策略**，并提供一键删除。

---

## 9. 导出方案评估：程序内导出 vs 交给 PR/Resolve

> **⚠️ 需求澄清（D13，本次新增）**：用户明确了切片分析的三个真实用途：
> 1. **截取单一精彩回合导出**（一个小片段，单独发）
> 2. **把精彩回合剪辑成一条合辑**（多个回合拼成一条连续视频）
> 3. **精简掉非正常对抗的垃圾时间**（把捡球/走动/间歇压掉，留下紧凑的比赛流）
>
> **这直接改变了导出的主次**：主产物**不是"每回合一个 MP4"，而是"一条按条件筛选并紧凑拼接的成片"**。旧版把"独立片段"当默认路径是**主次颠倒**的，本版已修正。详见第 9.5 节。

### 9.1 两个技术前提（必须知道，否则会做错）

1. **无损切割（stream copy）会有关键帧对齐误差**：用 FFmpeg `-c copy` 切分时，切点必须落在关键帧上，否则开头会多出 0.5–3 s 内容（GOP 长度决定）。补救方式：① 输出 MP4 时写 `edts/elst` 编辑列表让播放器跳过多余部分（兼容性一般）；② 或干脆重编码一个小片段（1 个回合通常 10–40 s，重编码代价可接受）。
2. **烧字（比分/拍数/时长）必然要重编码**：需要 FFmpeg `drawtext` 或逐帧叠加，无法 stream copy。

### 9.2 四条导出路线对比

| 路线 | 实现 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| A. 程序内导出独立回合 MP4 | 关键帧对齐 seek + 硬件编码 | 拿来即用、手机上能看、可烧字 | 10 分钟素材导出约 20–60 s、占磁盘 | **必做**（主交付形式） |
| B. 程序内导出合并长片 | concat + 可选转场 | 一整个"集锦"文件，发朋友圈/群最方便 | 需统一参数 | **做，但设为可选** |
| C. 导出工程文件给 PR/Resolve | 生成 `FCPXML` / `CMX3600 EDL` / `CSV+JSON` | 保留无损画质、剪辑自由、文件极小（几十 KB） | 用户需装专业软件、需自行渲染 | 🔻 **降级为二期可选**（本场景人群基本不用 PR/Resolve，见第 12.6 节） |
| D. 直接输出到 PR 的插件/API | Premiere UXP 插件或 ExtendScript | 体验无缝 | 开发与维护成本高、版本敏感、平台绑定 | **不做**（明确排除） |

### 9.3 推荐组合与默认导出规格（已按 D13 调整主次）

**可以直接在程序内导出，且是唯一路径**（工程文件降级为二期可选）。**主产物是"成片"，不是"片段包"**：

| 优先级 | 产物 | 说明 |
|---|---|---|
| **P0 主产物** | **紧凑成片（单文件）** | 按筛选条件把多个回合拼成**一条连续视频**。三个预设见第 9.5 节。这是用户真正要的东西 |
| **P0 辅产物** | **单回合片段** | 只想发某一个球时用；也可从成片里截，但独立文件更省事 |
| P1 | 竖屏/方形模板 | 发抖音/小红书/朋友圈的实际形态（1080×1920 / 1080×1080，留白或模糊背景填充） |
| P1 | 烧字 | 拍数/时长/序号默认开；比分默认关（D11）；**竖屏下烧字位置需单独预设**，避免被平台 UI 遮挡 |
| P2 | 附 `rally_index.csv` + `project.json` | 机器可读，含每回合精确帧号，便于二次处理 |
| P2（二期） | `FCPXML v1.9` + `CMX3600 EDL` | 供 Premiere/Resolve/FCP |
| ❌ | Premiere 插件 | 不做（Adobe UXP/ExtendScript 版本绑定会把维护成本推高一个量级） |

- **编码规格**：H.264 High，AAC 192k，`yuv420p`，faststart；硬件编码优先。
- **单回合文件名**：`R012_24shots_18.4s.mp4`（序号/拍数/时长）；启用记分时升级为 `R012_11-9_24shots_18.4s.mp4`。
- **成片文件名**：`highlights_12rallies_3m42s.mp4` / `compact_full_8m05s.mp4`（把"这一段是什么、多长"放在名字里，比无意义的时间戳有用）。
- **磁盘策略（符合第 3.3 节）**：审核与预览阶段**不产生任何视频文件**（只有可选的整片代理 + 缩略图）；导出时才编码。提供"一键清理代理与缩略图/仅保留元数据"。

### 9.4 人工确认的三态设计（已确认方案）

**三态：`A方得分` / `B方得分` / `非回合`。** 这个设计比"二态 + 异常处理"好得多，理由如下。

**9.4.1 为什么"非回合"必须是一等公民，而不是异常**

非回合在真实训练视频里**占比很高**（业余场景常达 20–40%）：捡球、擦汗、换球、试挥、场边走动、系鞋带、局间休息、发球重发、擦网重打、多球训练（连续喂球没有死球）。如果把它们当作"识别失败"或"异常"，会连锁引发四个问题：

1. 它会被塞进比分推进逻辑；
2. 它会被当成正常回合参与统计（拍数分布、平均时长全部被污染）；
3. 它会被导出成切片，污染切片列表；
4. 无法从中学习——而**它们恰恰是训练"回合起点检测器"最有价值的负样本**。

**9.4.2 数据模型（三态契约）**

```
rally {
  id, video_id, idx, start_ms, end_ms, stroke_count, duration_ms,
  outcome:      A_POINT | B_POINT | NON_RALLY   ← 三态，用户点选
  outcome_source: MANUAL | AUTO_LANDING | AUTO_OCR   ← v1 恒为 MANUAL
  outcome_confidence: 1.0                        ← v1 恒为 1.0
  serve_side:   A | B                            ← 由规则引擎回填，用于校验
  review_state: CONFIRMED | PENDING              ← 三态是否已人工确认
  is_highlight: bool                             ← 🆕 用户标记的"精彩球"（D13：集锦预设的主筛选条件）
  quiet_before_ms / quiet_after_ms: int          ← 🆕 该回合前后的静止段时长（垃圾时间压缩用）
  is_exportable: bool                            ← 派生字段：outcome != NON_RALLY（预设③用）
}
```

- `outcome` 与 `outcome_source` 分离，保证 v2 接自动落点判定时**主线零改动**（第 7.3 节的契约原则）；
- `is_exportable` 是**派生字段**（`outcome != NON_RALLY`），不是独立存储，避免两处状态不一致；
- `serve_side` 由规则引擎算出来并在 UI 上显示，作为"你点的分和发球权轮转是否自洽"的软校验——**这是三态带来的一个白送的纠错能力**：如果用户把连续三回合都点成同一方得分，而规则要求得分方继续发球，UI 可以提示"疑似点错"（当然，连得三分是完全合法的，所以只能是提示而非阻止）。

**9.4.3 "非回合"的语义与规则处理（关键细节）**

> ⚠️ **先明确一个容易歧义的词**：这里的**「非回合」= "这一拍不算分（回合作废）"**，不是"在非回合中产生的分数"。
> 也就是说：捡球、试挥、擦汗、发球重发、擦网重打 → 点「非回合」→ **比分不变、发球权不变**。
> 若需求是"非回合中出现的犯规仍要判分"（例如正式比赛里发球失误要送分），那是**第四种状态**，见第 16.5 节的待确认项。

| 场景 | 是否推进比分 | 说明 |
|---|---|---|
| `A_POINT` / `B_POINT` | ✅ 推进 | 胜者成为下一回合发球方（每球得分制） |
| `NON_RALLY` | ❌ **不推进、不换发球权** | 视同"这个回合没有发生"，比分与发球方都保持不变 |
| `PENDING`（未确认） | ❌ 暂不推进 | 直到用户确认；UI 上把后续比分显示为"待定"而不是猜一个 |

- **发球方锚定规则**：`serve_side` 取**最近一个已确认的 `A_POINT`/`B_POINT` 回合的胜者**；若该回合之前全是 `NON_RALLY`，则沿用更早的锚点；开局无锚点时才要求用户指定初始发球方（每局一次，10 秒的事）。
- **为什么这条重要**：它让"非回合"不会把发球权轮转搞乱。如果错误地把非回合计为得分，比分会逐渐漂移且很难发现；而"不推进"是保守且可解释的。
- **重发/擦网重打**：归为 `NON_RALLY` 即可（不推进），而不是"得一分"。这符合业余复盘的实用需求——用户关心的是有效对抗的比分。
- **多球训练视频**：整段可能**没有任何 `A_POINT`/`B_POINT`**。此时比分列显示"—"，程序退化为"纯切片工具"。所以三态模型天然兼容"根本没有比分"的场景，这是二态模型做不到的。

**9.4.4 交互设计（决定这个功能是否真的好用）**

每个回合卡片上三个按钮 + 键盘快捷键，**必须在播放中就能标记，不需要暂停、不需要进详情页**：

| 操作 | 键盘 | 触屏 |
|---|---|---|
| A 方得分 | `A` 或 `1` | 左侧大按钮 |
| B 方得分 | `B` 或 `2` | 右侧大按钮 |
| 非回合 | `X` 或 `0` | 中部灰色按钮 |
| 撤销上一步 | `Ctrl+Z` / `Backspace` | 左上角撤销 |

三条设计要点：

1. **连播中标记**：列表自动连播，用户看着球飞落地，随手按 `A`/`B`/`X`，程序自动跳到下一回合。**这是整个审核流程的效率核心**——如果必须暂停→点按钮→播放→下一回合，效率会掉 5 倍。
2. **三态是"默认三选一"而不是"先在二态里选再标异常"**：把"非回合"放在同一层级，用户的心智负担最低，也不会漏标。
3. **高亮未确认**：`PENDING` 的回合在列表里明显高亮，并显示"待确认 N 个"计数，支持"跳到下一个未确认"。

**9.4.5 三态标签的副产品价值（这是白赚的）**

用户每确认一个回合，就免费产生了：① `A_POINT`/`B_POINT` → **落点判定的真值标签**（配合轨迹末端坐标，就是 v2 自动计分的训练集）；② `NON_RALLY` → **回合起点检测的负样本**（极其稀缺，公开数据集里几乎没有）；③ `start_ms`/`end_ms` 的人工修正 → **回合边界回归的监督信号**。

建议在实现时就**静默累积这些样本**（存表 + 一键导出为训练集格式），不要等到 v2 才想起来。这是把"人工确认"从"不得已的兜底"变成"数据飞轮"的关键。

> ⚠️ 设计上不要本末倒置：**不要为了收数据而故意降低自动精度或增加确认次数**。用户确认得越快，产品体验越好；数据只是顺带收获。

### 9.5 三个导出预设（对应 D13 的三种用途）

> 用户的三种用途本质上是**同一件事的三种筛选条件**：都是"从时间轴上挑出若干段 → 紧凑拼成一条成片"。所以**只需一个"成片生成器"**，用三个预设覆盖全部需求 —— 这比"三个独立功能"省得多。

| 预设 | 用途 | 筛选条件 | 时间轴处理 | 典型产物 | 交互设计 |
|---|---|---|---|---|---|
| **① 单球导出** | 截取单一精彩回合 | 当前回合（或勾选 1 个） | 单个片段，前后各留 1.5 s | `R012_24shots_18.4s.mp4`，几 MB | 第 9.6 节 · 方式①（**帧级微调 + 循环预览**） |
| **② 集锦合并** ⭐ | 人工选中若干回合拼成一条 | **人工选中/标记的回合**（`is_highlight` 或勾选） | 多段拼接，段间硬切 | `highlights_12rallies_3m42s.mp4` | 第 9.6 节 · 方式②（**多选 + 排序 + 试看接缝**） |
| **③ 垃圾时间压缩** | 自动精简非对抗间隙 | **全部静止段自动切除**（不依赖回合切分） | 保留对抗段，切除静止段 | `compact_full_6m20s.mp4`（10 min → 6–7.5 min） | 第 9.6 节 · 方式③（**双色时间轴 + 局部翻案**） |

**关键设计点：② 与 ③ 是同一个生成器的两个筛选条件，不是两个功能。** 实现上就是：

```
成片 = concat( [ rally[i].start_ms - lead , rally[i].end_ms + tail ] for i in 选中回合 )
```

**三个必须先钉死的技术要点**（详见第 9.6 节的交互设计）：

**① "垃圾时间"有两类，处理方式相反**

| 类型 | 位置 | 处理 |
|---|---|---|
| **回合间垃圾**（捡球、走动、擦汗、间歇、换球） | 回合与回合之间 | ✅ **直接切除** —— 压缩主体，通常占 20–40% 时长 |
| **回合内准备**（发球准备、球出手前的站位调整） | 回合内部 | ⚠️ **绝对不能切** —— 切了会看到"球凭空飞出来"（这正是第 2.2 ④-补定义"回合起点前推 2 s"的原因） |

**② 拼接点必须做音频交叉淡化**：回合间切除会造成**音频不连续**（前后两段底噪、混响、场馆声完全不同），硬接会有明显爆音。做法：**视频可硬切，音频必须做 80–150 ms 交叉淡化**。成本极低但极易漏，是"压缩版听起来专业不专业"的分水岭。

**③ 压缩力度三档**（"多紧凑"是审美问题不是技术问题，应让用户选）：

| 档位 | 回合前保留 | 回合后保留 | 10 min 素材预期成片 |
|---|---|---|---|
| 保守 | 2.5 s（含完整发球准备） | 2.0 s（含落地反应） | ≈7–8.5 min |
| **标准（默认）** | 2.0 s | 1.5 s | ≈6–7.5 min |
| 激进 | 1.2 s | 1.0 s | ≈5–6.5 min |

**④ 保底价值（重要）**：**"切除回合间静止段"只依赖"静止期检测"，不依赖"回合切分是否准确"** —— 静止期检测（球不可见 + 球场帧差低）是整条链路里最可靠的一环，而回合切分是最难的。所以**即使 P1 预研的轨迹召回不达标，方式③仍然可用**。建议把这条写成明确的产品降级策略：*"若切分置信度整体偏低，自动切换为紧凑模式——只压掉静止段，不做回合划分。"* 这为项目买了一份保险。

### 9.6 三种导出方式的交互设计（详细规格）

> 这一节是 D13 的落地设计。三种方式**共用同一个"片段选择器 + 成片生成器"内核**，但**入口与交互完全不同** —— 这是本节的核心结论。
>
> **统一抽象**：三种导出都归约为"**一个有序区间列表 → 拼成一条成片**"：
> ```
> 成片 = concat( [ [start_i, end_i] for i in 选中的片段 ] )   + 拼接点音频交叉淡化
> ```
> | 方式 | 选中的片段 | 独立入口 |
> |---|---|---|
> | ① 单球 | **1 个区间**（可微调边界） | 播放器页（"正在看的这个球"） |
> | ② 集锦 | **N 个区间**（人工挑选/标记） | 列表页（多选）+ 候选池 |
> | ③ 压缩 | **全部对抗区间**（自动，减掉静止段） | 时间线页（自动生成 + 可视化调整） |

---

#### 方式①：单球导出（预览时微调 + 单独导出）

**触发场景**：用户在播放器里看某个回合，觉得不错，想单独导出。

**操作流程（4 步，目标 15 秒内完成）**

```
① 播放中发现好球 → 按 D（或点"导出此球"）
        │
② 播放器切到"剪辑模式"：画面下方出现双柄轨道（入点/出点）
   · 当前回合已自动填好入点/出点
   · 立刻开始【循环播放】该片段 ← 关键：微调必须能循环看
        │
③ 微调边界（四种手段，覆盖粗调到帧级）
   · 拖动左侧/右侧柄        → 粗调（吸附到 0.1 s）
   · [ / ]                   → 入点 −/+ 1 帧
   · Shift + [ / ]           → 入点 ∓ 0.1 s
   · 快捷按钮：收紧到首拍 / 收紧到落地 / 保留发球准备 / 重置为自动值
        │
④ 导出 → 显示"时长 18.4 s · 约 12 MB"→ 确认
```

**界面元素（剪辑模式）**

| 元素 | 说明 |
|---|---|
| **双柄轨道** | 显示片段在整片中的位置；灰区 = 未选，亮区 = 已选；柄上显示时间码 |
| **时间码显示** | `00:01:23.450 / 帧 5007`（同时给时间和帧号，便于对帧） |
| **循环指示** | 明确显示"循环播放中"——微调时的核心反馈 |
| **四个快捷按钮** | 收紧到首拍 / 收紧到落地 / 保留发球准备 / 重置为自动值 |
| **导出按钮 + 预估** | 显示时长与预估体积（**预估体积很重要**，用户要知道发出去多大） |
| **🆕 相邻区间提示** | 若用户把入点拖过了上一个回合的结束点，提示"已包含上一回合的结尾" |

**设计要点**

1. **默认值就是自动值** —— 用户不想调就直接导出，想调才有工具。**不要空着让用户自己拖**。
2. **循环播放是微调的必要条件** —— 不能循环，用户就要反复按播放，微调效率会崩。
3. **同时给"时间"和"帧"** —— 60fps 下 1 帧 = 16.7 ms，只给秒会让人无法精确对齐击球瞬间。
4. **预设按钮比自由拖动更高频** —— 大多数用户要的是"刚好包含发球准备"或"从首拍开始"，这两个应做成一键。

**边界情况**

| 情况 | 处理 |
|---|---|
| 拖到入点 ≥ 出点 | 禁止，柄互相约束（最小保留 0.5 s），并给出提示 |
| 片段 < 1 s | 允许，但提示"片段很短（<1 s），是否确认？" |
| 用户想同时导出多个"当前球" | 引导到方式②（"加入候选池"按钮就在旁边） |
| 比分列为隐藏状态（D11） | 导出文件名不带比分：`R012_24shots_18.4s.mp4` |

---

#### 方式②：集锦合并导出（人工挑选回合，按序列拼接）

**触发场景**：用户想把这场的精彩球（或某几个特定回合）拼成一条，发出去。

**操作流程（4 步）**

```
① 挑选（三种入口，任选）
   · 列表页多选：Shift/Ctrl 连续或离散多选，勾选框
   · 筛选后全选：如"拍数 ≥10 的回合"→ 全选结果
   · 候选池：把球一个个丢进"候选池"（P 键 / 拖拽）
        │
② 排序（默认按原片时间顺序；允许自定义）
   · 候选池内拖拽排序 → 成片按拖拽后的顺序拼接
   · 显示编号 + 每段 缩略图/拍数/时长
   · ⚠️ 明确提示当前是"按原顺序"还是"按你的自定义顺序"
        │
③ 试看拼接效果（关键：这是发现接缝问题的唯一手段）
   · 点"预览成片" → 连续播放选中的 N 段（含拼接点淡化）
   · 拼接点用**视觉标记**在时间轴上标出（竖线 + 编号）
        │
④ 导出 → 显示"12 个回合 · 总时长 3 min 42 s · 约 240 MB"
```

**界面元素（成片页）**

| 元素 | 说明 |
|---|---|
| **片段序列视图** | 横向缩略图带，每段显示 `序号｜拍数｜时长`；可拖拽重排、可单段删 |
| **汇总栏** | `已选 12 个回合 · 总时长 3:42 · 预计 240 MB`（随挑选实时更新） |
| **重复检测提示** | 若选中的回合在时间上重叠（人工合并/拆分过的边界），提示并自动去重 |
| **拼接点标记** | 时间轴上的竖线，点一下可单独试听接缝 |
| **成片参数** | 模板（横屏/竖屏/方形）· 烧字项 · 分辨率 · **是否在每段前插 0.3 s 黑场或片头序号** |

**设计要点**

1. **"挑选"必须支持筛选后全选** —— 用户不会一个个点 20 个球；"拍数 ≥ 10"这类条件筛选 + 全选才是高效路径。
2. **顺序默认跟原片，但必须能改** —— 有人要按时间顺序（叙事），有人要按精彩程度（高潮在后）。
3. **试看拼接效果不可省** —— 否则用户导完才发现接缝有爆音或跳跃，会返工。
4. **实时显示总时长与体积** —— 集锦最容易失控的就是"选了 40 个球变成长片"，实时汇总能立刻劝退。
5. **可选"片头序号"** —— 每段开头烧 `#03` 之类，便于对照讨论（对教练场景有用，纯娱乐场景可关）。

**边界情况**

| 情况 | 处理 |
|---|---|
| 选中 0 个回合 | 导出按钮置灰，提示"至少选择 1 个回合" |
| 选中 1 个回合 | 允许，但提示"只选了 1 个回合，是否改用『单球导出』？"（引导到方式①） |
| 选中超过 50 个 / 总时长 > 10 min | 提示"成片较长（>10 min），是否确认？"（不阻止，只提醒） |
| 选自不同视频 | **v1 不支持跨视频合成**（明确写在 UI 上），避免引入"多源分辨率/帧率统一"的复杂度 |
| 相邻回合首尾相接 | 自动去重重叠部分，并在接缝处**优先用较长的音频淡化**（避免双重淡化叠加） |

---

#### 方式③：自动精简垃圾时间（捡球/擦汗等），然后导出

**触发场景**：用户想要一条"没有废话"的紧凑比赛视频，不想手动挑。

**操作流程（4 步，几乎零操作）**

```
① 进入"压缩"页 → 自动扫描全部静止段（复用切分链路的静止期检测）
   · 顶部统计卡：原片 9:58 → 预计成片 6:20（省 38%）
        │
② 压缩力度三档：保守 / 标准(默认) / 激进  ← 改动即时重算时长
        │
③ 可视化确认（这一屏是可信度的关键）
   · 时间轴：绿色 = 保留的对抗段；灰色 = 将切除的静止段
   · 点任意灰块 → 弹出预演（显示该段缩略图序列 + 原始时长 + "确认为垃圾"的可选标记）
   · 可手动"反选保留"某个灰块（例如某个停顿其实很有意思）
   · 可手动把某个绿块标为"也要切"
        │
④ 导出 → "紧凑版 6:20 · 约 420 MB"（可选"保留局间休息"开关）
```

**界面元素（压缩页）**

| 元素 | 说明 |
|---|---|
| **统计卡** | `原片 9:58 → 成片 6:20`、`切除 27 段静止 · 共 3:38`、`保留 42 个对抗段` |
| **双色时间轴** | 绿（保留）/ 灰（切除），块上标注时长；可缩放 |
| **灰块检查器** | 点灰块 → 缩略图序列（每 2 s 一帧）+ 该段起止时间 + 两个按钮：`确认删除` / `保留此段` |
| **压缩力度三档** | 保守 / 标准 / 激进（对应第 9.6 节的三档 lead/tail） |
| **🆕 "保留局间休息"开关** | 11 分技术暂停、换边、局间休息默认**保留**（这是比赛的一部分，不是垃圾）—— **必须做成显式开关，不能默认切掉** |
| **🆕 "跨回合不合并"开关** | 默认关闭；开启后不跨越"局"的边界做压缩 |
| **试听成片** | 预览压缩后效果（含拼接点淡化） |

**设计要点**

1. **必须先"可视化"再"导出"** —— 用户对自动切除天然不信任；让他看到"哪些被切了、切的是不是垃圾"，信任感才建立。**这一屏不能省。**
2. **灰块要能被局部翻案** —— 自动检测一定会有误判（把某个有意思的停顿切掉）。**"点一下就能保留"比"重新跑一遍"重要得多。**
3. **"保留局间休息"必须是显式开关** —— 局间休息和捡球在算法上很像（都是静止期），但语义完全不同。默认保留是安全选择。
4. **时长变化要实时可见** —— 用户判断"值不值得"的唯一依据就是"从 10 分钟压到几分钟"。
5. **不修改原片** —— 压缩只产生新文件，原片永远不动（明确写在 UI 上）。

**边界情况**

| 情况 | 处理 |
|---|---|
| 素材本来就很紧凑（无明显静止段） | 提示"这段视频已经很紧凑，压缩空间 <5%，是否仍要导出？" |
| 检出静止段异常多（多为误判） | 提示"检测到的静止段偏多（可能因光线/遮挡），建议改用方式②手动挑选" |
| 多球训练视频（无回合概念） | ✅ **天然可用** —— 压缩只依赖静止期，不依赖回合切分（第 9.6 节 ④）。这正是它的保底价值 |
| 整个视频只有一个长回合 | 压缩比会很低，提示"几乎没有可压缩的间隙" |
| 球出画频繁导致静止期误判 | 依赖 `visibility_raw` 与帧差双判据（第 17.3 节 T5）；UI 上给出"检测置信度偏低"的提示并建议保守档 |

---

#### 三种方式的关系与共同约束

| | 方式① 单球 | 方式② 集锦 | 方式③ 压缩 |
|---|---|---|---|
| 入口 | 播放器 | 列表页 / 候选池 | 压缩页 |
| 选择粒度 | 1 个区间，帧级可调 | N 个回合，整段 | 全部，自动减静止段 |
| 用户操作量 | 中（微调边界） | 高（挑选 + 排序） | **极低**（几乎零操作） |
| 依赖的能力 | 回合边界 | 回合切分 + 精彩标记 | **仅静止期检测**（最浅） |
| 技术风险 | 低 | **高**（依赖切分准确） | **低**（保底路径） |
| 典型产物体积 | 几 MB | 几十–几百 MB | 几百 MB |

**共同约束（三种都必须满足）**

1. **原片永不修改** —— 只产生新文件。
2. **拼接点做 80–150 ms 音频交叉淡化**（视频可硬切）。
3. **导出前显示时长与预估体积**。
4. **零编码预览** —— 调边界、改顺序、换档位都不触发编码（第 3.3 节）。
5. **导出是项目数据的纯函数** —— 同样的设置重复导出结果一致；改设置重导即可。
6. **可中断、可重试** —— 长成片分段编码，失败项单独重试。

**建议的实现顺序**：**先做方式③（依赖最浅、零操作、保底价值）→ 再做方式①（依赖回合边界，但只有 1 段，易验证）→ 最后做方式②（依赖切分准确 + 筛选 + 排序，最复杂）**。这个顺序让产品能最早交付一个可用的价值点。

### 9.7 由 D13 引起的其它连带调整

| 项 | 调整 |
|---|---|
| **合成音频的连续性验证** | 加入第 16.4 节 T2 的验收项：成片的拼接点**无可听爆音**（用波形检查或人工试听） |
| **时间线视图优先级上升** | 用户要"看到垃圾时间被压掉"，所以**全片时间轴 + 回合块 + 静止段可视化**从"锦上添花"升为 **P0**（第 5 节 UI 结构） |
| **"精彩球"标记升为 P0** | 它是集锦预设的主要筛选条件。原方案把它放在"标签"里（P1），现提升 |
| **暂存列表的定位变化** | 从"临时看一下这几个球"升级为"**集锦的候选池**"—— 拖进去的顺序就是成片顺序 |
| **`is_exportable` 语义明确** | 即 `outcome != NON_RALLY`，用于预设③；预设②用 `is_highlight` 标记 |
| **不需要"合并长片"这个模糊概念** | 原方案的"可选合并长片"被预设②③取代 —— 说清楚"合并的是什么"比给一个笼统的开关有用 |

---

## 10. Android 与 Windows 双端实施方案

**前提已定：全部本地处理，不做服务器（D2）。** 架构上因此简化为"一套算法核心 + 两个原生 UI 外壳 + 可选的工程包衔接格式"。

### 10.1 双端总览

| 维度 | Windows | Android |
|---|---|---|
| 定位 | 全能端：精算（含 4K）+ 批量 + 精细审核 + 多规格导出 + 归档 | 轻量但**闭环完整**：采集 + 720p 粗筛 + 审核标记 + 导出分享 |
| 目标用户场景 | 回家/办公室处理多局；大屏精修与批量 | 球馆现场：拍完就想看、想发 |
| 语言/UI | C++20 核心 + Qt6/QML | 同一 C++ 核心 + Kotlin/Compose |
| 推理后端 | ONNX Runtime：CUDA EP（N 卡）/ DirectML EP（A/I 卡）/ CPU 兜底 | LiteRT（TFLite）+ GPU delegate；NNAPI 作可选加速 |
| 视频 | FFmpeg 自编译精简版 + NVENC/QSV/AMF/x264 | MediaCodec 硬编硬解（必要时薄 FFmpeg 处理容器与旋转元数据） |
| 数据库 | SQLite (WAL) | SQLite（同一 schema） |
| 分析档位 | 快速 720p / 标准 1080p / 精细 **4K-ROI** | 快速 480p / **标准 720p 全流程**（1080p 仅插电+散热好） |
| 导出 | MP4（含 **4K** 原分辨率或指定规格）+ 合并长片 + 竖屏/方形模板 | MP4（720p–1080p）+ **相册/微信分享** + 竖屏模板 |
| **单局耗时（10 min 60fps）** | **≈5–12 min**（独显）/ 26–55 min（纯 CPU）⚠️ 待 A1 实测 | **≈17–33 min**（仅第一遍 720p，需插电）⚠️ |
| 安装包 | **200–400 MB** | **90–150 MB**（arm64 单 ABI，模型 int8 量化） |
| 工程包衔接 | 可导入/导出（可选） | 可导入/导出（可选） |

### 10.2 共享的东西（决定了两端是不是"两套产品"）

这三样必须从第一天就共用，否则会退化成两套独立代码：

1. **算法核心（platform-independent core）**：场地标定、球轨迹、事件融合、回合状态机、规则引擎、计分三态。纯 C++/Rust，无 UI 依赖、无平台 API 依赖，可单元测试、可编译到桌面与 arm64。
2. **数据模型与项目格式**：同一份 SQLite schema；**可选的"工程包"** = `project.json` + `rallies.csv` + `calib.json` + 可选 `proxy.mp4`，双端可互相导入导出（Android 粗筛结果 → Windows 继续精算，反之亦可）。
3. **参数档位系统**：把"分辨率/帧率/推理后端/线程数/窗口大小"全部抽象成 profile 配置文件，两端只是**不同的默认值**，不是不同的代码。这样调优一次，两端受益。

> 核心原则：**同一份代码，两端跑不同档位；而不是同一功能写两遍。**

### 10.3 Windows 端方案

**功能清单**

| 模块 | 内容 |
|---|---|
| 输入 | 文件/文件夹批量拖入、相机/采集卡、网络路径（NAS）；480p–4K、VFR 自动转 CFR、HDR tone-map |
| 分析 | 三档 profile；可暂停/取消/断点续跑；长任务期间可查看已完成回合 |
| 场地标定 | 自动 + 手动微调（拖 4–16 个点），重投影误差作为置信度显示 |
| 审核工作台 | 多列列表（序号/比分/拍数/时长/置信度/标签）、键盘全流程（`A`/`B`/`X` 三态标记、连播中标记）、多选批量操作、并排对比两回合、轨迹+骨架+音频波形同屏 |
| 播放器 | 逐帧 / 0.25x–2x / 事件刻度轴 / 叠加开关 |
| 库管理 | 按球员/赛事/日期归档，跨场次统计（胜率、拍数分布、站位热图趋势） |
| 导出 | 五种产物（见 9.3）；烧字开关；批量渲染队列 |
| 归档 | `-c copy` 无损导出 + 一键清理代理 |

**硬件支持矩阵（必须做能力探测 + 自动降级）**

| 硬件 | 推理 | 编码 | 单场 60 min（1080p） |
|---|---|---|---|
| NVIDIA 独显（8 GB+） | CUDA EP | NVENC | **8–15 min** |
| Intel 核显/Arc | DirectML 或 OpenVINO | QSV | 15–35 min（Arc 更快） |
| AMD 独显 | DirectML | AMF | 20–40 min |
| 纯 CPU（8 核+） | CPU EP（int8 量化，多线程） | x264 | 1–1.6 h |

- **必须有无显卡兜底路径**：用户机器千差万别，检测不到加速就自动切 CPU 并明确告知预估时间（而不是让用户以为卡死了）。
- 首次启动做一次"性能标定"（跑 10 秒基准测试），据此设定默认档位与分析时间预估，这个体验细节很影响留存。

**UI 结构（4 个主界面）**：库/项目页 → 分析进度页 → **回合审核页（核心）** → 导出页。设计核心是"审核效率"：键盘可达一切、连播中标记、未确认高亮计数、全操作可撤销。

**工程注意事项**
- 长时间任务（1–1.6 h 纯 CPU）必须有进度、可暂停、异常可恢复；Windows 休眠会中断任务，要申请"阻止睡眠"。
- 长路径与中文路径：Windows 的 260 字符限制与中文编码是常见坑，所有路径处理统一走宽字符 + `\\?\` 前缀。
- 安装器与依赖：Qt 依赖较多，建议用 NSIS/WiX 打包并带 VC++ 运行库；CUDA 作为**独立可选包**（不要把 1.5 GB 塞进主安装包）。

### 10.4 Android 端方案

**功能清单**

| 模块 | 内容 |
|---|---|
| 采集 | 应用内拍摄（**强制 50/60fps + 高速快门 + 曝光锁定**）、**拍摄前质量体检**、相册导入、分享导入、SAF 读取外置存储 |
| 分析 | 快速档（480p/降帧率，0.5–1× 实时）、标准档（720p 全帧 + 音频证据）；精细档仅"插电 + 温度正常"时开放 |
| 生存机制 | 前台服务常驻通知、分段落盘、断点续跑、温控降速、电量/耗时/温度三预估 |
| 审核 | 大按钮三态标记（左 A / 右 B / 中 非回合）、连播、慢放、逐帧、轨迹叠加开关 |
| 管理 | 回合卡片列表、暂存观看列表、标签、筛选 |
| 输出 | 生成 720p/1080p 片段（MediaCodec 硬编）、烧字、**导出相册 / 分享到微信钉钉**、**导出工程包供 PC 接力** |

**必须处理的平台约束（红线，不是优化）**

1. **前台服务类型**：长任务必须用前台服务 + 通知栏进度，并声明正确的 `foregroundServiceType`（媒体处理类）；Android 14+ 对类型校验严格，声明不当会被拒绝启动，部分类型还有每日累计时长上限 → 因此"可分段落盘 + 续跑"是架构要求。
2. **温控是主要矛盾**：持续满载会降频（吞吐掉到峰值一半以下），且**无法边充边跑**（充电发热叠加）。→ 必须做档位降级策略与"插电时才允许精细档"。
3. **存储访问**：分区存储/SAF 下常拿到**内容 URI 而非文件路径**，随机 seek 与大文件流式解码要额外封装 → FFmpeg 接入层写两套输入适配器（fd/URI 与普通路径）；申请 `READ_MEDIA_VIDEO` 类权限。
4. **MediaCodec 厂商差异**：分辨率/码率/profile/实例数支持不一，4K60 编码不保证 → 能力探测 + 多级回退（HEVC → H.264 → 软编）。
5. **端侧加速参差**：LiteRT GPU delegate 覆盖尚可；**NPU 高度依赖芯片与厂商运行时，不能当必需路径**，CPU+GPU 必须是可用保底。
6. **内存上限**：中端机可用堆有限，位图缓存与解码窗口必须显式设上限（目标峰值 < 1.2 GB）。
7. **后台被杀**：Doze/电池优化会清理后台 → 引导用户关闭本应用的电池优化（一次性设置引导）。

**设备分级建议**（用于设定"能不能跑标准档"）

| 档位 | 建议门槛 | 预期 |
|---|---|---|
| 可跑快速档 | 骁龙 6 系及以上 / 6 GB RAM / Android 11+ | 480p 粗筛，能出回合列表 |
| 可跑标准档 | 骁龙 8 系 / 天玑 8000 系及以上 / 8 GB RAM | 720p + 音频证据，1–1.5× 实时 |
| 推荐精细档 | 旗舰 SoC + 12 GB RAM + 良好散热 | 1080p，仅插电 |

**拍摄参数建议（这是手机端最容易被忽视但价值最高的部分）**

- 帧率：**60fps（已确认，D12；低于 50fps 会显著降低精度上限）**；
- 快门：**≥ 1/1000 s**（自动模式通常只有 1/120–1/500，杀球会糊成一条线）；
- 分辨率：1080p 足够，4K 收益有限而代价大；
- 机位：三脚架、场地完整入画、略高于网带、避免逆光与球场白线正对镜头；
- 建议在 UI 里做"推荐参数一键设置"并实时校验，比事后修数据高效得多。

### 10.5 双端衔接：可选的"工程包"接力

> ⚠️ **注意**：这不是必经流程。**两端各自都能独立完成"导入 → 分析 → 审核 → 导出"完整闭环**（手机走 720p 档）。"工程包"只在下面这类场景下才有价值：手机上先粗筛标好，之后想在 PC 上做 4K 精算或大屏精修。

```
[Android] 拍摄/导入 → 720p 粗筛 → 三态标注 → 直接导出片段并分享  ← 独立闭环（默认路径）
                                   │
                          （可选）导出工程包
                                   │  微信 / 网盘 / USB / 局域网
                                   ▼
[Windows] 导入工程包 → 复用 calib 与回合表 → 4K 精算 → 精修 → 高清导出 / 归档
```

**工程包内容（约 1–20 MB，不含视频时极小）**：`project.json`（视频指纹与规格）、`rallies.csv`（含三态、边界帧号、拍数、置信度）、`calib.json`（标定结果，PC 可复用避免重算）、可选 `proxy.mp4`（**一整支全片代理**，而非每回合一个）与 `thumbs/`。

**接力设计的两个价值**：① 手机现场出的粗筛结果不会被浪费（PC 直接在此基础上精算，省掉一次全量分析）；② 用户不必把 4K 原片传到手机或从手机传回（只传代理 + 元数据）。

### 10.6 研发路径与工作量

| 阶段 | 内容 | 单人估时 |
|---|---|---|
| S1 | **共享核心**：算法引擎接口、数据模型、SQLite schema、工程包格式、profile 系统 | 2 周 |
| S2 | **Windows MVP**：库 + 分析 + 审核 + 三态标注 + 切片导出（用 S1 核心） | 5–7 周 |
| S3 | Windows 完善：场地标定 UI、OCR（可选）、FCPXML/EDL、库统计、性能标定 | 3–4 周 |
| S4 | **Android MVP**：采集 + 质量体检 + 快速档 + 三态标注 + 分享导出 | 4–6 周 |
| S5 | Android 完善：标准档调优、前台服务健壮性、温控策略、工程包接力 | 2–3 周 |
| S6 | 双端联调 + 长稳测试（含弱机兜底、异常恢复） | 2 周 |

- **总计约 18–24 周（单人）**；若先只做 Windows 一条线，Windows 侧约 10–13 周可交付（S1+S2+S3）。
- **省时的关键**：S1 一定要先做且做扎实。如果为了"快点看到东西"而把核心逻辑写进 Windows UI 里，Android 端就会变成重写，总成本反而翻倍。
- **风险最高的是 S4 的"生存机制"**（前台服务 + 温控 + SAF），不是算法。建议在 S1 之后就先用一个最小 Demo 在真机上验证"能不能稳定跑 30 分钟"，早暴露早解决。

**里程碑验收标准（双端一致，因为核心相同）**

| 里程碑 | 验收 |
|---|---|
| MVP | 回合切分召回 **≥92%** / 精确 **≥88%**（60fps + 三脚架条件下的目标；30fps 或半场取景时按 ≥85%/≥80% 验收） |
| 审核流程 | 100 个回合的三态标注 ≤ 10 min（Windows 键鼠）/ ≤ 25 min（Android 触屏） |
| 稳定性 | 60 min 视频连续处理无崩溃、无内存增长、断点续跑成功 |
| Android 生存 | 真机连续 30 min 满载不崩、不被系统回收、温度可控 |

---

## 11. 浏览器 B/S 方案评估（❌ 已排除，仅存档）

> **决策更新（D6）：浏览器方案已排除，不做 Web UI。** 前端确定为原生客户端（Windows 原生 + Android 原生，共享同一 C++ 核心）。
> 本节保留评估过程作为技术记录（说明为什么曾考虑它、它的价值与代价在哪），**不再作为候选路线**；第 11.7 节中"以本地后台 + Web UI 为主形态"的建议**已作废**。
> 唯二仍然有效的结论：① **模型权重可以放在服务器做分发与版本管理**（与前端形态无关，属于可选的工程优化）；② **服务器不承担算力**（与第 8 节一致）。

> 本节评估一个具体设想：**"在服务器上建一个 HTML，承担 UI、调度与模型分发，内存与算力负载都放在本地。"**

### 11.1 先把"计算模型"这个词拆开

这个提法里的"计算模型"有两种可能含义，结论完全不同：

| 含义 | 放在服务器上？ | 说明 |
|---|---|---|
| **模型文件（权重）的分发与版本管理** | ✅ **非常适合** | 服务器当静态资源托管 ONNX/TFLite 权重，带缓存头与版本号；浏览器首次拉取后本地缓存。**这是这个方案最有价值的用途**：模型一更新，所有客户端自动生效，零安装、零分发成本 |
| **模型的实际推理执行** | ❌ 必须在本地 | 一旦在服务器上执行，就退回了"重算力上云"的老路（第 8 节已论证 2 核/2 GiB 不可行）。用户说"算力负载在本地"正是对的 |

所以正确的表述是：**服务器负责"把模型和 UI 递到手边"，本地负责"真正算"。**

### 11.2 一个必须先纠正的架构前提

**服务器不能主动向本地机器派发任务。** 这是 Web 的根本约束（同源策略 + 浏览器沙箱 + 没有入站连接），加多少 API 都不行。浏览器只能**主动来取**。

这意味着"后台调度"的语义变了，具体分三种情况：

| 场景 | 谁来触发计算 | 是否可行 |
|---|---|---|
| **浏览器标签页开着** | 页面内的 Worker 主动轮询/建立连接拉任务 | ✅ 可行（这就是标准 B/S） |
| **标签页关掉了，还想让服务器安排任务** | ❌ 没人执行 | 只有两种出路：① 页面用 WebSocket 保持长连接，靠 Service Worker/后台同步尽量续命（不可靠，会被浏览器节流或丢弃）；② **本地跑一个常驻小 Agent 去轮询** |
| **真正无人值守的夜间批量** | 必须有本地常驻进程 | → 需要本地 Agent，回到"控制面/计算面分离"的原方案 |

**这条修正很重要**：它意味着"服务器调度 + 本地计算"要真正做到无人值守，本地必须有个常驻进程；否则"调度"只能在"用户开着浏览器"的时段内生效（对个人用户其实够用，对批量场景不够）。

### 11.3 这个方案的真正优势（比"省个客户端"大得多）

1. **零安装、零分发、零更新**：不用做安装包、不用处理 DLL/驱动/VC 运行库、不用为 Windows/macOS/Linux 分别打包。UI 一改全员立刻生效。**这一条能省掉整条发布链路的工作量。**
2. **天然跨平台**：同一份 HTML 在 Windows / Mac / Linux / Android / iPad 上都能跑。**这恰好补上了第 6.5 节里"Android 端要单独写一套 Compose UI"的成本缺口**——如果浏览器方案成立，Android 端可以直接复用 Web UI，S4 的 4–6 周能大幅压缩。
3. **模型热更新**：权重放在服务器，改版本号即可灰度/回滚，不必让用户重装。
4. **局域网共创模式（被低估的场景）**：本地主机跑后端，**多个教练/学员各自用浏览器看结果**，主机统一调度——不用每台机器装软件。这是 B/S 在局域网内真正不可替代的价值。
5. **隐私更优**：视频**从不离开本地机器**（浏览器只读取本地文件句柄，不上传），比原方案还干净。

### 11.4 浏览器现在的能力盘点（决定"能跑多重的活"）

| 能力 | API | 现状 | 用途 |
|---|---|---|---|
| **GPU 推理** | WebGPU | ✅ **Chrome 144 / Edge 144 / Safari 26 均支持；Firefox 不支持**；页面覆盖率约 10.9% | 走 WebGPU 执行端跑球轨迹/姿态模型 |
| 推理运行时 | ONNX Runtime Web（WebGPU/WASM EP）、TF.js WebGPU 后端 | ✅ 成熟 | 复用现有 ONNX 权重，无需重训 |
| **视频解码** | WebCodecs `VideoDecoder` | ⚠️ Chrome/Edge 支持好且可请求硬件加速（`hardwareAcceleration` 提示）；Safari 较弱；需 `isConfigSupported()` 探测 + 回退 | 逐帧解码，避免把整段视频读进内存 |
| **视频编码** | WebCodecs `VideoEncoder` | ⚠️ 可用但支持面比解码窄，4K/HEVC 不保证 | 导出回合片段 |
| 本地大文件访问 | File System Access API（`showOpenFilePicker`） | ⚠️ **仅 Chromium 系**；句柄可持久化（刷新后免重选）；Safari/Firefox 只能走 `<input type=file>`（会复制一份，4K 大文件不可接受） | 直接读本地原片 |
| 本地持久化 | OPFS / IndexedDB | ✅ 可用 | 存工程、回合索引、缓存模型与代理片段 |
| 多线程 | Web Worker + `SharedArrayBuffer` | ✅ 可用（需正确响应头） | 解码/推理/编码流水线并行 |
| 接近原生的算力 | WASM SIMD / 多线程 | ✅ 可用 | WebGPU 不可用时的 CPU 兜底 |

**由此得出的能力边界**：

- ✅ **可行**：720p 快速粗筛、音频证据分析、回合状态机、三态审核 UI、列表播放、生成低码率片段、导出工程包。**这套"能出回合列表并审核"的核心闭环，浏览器完全扛得住。**
- ⚠️ **勉强**：1080p 全帧分析（取决于显卡与浏览器），纯 WASM CPU 路径会明显慢。
- ❌ **不现实**：4K 两级 ROI 精细分析、批量多场、`-c copy` 无损切割（需要精确显存/磁盘控制）、无人值守夜间批处理。

### 11.5 三个方案对比

| | 方案 A：纯浏览器（B/S） | 方案 B：浏览器 UI + 本地原生 Agent | 方案 C：原生双端（第 10 节原方案） |
|---|---|---|---|
| 服务器角色 | 托管 UI + 模型 + 调度 API | 同左（可放公网） | 无服务器 |
| 计算执行者 | 浏览器 Worker（WebGPU/WASM） | **本地常驻 Agent（原生 C++，性能与体积最优）** | 原生客户端自身 |
| 安装成本 | **零** | 需装一个轻量 Agent | 需装完整客户端 |
| 跨平台 | **一份代码全平台** | 全平台（Agent 需分平台编译） | 需分别开发 |
| 性能上限 | 中（受浏览器沙箱与 API 限制） | **高（等同原生）** | 高 |
| 4K / 批量 | ❌ | ✅ | ✅ |
| 无人值守 | ❌（需保活标签页） | ✅ | ✅ |
| 硬件编码 | ⚠️ WebCodecs 支持面窄 | ✅ NVENC/QSV/AMF/MediaCodec | ✅ |
| 模型热更新 | ✅ | ✅ | ❌（需重装） |
| 局域网多人共看 | ✅✅（最大优势） | ✅ | ❌ |
| 隐私 | ✅✅ 视频不出本机 | ✅✅ | ✅✅ |
| 额外工作量 | 中（一套 Web UI） | 中高（Web UI + Agent 协议） | **高（两套原生 UI）** |

### 11.6 服务器地域带来的问题（必须指出）

现有服务器在**美国（弗吉尼亚）**，而用户大概率在国内。如果"UI 在服务器、算力在本地"，那么**每一次 UI 交互、每一个列表翻页、每一次进度轮询都要绕太平洋一圈**（国内到美东往返通常 200–400 ms）。结果是"算力本地化了，但操作体验被网络拖垮"——这与"把负载放本地"的初衷正好相冲突。

**三种解法，推荐第 2 种**：

1. 把服务器迁到国内节点（若必须用公网 B/S）。
2. **后端与浏览器都跑在本地**（`localhost:PORT`，零网络开销），只把**工程元数据**（不含视频）选择性同步到云端，用于"在外面用手机看一眼队列和结果"。→ 兼顾体验与远程能力，且这个后端与第 11.2 节的"本地 Agent"可以是同一个进程。
3. 完全不用公网：局域网内主机跑后端，其他设备用 `http://主机IP:PORT` 访问（家用/球馆场景最优）。

### 11.7 推荐结论

**采纳它的"UI 与调度"部分，不要采纳它的"推理执行"部分。**

具体建议：

1. **保留第 10 节的核心决策不变**：算法核心仍是**平台无关的 C++/Rust 库**（不要为了 Web 而把核心改写成 TypeScript，否则将来原生端要重做一遍；也不要一上来就走 WASM + WebGPU，调试成本很高）。
2. **把 Web UI 作为"第三个可选前端"**：新增一个**本地后端服务**（HTTP + WebSocket，监听 `127.0.0.1` 或局域网），对外提供 API 并托管 Web UI。原生客户端（Windows/Android）和浏览器**连的是同一套 API**。这样：B/S 的零安装优势拿到了，原生端的性能上限也保住了，且两条路共用一套业务逻辑。
3. **模型分发放服务器是明确的加分项**：权重走带版本号的静态资源 + 缓存，实现热更新。
4. **算力分流策略**：
   - 浏览器 Worker 负责**轻活**（音频分析、状态机、720p 粗筛、审核 UI、预览片段）；
   - 一旦用户要求 1080p/4K 精算或批量，自动**降级引导到本地原生引擎**（同一台机器上通常就是那个后端进程本身）。
5. **若做纯 B/S（方案 A）**，必须明确标注能力边界，并把下列限制写进 UI：仅 Chromium 内核支持完整功能（Firefox 无 WebGPU、Safari 的 WebCodecs 与文件访问较弱）；标签页必须保持打开；不支持 4K 精算与批量。
6. **与第 10 节的关系**：这不是推翻原方案，而是**给第 10 节加一个"Web UI 前端"选项**，并把"是否用它替代 Android 原生 UI"作为一个新的待确认决策（见第 13.2 节）。考虑到 Android 原生 UI 要 4–6 周，而 Web UI 一次开发即可覆盖桌面+移动，**这个替代的性价比很高，值得认真评估**。

**一句话总结**：服务器出 HTML + 调度 + 模型分发，本地出算力与内存 —— **方向正确，但"本地"必须有一个常驻进程或至少一个开着的标签页来承接计算；并且"计算"不该放进服务器，服务器也不该负责执行。** 最优落地形态是：**本地后端 + Web UI（再加可选的原生高性能引擎）**，而不是"公网服务器 + 浏览器算力"。

---

## 12. 场景收敛：业余娱乐单局（6–10 分钟）的专项分析

> **新增需求约束（D5）**：**用户是业余打着玩的人群；素材为单视频单局，典型时长 6–10 分钟。**
> 本节重新评估前 11 节中所有基于"60 分钟正式比赛"得出的结论。**结论：短单局让整个方案的难度、成本、体积、平台选择都显著改善，但也让"拍摄质量差"和"精度上限"成为新的主要矛盾。**

### 12.1 需求其实变了：这不是"比赛复盘工具"，是"短视频素材机"

先把这个场景的真实意图说清楚，因为它会改变全部优先级：

| | 正式比赛复盘（原假设） | **业余娱乐单局（实际）** |
|---|---|---|
| 用户 | 教练、球队分析师 | 打球的人自己、偶尔发给球友 |
| 核心诉求 | 战术分析、落点、拍型、跨场次统计 | **"把这场打得好的球剪出来"** |
| 最终去处 | 教练复盘、专业软件精剪 | **抖音/小红书/B站/微信** |
| 单次素材量 | 40–90 分钟 | **6–10 分钟** |
| 单次回合数 | 300–600 | **20–50** |
| 比分重要性 | 重要（要复盘得失分） | 中低（**自己记得比分**） |
| 关键指标 | 精度、召回、统计维度 | **"我 30 秒内看到这场有几个球值得发"** |

**这个转变带来的最重要结论：**

- ✅ **"没人安装"成了比"性能"更硬的约束** → 这曾让浏览器方案看起来很有吸引力（已在 D6 排除，因为性能与能力上限更重要）；**原生客户端必须把"安装后的第一分钟体验"做到极简**（拖入视频 → 自动分析 → 直接出回合列表，不要有向导和配置步骤）。
- ✅ **导出即分享**（内置竖屏模板 + 一键发抖音/微信）的价值**高于** FCPXML/EDL 工程文件（那是专业流程的事，这个人群用不上）。
- ✅ **"自动计分"的重要性进一步下降** —— 业余单局自己清楚比分。**第 7 节的三态人工确认选得更对了。**
- ❌ **跨场次统计、多机位、库管理、批量队列**这些"专业功能"基本可以砍掉（或降到很低的优先级）。
- ⚠️ **新增最重要的一个需求**：**素材质量的引导与自动修复**。业余素材的拍摄质量差是精度杀手，而用户不会为此做任何学习成本高的操作。

### 12.2 短视频对技术假设的改变（逐项重新计算）

按 **10 分钟、1080p60** 估算（业余单局上限），对比原先的 60 分钟口径：

| 项 | 60 分钟口径 | **10 分钟单局** | 变化 |
|---|---|---|---|
| 总帧数 | 216,000 | **36,000** | ↓ 6× |
| Windows + 独显分析 | 8–18 min | **1.5–3 min** | ✅ 快到几乎无感 |
| Windows 纯 CPU | 1–1.6 h | **10–16 min** | ✅ 可接受 |
| **Android 中端机 720p 粗筛** | 1.5–3 h | **5–10 min** | ✅ **从"基本不可行"变成"可行"** |
| Android 标准档 1080p | 4–10 h | **25–60 min** | ⚠️ 仍偏慢，但插电时可用 |
| 源文件体积（1080p60 H.264） | 6–10 GB | **1–1.7 GB** | ✅ 手机本地存储无压力 |
| 源文件体积（4K60 HEVC） | 12–25 GB | **2–4 GB** | ✅ 4K 工作流重新变得可行 |
| 单场磁盘占用（源+代理+切片） | 12–20 GB | **2–4 GB** | ✅ 逐条推翻第 8 节的"盘不够写满"结论 |
| 中间帧缓存（512×288 全片段驻留） | 不可能 | **10 min@60fps ≈ 9 GB RGB**，仍偏大；但 **720p 降采样缓存 ≈ 1 GB，完全可行** | ✅ 可以做"整片段常驻"简化架构 |

**三个可直接落地的架构简化（只在短单局成立）：**

1. **可以"整片段一次性处理"**，不必做复杂的"有界窗口流式 + 分段落盘 + 断点续跑"——后者是为 60 分钟长视频设计的，短单局可以直接简化（仍保留"可取消"即可）。**这能砍掉不少工程复杂度。**
2. **允许"两遍处理"**：第一遍快跑（720p/降帧率）出候选回合 → 第二遍**只对候选回合的原始帧做高精度复算**。10 分钟素材跑两遍的代价完全可接受，而精度收益明显。**这是本场景下性价比最高的一招**（原方案因为长视频代价太高而不敢用）。
3. **模型加载开销占比变大**：10 分钟素材总计算量小，模型初始化（几百毫秒到几秒）在总耗时里占比上升 → **常驻进程的价值提高**（避免每次冷启动）。这也意味着**桌面端可以让引擎服务常驻后台**，用户拖入视频即可立刻开跑。

### 12.3 拍摄与素材规格：从"硬性要求"改为"引导 + 自动修复"

> **素材条件**：**三脚架手机拍摄**（D10）+ **60fps**（D12）—— 这两条组合起来是本方案最重要的有利条件：前者让"静止期检测"成为极可靠的第一道锚，后者把杀球的帧间位移压到可关联范围内。下表原按"手持 + 30fps"准备的降级措施**大部分不再需要**，但保留作为异常素材的兜底路径。

原方案要求"≥50fps + 快门 ≥1/1000s"，这对业余用户是**不可能落地的要求**。**好消息是这两条在本场景已基本满足**（60fps + 三脚架）。仍需处理的残余问题如下：

| 项 | 原要求 | 业余现实 | 新的处理策略 |
|---|---|---|---|
| 帧率 | ≥50fps | ✅ **60fps（已确认，D12）** | **无需降级处理**。注意两点：① 富余的帧数可用于**轨迹平滑与亚帧插值**，把击球时刻定位精度做到亚帧级；② 但仍要**自动检测并转 CFR**（60fps 录制同样可能是 VFR，防抖机型尤甚） |
| 快门/模糊 | ≥1/1000s | 自动模式常 1/120–1/500，杀球糊成一条线 | 不做硬要求。改为**多帧累积/去模糊预处理**；UI 只做"拍摄小贴士"，不阻断流程 |
| 分辨率 | 1080p 足够 | **720p–4K 都有，4K 是常态** | ✅ 全部支持；**4K 走两级 ROI**（第 2 节 ③-补二） |
| 机位稳定性 | 固定三脚架 | ✅ **三脚架为主（已确认）** | **无需稳定化预处理**；保留"检测到抖动时降级"的兜底路径即可 |
| 场地入画 | 全场完整入画 | 多数能拍到全场；仍可能有半场取景 | 优先全场标定；**保留"半场标定"作为降级路径**（只标定可见的一半：底线 + 两条边线 + 发球线，靠单应外推另一半） |
| 标定时机 | 前 N 秒中值 | 三脚架下机位基本不动 | **可"标定一次、全片复用"**（配合滑动窗口重标定作为保险，用于检测意外碰动） |
| 光照/曝光 | — | 球馆顶灯、逆光、逆光白墙 | 分析前做**亮度/对比度归一化**；极端逆光时提示"这段精度低" |
| **球出画** | 未考虑 | **可能会出画（已确认）** | **出画即标 UNKNOWN，绝不外推猜测落点**；该回合的结束原因记为"出画"，交人工确认 |

> **重要提醒（必须写进产品）**：业余素材的失败模式大部分**发生在算法介入之前**。三脚架 + 60fps 已经解决了其中两条最主要的（抖动、帧率），**剩下的关键变量是光线**（光线不足 → 自动快门变慢 → 球糊）。因此拍摄引导只需给一句最关键的："**把手机架稳、横屏、让整个球场入画、保证光线充足**"。

### 12.4 短单局独有的"全局校验"机制

> ⚠️ **状态更新（D11）**：**比分非核心、甚至可以不显示**，因此下面**机制 1（终局比分硬约束）与机制 2（发球方规则校验）从"核心功能"降为"可选辅助"** —— 它们只在用户愿意使用三态记分时才有意义。
> **主校验改为"机制 3（双向起止）+ 机制 4（静止期结构）"**，因为这两者**不需要用户输入任何信息**，与"比分可不显示"的前提一致（见第 16.9 节 D11 的补偿说明）。

**（可选）机制 1：用已知终局比分做硬约束 —— 仅当用户启用记分时**

业余用户打完一局，**通常记得最终比分**（比如 21-18）。那么：

```
若 用户输入的最终比分 = (A_POINT 个数, B_POINT 个数)
   → 说明回合检出与三态标注自洽，高置信通过 ✅
否则
   → 差值 = 漏检回合数 / 误判回合数，直接得到"还差几个球"的精确提示
   → 引导用户去列表里找（比如"还差 2 个 A 得分，建议检查拍数 < 3 或置信度低的回合"）
```

这是一个**关于整段视频的、可验证的全局约束**，比任何"置信度"都可靠。但**前提是用户愿意记分** —— 由于比分非核心，v1 把它做成"可选的加分项"，而不是必经流程。

**（可选）机制 2：用发球方规则做一致性校验 —— 同样依赖记分**

- 若某回合的三态标注会导致"比分跳跃"（5-3 直接到 5-5），提示"疑似漏了一个回合"；
- 若发球方与标注结果矛盾，提示"该回合的发球方看起来不对"。

**注意**：业余打球常有"重新发球""没算分""连着练几个球"的情况，所以**这些校验只能做"提示"，绝不能自动改正**（与第 9.4 节的三态原则一致）。

**🆕 主校验机制 4：静止期结构校验（不依赖任何用户输入）**

单局视频中，"静止期 → 回合 → 静止期"应当**严格交替**。由此可得两个零成本的强校验：

| 异常模式 | 推断 | 提示 |
|---|---|---|
| **静止期缺失**（两个回合直接相连，中间没有 ≥2.5 s 的静止段） | 大概率是**漏切**（把两个回合并成了一个）或**多切** | 高亮该处，建议"拆分为两个回合" |
| **回合内缺少首拍**（某回合直接从连续对拉开始，没有"球自静止启动"的起点） | 大概率是**起点定位错误**（把上一条的尾巴并进来了） | 高亮该回合，建议复核起点 |
| **静止期异常长（>60 s）但无回合** | 可能是局间休息/换边，属正常 | 不提示，仅在时间轴上标注分段 |
| **静止期过短（<1.5 s）** | 业余发球前几乎总有 ≥1.5 s 准备，过短可能是**误合并** | 低置信提示 |

**🆕 主校验机制 5：发球节奏自洽性（不依赖用户输入）**

发球必然是回合的**第 1 拍**，且发球前必然有静止期。因此可以校验"每个回合的第一个击球事件是否具备'球自静止/低高度启动'的特征"。若某回合的首拍看起来是"高速对拉中段"，说明回合起点大概率切错了。

**机制 3：双向起止校验**

单局视频的天然边界是"开始发球"和"最后一球落地"。因此可以：

- 从视频开头往后扫（发球检测）和从结尾往前扫（最后落地检测），**两端同时收敛**，中间对不上的地方就是最可疑的段落；
- 这对业余素材特别有用 —— 因为业余视频常把"开始前的热身""结束后的握手"都录进去，起止锚点能自动裁掉这些。

**结论**：**"短单局 + 已知终局比分"这个组合，让原本最难的问题（切分是否准确）变成了一个可以自我验证的问题。** 建议把"输入终局比分 + 自动校验"作为 v1 的**核心功能**，而不是可选项。这是本场景相对长视频方案最大的优势。

### 12.5 运行平台重新评估：手机端从"辅助"变成"可行"

| 平台 | 60 分钟口径结论 | **10 分钟单局口径** |
|---|---|---|
| Windows 本地 | 主力（8–18 min） | ✅ 主力（**≈5–12 min** ⚠️ 待 A1 实测） |
| **Android 本地** | ⚠️ 辅助（1.5–3 h + 发热，不推荐全量） | ✅ **可行且可独立闭环**（仅第一遍 720p，**≈17–33 min** ⚠️，插电跑，可接受）—— 精算档不做 |
| 浏览器（Web UI） | 只适合粗筛与审核 | ~~最适合~~ **已排除（D6）**：10 分钟单局下浏览器确实扛得住 720p 粗筛，但原生化在导出、编码器控制与长期维护上更优 |
| 服务器 | ❌ | ❌ **仍然不可行，但理由变了** —— 不再是"吞吐不够"，而是**内存硬约束与视频时长无关**（模型常驻 + 推理张量 + 解码缓冲要 1.5–3 GB，2 GiB 装不下）。这个结论被短视频"救不了" |

**平台结论更新**：

- **Android 端优先级上升**：中端机能在**插电跑十几到三十分钟**的情况下处理完 10 分钟单局，用户可接受（注意：这是第 17 节修正后的保守估计，非原先的 5–10 分钟）。M7 从"可选辅助"变成"值得做"。
- ~~**浏览器方案优先级上升**（第 11 节）~~ **已排除（D6）**。替代结论：**原生双端是最终形态**，因此"共享 C++ 核心 + 极简 Android UI"成为控制移动端成本的主要手段（第 10.2 节）。
- **服务器仍然不做**，但**模型分发**这件事依然可以放服务器（第 11.1 节）。

### 12.6 MVP 范围重估：砍掉什么、保留什么

| 功能 | 原优先级 | **业余单局场景下** | 理由 |
|---|---|---|---|
| 回合切分（自动） | P0 | ✅ **P0 不变，而且更有价值** | 核心价值就是"自动找出值得看的球" |
| **终局比分输入 + 全局校验** | 未规划 | 🆕 **P0（新增）** | 12.4 节，本场景最大的技术红利，成本低收益高 |
| 三态人工确认 | P0 | ✅ P0 | 每局只有 20–50 个回合，标记很快 |
| 浏览/连播/慢放 | P0 | ✅ P0 | — |
| **一键分享（微信/抖音/相册）** | P1 | 🔺 **P0 提升（移动端）** | 这才是用户的最终目的 |
| **竖屏/方形导出模板 + 烧字** | 未规划 | 🆕 **P0（移动端）** | 发短视频平台的实际形态；横屏原片发出去效果差 |
| 拍摄引导 + **VFR 转 CFR** + 稳定化 | 未规划 | 🆕 **P0（移动端）** | 12.3 节，业余素材的成败前提 |
| **半场标定**（而非全场） | 未规划 | 🆕 **P0** | 12.3 节，否则劝退大部分素材 |
| 暂存观看列表 | P1 | P1 | 依然有用（"这几个球发出去"） |
| 拍数/时长显示 | P0 | ✅ P0 | 用户就是想看"这球打了多少拍" |
| 4K 两级 ROI | P1 | ✅ **升为 P0**（D10：1080p–4K 都有、4K 是常态，全帧直接缩放会丢 92% 像素） | 只是"第一遍在 720p 跑、第二遍只在候选段回 4K ROI"，额外成本只落在少数帧上 |
| FCPXML / EDL 工程导出 | P0（原） | 🔻 **P2 降级** | 这个人群不用 PR/Resolve |
| 批量队列、库管理、跨场次统计 | P1 | ❌ **砍掉** | 与"单视频单局"矛盾 |
| 姿态/拍型分类 | P1（M5） | 🔻 P2 | 娱乐用户对"这是杀球还是吊球"兴趣有限；不如把回合切分做准。**姿态本身保留**（用于静止期检测），只是不做拍型分类 |
| 自动计分（落点判定 + OCR） | P2（M9） | 🔻 **P2 进一步降级 / 砍掉** | 比分非核心、可不显示（D11）；已有静止期结构与发球节奏两类主校验替代 |
| 比分板 OCR | P1 | ❌ **砍掉** | 业余视频没有比分板 |

### 12.7 时间与体积重估

| 项 | 原估算（60 min 专业场景） | **业余单局（10 min）** |
|---|---|---|
| Windows MVP | 10–13 周 | **6–9 周**（砍掉批量/库管理/工程导出，新增终局校验与分享） |
| 双端并行 | 18–24 周 | **12–18 周**（场景收敛带来的范围削减） |
| Windows 安装包 | 250–450 MB | **150–300 MB**（可去掉 CUDA 可选包与部分模型，用户机器普遍更弱） |
| Android 包 | 90–150 MB | **80–140 MB** |
| 单场分析 | 8–18 min（GPU） | **≈5–12 min** ⚠️ 待实测 |
| 单场切片导出 | 2–4 min | **20–40 s**（10 分钟素材的片段总量小得多） |

**总耗时预期（端到端，用户视角）**：导入 10 分钟视频 → 等 **2–3 分钟**（Windows）或 **5–10 分钟**（手机粗筛）→ 40 个回合已分好 → 连播标记 40 个回合约 **3–5 分钟** → 一键导出/分享。**全程 10 分钟以内。** 这是本场景可以承诺的用户体验。

### 12.8 不得不要的取舍（必须向需求方明示）

1. **精度上限仍受"60fps 之外"的因素支配**：帧率这一最大的物理约束已被消除（D12），剩余变量是**光线（夜间顶灯 → 自动快门变慢 → 糊）、球贴白线、遮挡、出画**。在 60fps + 三脚架条件下，**单局漏检 1–3 个回合、误切 0–2 个**是合理预期（比 30fps 时代明显改善）。但"三态人工确认 + 静止期结构校验"仍是**让产品可用的必要组成**，不要把宣传口径写成"全自动精准识别"。
2. **不做拍型识别是明智的**：把同样的工程量投在"切分准确 + 素材质量引导"上，用户感知的提升更大。
3. **单视频单局意味着没有"统计价值"**：拍数分布、胜率趋势、站位热图这些跨场次功能在本场景基本无意义 —— 除非用户长期使用并攒了很多局，那是 v3 的事。
4. **"值得发的球"筛选**：这是个隐藏的真实需求（用户想要的是"精彩球"，不是"全部 40 个回合"）。可以用**拍数 ≥ N、回合时长 ≥ X 秒**做启发式排序，但**真正的精彩度判定需要模型，v1 不要承诺**。

### 12.9 对本场景的一句话总结

**业余娱乐单局（6–10 分钟）把原方案的三个难点（算力、体积、平台）全部解除了，但把"素材质量"从一个注意事项变成了头号矛盾。** 因此：

- **技术重心转移**：从"追求模型精度"转向"**素材预处理（VFR/稳定化/半场标定）+ 全局自校验 + 高效人工确认**"；
- **产品形态转移**：从"专业桌面工具"转向"**零安装的 Web/移动端轻工具 + 一键分享**"；
- **最大红利**：单局 + 已知终局比分 = **可自我验证的闭环**，这是长视频方案永远得不到的东西，应当作为 v1 的核心功能来设计。

---

## 13. 路线图与里程碑

| 阶段 | 目标 | 交付 | 验收标准 | 预估（单人） |
|---|---|---|---|---|
| **M-1 可行性预研** 🆕 | **纯算法验证，无 UI**：P1 轨迹召回 / P3 半场标定 / P4 手机温控 / P5 两遍处理收益 / P6 失败模式普查 + 🆕 **A1 第一遍真实吞吐 / A2 事件窗口是否够用 / A4 静止期判据 / A6 4K60 硬解探测**（A5 ROI 消融可选） | 预研报告 + 失败模式对照表 + 实测时间预算 + Go/No-Go 结论 | **P1 逐帧召回 ≥90%（4K60）、最长断裂 <0.5 s —— 唯一硬门**；A1/A2/A4/**A6** 必须完成但可放宽 | **1–2 周**（可先用官方 `Amateur` 片段开跑，见第 17.6 节） |
| M0 需求冻结 | 锁定机位类型、单/双打、视频规格、导出用途 + 收齐 20 段基准素材 | 需求确认单 + 测试视频集（T1） | 有 20 段真实视频，覆盖第 16.4 节的失败模式 | 3–5 天 |
| M1 骨架 | 项目/数据库/FFmpeg 解码/播放器/回合表 UI（数据全为手工录入） + **素材体检与预处理（VFR→CFR、亮度归一化、稳定化检测）** | 可运行的切片与回看工具 | 手工导入能管理 100 个片段不卡；VFR 素材能被正确转 CFR 且帧号索引无漂移 | 2–3 周 |
| M2 场地标定 + 球轨迹 | ONNX Runtime 接入 TrackNetV2/V4（第一遍粗筛）+ TrackNetV3/InpaintNet（第二遍候选段）、标定叠加可视化 + **半场标定与滑动窗口重标定** | 分析进度 + 轨迹叠加 | **60fps + 三脚架素材下，球的可见帧追踪召回 ≥90%**（30fps 或半场取景按 ≥85% 验收） | 3–4 周 |
| M3 回合切分 | 静止期检测（帧差 + 球不可见为主判据）+ 轨迹运动学事件 + 状态机 + **时间索引输出（零编码）** + 可选整片代理 + **两遍处理（720p 跳帧粗筛 → 仅事件窗口跑 V3）** | 自动回合列表（立即可审核） | **回合切分召回 ≥92%、精确 ≥88%（60fps 目标，需 P1 实测确认）**；分析完成到可连播审核之间**无编码等待** | 3–4 周 |
| M4 人工确认 + 主校验 | 规则引擎 + 三态标注（`A方得分/B方得分/非回合`）+ 连播中标记 + 🆕 **静止期结构校验 + 发球节奏自洽性**（不需用户输入）；比分列为可选显示项 | 高效审核流 + 结构异常高亮 | 40 个回合标注 ≤3–5 min（键鼠）；结构异常能定位到可疑回合 | **1.5–2 周** |
| M5 导出与成片生成 | 🆕 **"成片生成器"**（三预设：单球 / 精彩集锦 / 垃圾时间压缩）+ 压缩力度三档 + **拼接点音频交叉淡化** + 烧字 + 竖屏/方形模板 + 相册/微信/抖音分享 | 导出向导 + 时间线可视化 | 从素材导入到把 1 条集锦成片发到微信 **≤10 min 全程**；成片拼接点**无可听爆音** | 2–3 周 |
| M6 姿态与动作分类（二期） | 骨架时序分类器（击球/发球/挥空） | 拍型与发球标识 | 击球/发球二分类 F1 ≥ 0.85（干净机位） | 3–4 周 |
| M7 Android 原生端 | Android 原生（共享 C++ 核心）：拍摄引导 + 质量体检 + 720p 粗筛 + 三态标注 + 分享 | APK | 手机上 10 分钟单局从导入到分享 ≤10 min；真机连续 30 min 满载不崩 | 4–6 周 |
| M8（二期可选）自动计分 | 落点判定 + 比分板 OCR，消费第 7.3 节的 `winner` 契约，主线零改动 | 自动计分模块 | 端到端比分 ≥85%（但已有 M4 的全局校验兜底，优先级低） | 3–4 周 |

**MVP 建议到 M4 为止（约 7–9 周）** —— 到这一步"识别回合 + 三态确认 + 全局校验 + 切片 + 回看"的闭环就成立了。M5 导出分享紧随（+2 周）。

> **已砍掉/降级（见第 12.6 节）**：批量队列、库管理与跨场次统计、FCPXML/EDL 工程导出、比分板 OCR、把 4K 两级 ROI 当作"整段视频都要走"的重流程 —— 这些是为"60 分钟专业比赛复盘"设计的，与"业余娱乐单局"不匹配。**注**：4K 的两级 ROI 本身**保留**（D10 下 4K 是常态），但只用于"第二遍的候选段"，不是全片流程。
> 双端并行研发的 S1–S6 拆分见第 10.6 节；**按业余单局场景重估，双端并行约 12–18 周**（原生双端，Android UI 需单独实现，靠共享核心控制成本）。

---

## 14. 风险清单与应对

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| R1 | **业余 30fps / 自动快门导致球糊成一条线**（头号风险；手持已排除，但 30fps 仍常见） | 回合切分崩坏 | 不作为硬性要求（用户不可能满足），改为 ① 拍摄引导与质量体检；② 30fps 时**加大轨迹平滑窗口、加长静止期判据**并在 UI 明示精度下降（**注意：不再回退到"音频为主"**，见 D9）；③ 依靠第 12.4 节的**静止期结构校验 + 出画标 UNKNOWN** 兜住
| R2 | 密集多拍中球贴近球员身体/被遮挡，轨迹断裂 | 拍数错、回合误合并 | InpaintNet 修复 + 音频峰值作为"补拍"证据 + 把"合并/拆分回合"做成一键操作 |
| R3 | **业余素材常见的"只拍到半场"** —— 全场标定根本不可行 | 无法标定 → 全盘失效 | **改用半场标定**（底线+两边线+发球线，靠单应外推另一半）；标定结果给置信度；标定失败时降级为"仅切片、不计分"（第 12.3 节） |
| R4 | 音轨质量差（现场嘈杂、有解说、有背景音乐） | 音频证据失效 | 音频仅作校验不作唯一判据；检测到音轨 SNR 低时自动降低其权重并在 UI 提示 |
| R5 | 比分板不存在或位置随镜头变化 | OCR 校验失效 | 计分以轨道 A 为主，OCR 为可选增强；无比分板时不影响主功能 |
| R6 | 双打场景复杂度爆炸 | 工期失控 | v1 明确只支持单打，双打列为 v2 并单独评估 |
| R7 | 版权与隐私（训练数据、他人比赛视频） | 法律风险 | 训练只用公开数据集（ShuttleSet 等）与其许可条款；产品默认全本地、不上传；如做服务器必须提供数据删除与加密 |
| R8 | 安卓内存/发热导致分析中断 | 端侧体验差 | 有界窗口流式推理 + 分段落盘断点续跑 + 温控降速策略；把全量分析引导到 Windows |
| R9 | 用户不信任自动结果 | 产品被弃用 | 证据可视化（轨迹/音频/落地帧）+ 低置信清单 + 键盘级高效审核 + 全操作可撤销 |
| R10 | 用户对"拍型识别"期望过高（吊/杀/高远/网前） | 验收失败 | 明确分期：v1 只承诺"击球/发球/未击球/回合结束"四类，拍型细分为 v2 研究项 |
| R11 | 关键帧对齐导致切片头尾不精确 | 观感差 | 默认重编码片段（短片段代价可接受）；提供"无损模式"并明示其偏差 |
| R12 | 4K/HEVC/可变帧率(VFR)源 | 解码与帧号对齐踩坑 | 统一走 FFmpeg 解码并重建恒定帧率索引；VFR 源必须转 CFR 后再分析 |
| R13 | **三态人工标注的误点会连锁污染后续比分** | 用户对结果失去信任 | 依第 9.4.3 节：`非回合` **不推进比分、不换发球权**（保守规则）；`PENDING` 回合不推进并在 UI 显示"待定"而非猜测；发球权锚定算法提供自检（把连续多回合标成同一方得分本身合法，故只做提示不做阻止）；全操作可一键撤销 + 撤销历史面板；导出前做一次"比分完整性检查"（是否存在未确认回合、比分与局数是否自洽） |
| R14 | 用户不看"非回合"提示，把捡球段误标为得分 | 比分漂移且难察觉 | 回合卡片默认按"置信度 + 拍数"排序并把拍数 < 2 的回合标记为"疑似非回合"提示（**仅提示，绝不自动改**）；🆕 **终局比分全局校验**（第 12.4 节）直接告诉用户"还差几个球、是哪一方"，比任何置信度都可靠 |
| R15 | 🆕 **手机电子防抖导致 VFR（可变帧率）** | 帧号↔时间戳漂移 → 切片边界全错、回合时长算错 | **列为最高优先级预处理**：导入时一律检测 VFR 并转 CFR，重建恒定帧率帧号索引；未转换前禁止进入分析流程 |
| R16 | 🆕 **手持视频晃动使标定漂移** | 球员位置与落点判定逐渐失准 | 滑动窗口重标定（每 30–60 秒或检测到漂移时重标）；稳定化失败时自动降级为"仅切片 + 人工确认"模式并明确告知 |
| R17 | 🆕 **用户对精度期望过高**（以为是"全自动精准识别"） | 差评与弃用 | 宣传口径明确为"自动切分 + 人工快速确认"；在产品内用第 12.4 节的"还差 N 个球"把不确定性变成可操作的任务，而不是隐藏起来 |

---

## 15. 决策状态（已确认项 + 剩余待确认项）

### 15.1 已确认（本方案已按此调整）

| # | 决策 | 状态 | 影响 |
|---|---|---|---|
| D1 | **计分采用人工确认，三态：`A方得分` / `B方得分` / `非回合`** | ✅ 已定 | M4 从 3–4 周压到 1.5–2 周；落点判定与 OCR 剥离至二期；三态契约见第 7.3、9.4 节。⚠️ 语义待确认项见第 16.5 节 |
| D2 | **全部本地处理，不做服务器** | ✅ 已定 | 取消服务器批量里程碑；架构简化为"一套核心 + 两个外壳 + 工程包接力"；现有服务器不再纳入方案 |
| D3 | **同时做 Android 与 Windows 两端** | ✅ 已定 | 研发路径见第 10.6 节（S1–S6，约 18–24 周）；S1 共享核心必须先做 |
| D4 | 评估"服务器 HTML（UI+调度+模型分发）+ 本地算力" | ❌ 已排除（第 11 节存档） | 结论：推理必须留在本地；且服务器无法主动派活。该路线整体放弃，仅保留"模型权重可放服务器做分发"这一条可选优化 |
| D5 | **场景收敛：业余娱乐、单视频单局、时长 6–10 分钟** | ✅ 已定（第 12 节） | 算力/体积/平台三大难点基本解除（手机端仅跑第一遍即可闭环）；**素材质量成为头号矛盾**；砍掉批量/库管理/FCPXML/比分板 OCR；导出重心转向**竖屏模板 + 一键分享**；双端并行从 18–24 周降到 **12–18 周**。注：① 原表中"新增终局比分全局校验为核心功能"已被 **D11** 取代；② **单场耗时数字已被第 17 节复核修正**（原 1.5–3 min 低估了数量级） |
| D6 | **排除浏览器方案** | ❌ 已排除（第 11 节仅存档评估） | 不做 Web UI；前端为**原生客户端**（Windows 原生 + Android 原生，共享同一 C++ 核心）。第 11 节的评估结论保留作技术记录，不再作为候选路线。相应地，"本地后台 + Web UI"不作为主形态，第 11.7 节的建议作废 |
| D7 | **确立"粗切片 / 预览 / 导出"三层分离架构** | ✅ 已定（第 3.3 节） | 分析与预览**零编码、零视频文件**（回合 = 时间索引）；**唯一一次编码发生在导出**；放弃"每回合生成代理片段"的旧设计，改为"可选一支全片代理 + 索引播放"。顺序调整、删除、合并、改边界全部变成纯数据操作 |
| **D8** | **`非回合`语义 = "不计分、回合作废"（三态，非四态）** | ✅ 已定（第 9.4.3 节） | 数据模型保持三态；"非回合不推进比分、不换发球权"成立；无需四态扩展 |
| **D9** | **音画偏移 → 以球轨迹为唯一主证据，音频降为可选增强** | ✅ 已定（第 2.2 ④-补二） | 移除 v1 的音频-视觉对齐逻辑；粗筛档无需解码音频；**M-1 决策门只看 P1/P3**；音频保留为"增强档"，启用前须先估计并补偿偏移 |
| **D10** | **素材条件：三脚架手机拍摄为主 / 单打与双打都有 / 球可能出画** | ✅ 已定 | ① 手持稳定化工作量消除；② 静止期检测更可靠，成为切分的**第一道锚**；③ **双打必须支持**（但只做切分/拍数/三态，不做空间判定）；④ 球出画是常态 → **出画即标 UNKNOWN，不猜落点** |
| **D14** | **素材规格收敛为「全部 4K 60fps」，且开发前提供典型素材** | ✅ 已定（第 18 节） | ① **A3（素材来源）解除**，但**新增 A6：4K60 硬件解码可用性验证 —— 新的最高优先级环境风险**；② **硬解从"建议"变为"必需"**，不可用则强制转代理；③ **代理生成从可选变为 4K60 场景的默认项**；④ 导出必须依赖硬件编码；⑤ 内存峰值上修（Win 2.5–4 GB / 手机 1.2–1.8 GB）；⑥ **第一遍策略不变**（仍是 720p，因为 4K 降采样后球的信息量与 1080p 基本等价）；⑦ ROI 消融实验（A5）优先级上升 |
| **D11** | **比分非核心，甚至可以不显示** | ✅ 已定（第 12.4 节修订） | 三态标注保留（作为切分质量校验 + 训练数据来源），但 **UI 默认隐藏比分列**、**烧字默认不烧比分**；"终局比分全局校验"降为**可选辅助**；主校验改为**静止期结构校验 + 发球节奏自洽性**（不需用户输入） |
| **D12** | **典型帧率 = 60fps** | ✅ 已定 | ① **最大精度约束被解除**：杀球帧间位移压到可关联范围，边界定位可做到亚帧级；② **验收目标上调**：切分召回 ≥90%→**≥92%**、精确 ≥85%→**≥88%**、边界误差 ≤0.5s→**≤0.3s**；③ **算力成本翻倍**（36,000 帧/局），必须用"第一遍跳帧 + 第二遍仅事件窗口 + 亚帧插值"三条手段压回（第 4.3 节 C）；④ **VFR 检测仍必须做**（60fps 录制同样可能是 VFR）；⑤ 残余头号风险从"帧率/抖动"转为**光线不足导致糊球**（R1） |
| **D13** | **切片分析的真实用途 = ① 截取单一精彩回合导出 ② 精彩回合剪辑成合辑 ③ 精简非对抗垃圾时间** | ✅ 已定（第 9.5 节） | ① **导出主产物从"每回合一个 MP4"改为"一条成片"** —— 旧版主次颠倒，已修正；② 三种用途归并为**一个"成片生成器"的三个预设**（单球 / 精彩集锦 / 垃圾时间压缩），不是三个功能；③ **"精彩球"标记升为 P0**（集锦主筛选条件，需连播中一键标记）；④ **时间线可视化升为 P0**（让用户看到垃圾时间被压掉）；⑤ **新增音频连续性要求**：拼接点必须做 80–150 ms 交叉淡化；⑥ **发现一个低风险保底价值**：垃圾时间压缩只依赖"静止期检测"，**不依赖回合切分是否准确** → 即使 P1 预研不达标，产品仍有可用价值（降级为"紧凑模式"） |

### 15.2 仍需确认（进入 M0 前请拍板）

**A 类：阻塞开工**

| # | 问题 | 状态 |
|---|---|---|
| ~~A1~~ | ~~`非回合`的语义：三态还是四态？~~ | ✅ **已确认 = 三态**（D8） |
| ~~A2~~ | ~~是否接受先做 M-1 预研 + 决策门？~~ | ✅ **已确认接受**，决策门按 D9/D12 收敛为"唯一硬门 = P1" |
| ~~A3~~ | ~~20 段基准测试素材由谁提供？~~ | ✅ **已解除** —— 开发前会提供典型素材（D14）。⚠️ 但需确认**数量与条件覆盖**（单打/双打比例、时间段、光线条件、是否含球出画），因为第 16.4 节的验收指标依赖覆盖度 |
| **A6** 🆕 | **4K60 硬件解码可用性验证**（第 18.1 节） | ⛔ **新增必做项，且是当前最高优先级的环境风险** —— 若目标机器（含手机机型）不支持实时硬解 4K60，整个时间预算的前提就要改（必须走代理模式） |

**B 类：影响精度与工作量（多数已由 D10 回答）**

| # | 问题 | 状态 |
|---|---|---|
| B1 | 手持还是三脚架？ | ✅ 三脚架为主（D10） |
| B2 | 单打还是双打？ | ✅ 两者都有；双打只做切分/拍数/三态（D10） |
| B3 | 分辨率/帧率？是否有 4K？ | ✅ 1080p–4K，4K 是常态（D10）；**帧率 = 60fps（D12）** |
| B4 | 球是否经常出画？ | ✅ 可能会出画（D10）→ 出画即标 UNKNOWN |
| B5 | 是否愿意输入终局比分？ | 🔻 已降级：比分非核心、可不显示（D11），此项不再关键 |

**C 类：影响产品形态，不阻塞算法开发**

| # | 问题 |
|---|---|
| C1 | 是否需要"精彩球筛选"（拍数 ≥N / 时长 ≥X 秒排序）？ |
| C2 | `A`/`B` 是否映射到具体球员姓名？ |
| C3 | 烧字要显示哪些项、什么语言？（**比分已定默认不显示**，见 D8/D11；其余项待定） |
| C4 | Windows 与 Android 是并行开发还是先 Windows 后移动？ |
| C5 | 是否需要"检查更新"功能与崩溃日志上报（默认关闭）？ |

---

## 16. 方案审阅结论：遗漏项、开工前预研与待确认清单

> 本节是对全案的通读自查结果，分三部分：**16.1 已修正的内部不一致**、**16.2 开工前必须先做的预研（本方案最大的遗漏）**、**16.3–16.7 补齐的缺失内容**、**16.8 待确认项汇总**。

### 16.1 本次通读已修正的内部不一致

| # | 位置 | 问题 | 修正 |
|---|---|---|---|
| 1 | 第 1 节 | 仍写"单场 40–90 分钟""固定单机位全景为主目标场景""≥50fps 强烈建议" | 已按 D5 改写；并标注该节 6 项中 5 项已定、仅烧字语言待定 |
| 2 | 第 1 节 | 转播源（场景 A）被列为主要来源之一 | 已标注"本场景不适用" |
| 3 | 第 2.2① / ③ | "只标定一次、全片复用"与第 12.3 节"手持需滑动窗口重标定"矛盾 | 已改为"固定机位可复用；手持必须滑动重标定；半场走半场标定" |
| 4 | 第 2.2③-补 | 验收标准写 ≥92%/≥88%，与第 12 节下调后的 ≥90%/≥85% 矛盾 | 已统一为 ≥90%/≥85%（固定机位可上探 92%/88%） |
| 5 | 第 4.1 | 模型清单含已砍掉的比分板 OCR，合计 45–110 MB | 已剔除 OCR，改为 v1 实际需要 **30–80 MB** |
| 6 | 第 4.2 | Windows 体积仍写 250–450 MB | 已按 D5 与模型削减修正为 **200–400 MB** |
| 7 | 第 4.3 | 只有 60 分钟口径 | 已新增 **B. 业余单局 10 分钟口径**，并单列"到可审核为止"一行 |
| 8 | 第 9.2 | 路线 C（FCPXML/EDL）仍写"做" | 已按第 12.6 节降级为**二期可选** |
| 9 | 第 9.3 | 标题与结论仍以"导出工程文件给 PR"为主线，且缺竖屏模板 | 已重写为"唯一路径 + 竖屏/方形模板"，并加入与 D7 一致的磁盘策略 |
| 10 | 第 9.3 | 文件名示例强制含比分，与"v1 记分可关"矛盾 | 已改为比分可缺省的命名规则 `R012_24shots_18.4s.mp4` |
| 11 | 第 6.5.1 | 写"烧入比分…默认开" | 已改为"比分项仅在启用记分时可用" |
| 12 | 第 11 节 | 浏览器方案已排除，但文中仍称"最优形态是本地后台 + Web UI" | 已在节首显式标注**作废**，并说明仅保留"模型放服务器分发"这一条 |
| 13 | 全局 | "非回合分"一词歧义（"作废的回合" vs "在非回合中产生的分数"） | 已统一改为 **`非回合`**，并在 9.4.3 显式定义语义 |
| 14 | 第 15.1 | D1 的影响仍写"压到 1–1.5 周" | 已与 M4 的 1.5–2 周对齐 |
| 15 | 第 2.2 ④/⑤ | 音频被列为"强证据/第二证据链"，未考虑音画偏移 | 已新增 **④-补二**：音频降为可选增强；证据权重表与第 2.3 节同步重写（D9） |
| 16 | 第 2.2 ④ | 回合起点只写"最后一个击球事件 −1.2 s"，未定义发球窗口期 | 已新增 **④-补**：定义四个时间锚点（静止期结束/发球准备/发球时刻/死球），起点改为"发球时刻 −1.5–2.0 s"且受 `quiet_end` 下界保护 |
| 17 | 第 2.2② | 球员身份归属 + 站位热图作为功能，与"砍掉跨场次统计"及"双打不判空间"矛盾 | 已收敛姿态职责为两个轻量用途（活动量检测、击球位置校验） |
| 18 | 第 3.3.4 / 9.3 | 导出分辨率选项缺 4K；烧字默认含比分，与 D11 矛盾 | 已补 4K 与方形模板；烧字改为"拍数/时长/序号默认开，比分默认关" |
| 19 | 第 12.4 | 把"终局比分全局校验"称为"本场景最大红利/核心功能"，与 D11 矛盾 | 已降级为可选辅助，并新增两个不需用户输入的主校验（静止期结构、发球节奏） |
| 20 | 第 16.5 | 写"❌ 双打的空间判定"，易被误读为"不支持双打" | 已明确：**双打要支持**，只是不做空间判定 |
| 21 | 第 15.2 / 16.8 | 两处待确认清单内容不一致、重复 | 已收敛到第 15.2 节，16.8 只保留分级说明 |
| 22 | 第 4.1 | 只列了 TrackNetV3，未给出"两遍处理"该用哪个模型；且 4K 已为主场景 | 已改为"V2/V4 粗筛 + V3/InpaintNet 精算"，合计修正为 40–95 MB |

### 16.2 开工前必须先做的预研（M-1，本方案此前最大的遗漏）

**风险最高的不是 UI，也不是架构，而是"业余素材上到底能做到什么程度"。在写产品代码之前，必须先用 1–2 周做纯算法验证**，否则可能开发到一半才发现基础假设不成立。

| # | 预研项 | 方法 | **通过标准（Go）** | **不通过时的降级路径（No-Go）** |
|---|---|---|---|---|
| P1 | **TrackNet 在真实素材上的球轨迹召回** | 取 10 段真实 10 分钟素材（含 4K60 与双打），人工标注球的可见性，跑 TrackNetV2/V4（第一遍 720p）与 V3+Inpaint（第二遍候选段/ROI），统计逐帧召回与最长断裂 | **60fps 下：逐帧召回 ≥90%，最长断裂 <0.5 s**（30fps 素材降到 ≥85% / <0.8 s） | ① 加大平滑窗口 + 提高静止期权重，接受精度下调；② 若仍不达标 → **"自动切分"核心价值不成立，产品退化为"手动切片 + 播放列表工具"，需重新评估是否值得做**（这正是决策门的意义） |
| P2（**增强项，不阻塞**） | **击球音可信度**（为将来的"音频增强档"做准备） | 同样 10 段素材，做 4 kHz 带通 + 峰值检测，并与人工标注的击球点比对（**须先补偿音画偏移**） | 击球音召回 **≥85%**，误报 **<20%** | 直接放弃音频增强档，永久走"纯轨迹 + 静止期"（v1 默认状态），无负面影响 |
| P3（**降级路径验证**） | **半场标定可行性** | 在"只拍到半场"的素材上跑半场标定，看重投影误差与外推可靠性 | 重投影误差 **<3% 场地宽度** | 该片段放弃空间判定，只做切分与三态确认（**不影响主场景**：三脚架通常能拍到全场） |
| P4 | **手机端温控与时延** | 中端真机上跑 10 分钟 60fps 素材的 720p 粗筛，测耗时与温度 | 单局 **≤12 min**（60fps 帧数翻倍，比 30fps 慢约 1.5–2×）、不降频到不可用、不崩 | 手机端只保留"拍摄 + 审核 + 分享"，分析全部引导到 Windows |
| P5 | **两遍处理的净收益** | 对比"单遍 V2/V4"与"V2/V4 粗筛 + 仅事件窗口跑 V3"的精度与耗时 | 精度有提升且耗时仍可接受 | 退回单遍 V2/V4，接受精度损失（官方数据集含业余素材，单遍可能已够用 —— 见第 17.6 节） |
| P6 | **业余素材的失败模式普查** | 收集 20 段真实素材（含各种糟糕条件），统计失败原因分布 | 得到一张"失败模式 → 应对措施"的对照表 | ——（此项本身即产出，用于定 R 列表与拍摄引导文案） |
| **A1** 🆕 | **第一遍真实吞吐**（第 17.3 T8） | 目标硬件跑 720p 的 V2/V4，测稳定吞吐（含解码落盘） | ≥60 FPS（10 min 视频第一遍 ≤10 min） | 加大跳帧倍率，并在 UI 明示粗筛档精度 |
| **A2** 🆕 | **事件窗口方案是否够用**（第 17.2） | 对比"只精算事件窗口 ±0.2 s"与"精算整个回合"的边界误差 | 边界误差仍 ≤0.3 s | 窗口放宽到 ±0.4 s，或把边界误差目标降到 ≤0.5 s |
| **A5**（原误编号为 A3） | **ROI 消融实验**（第 17.1 T2） | 对比整幅 512×288 / 半场裁剪 / 球心 640×640 ROI 的精度 | ROI **不低于**整幅 | **彻底放弃 ROI**，并删除"4K 提精度"这一卖点 |
| **A6** 🆕 | **4K60 硬件解码可用性**（D14 新增，见第 18 节） | 在目标机器上探测 NVDEC/QSV/AMF 与手机 MediaCodec 能否**实时硬解该 4K60 源**，测 CPU 占用与内存峰值 | 硬解可用且**不触发软解回退**；否则必须阻断并提示 | **强制降级**：导入时先转 1080p/720p 代理再分析，UI 明示"本机不支持 4K60 硬解，已转代理" |
| **A4** 🆕 | **静止期判据加权方案**（第 17.3 T5） | 按"球场帧差 + 球不可见为主判据、球员活动为辅"重做静止期检测，与人工标注比对 | 静止期检出 **召回 ≥95%**（切分第一道锚） | 静止期阈值加长到 3.5 s，接受略微多切 |

> **决策门（第 17 节复核后最终版）**：**唯一硬门是 P1（轨迹召回）**，目标 **60fps 下召回 ≥90%、最长断裂 <0.5 s**。
> - **A1 与 A2 必须完成** —— 它们直接决定时间预算与档位设计是否成立（第 17.2 节）；不通过时按各自的降级路径调整，**不影响是否开工**；
> - P2（音频）是增强项、P3（半场标定）是降级路径验证、P4 不影响 Windows 主线；**A5（ROI）可选**，不通过只是砍掉一个卖点。
>
> **若 P1 不通过，则不应进入完整开发** —— "自动切分"的核心价值不成立，产品会退化成"手动切片 + 播放列表工具"。
> **执行顺序优化（第 17.6 节）**：官方 Shuttlecock Trajectory Dataset 自带 `Amateur` 素材，因此 **A1/A4/A5 可以先开跑，不必等用户提供基准素材**；只有 P1 的最终判定需要用户素材。**D14 后新增前置：先跑 A6（4K60 硬解探测），再跑 P1。**

### 16.3 术语表（缺失，必须补，否则沟通会持续歧义）

| 术语 | 定义 |
|---|---|
| **单局（game）** | 一个视频文件对应的完整一局比赛，本场景 6–10 分钟 |
| **回合（rally）** | 从**发球准备**开始到**死球**（落地/出界/下网）结束的一段连续对抗 |
| **非回合（non-rally）** | 视频中不属于任何回合的片段：捡球、擦汗、试挥、走动、局间休息、重发、擦网重打。**不计分、不换发球权**（D1/D8 已确认语义） |
| **拍（stroke）** | 一方的一次击球。回合拍数 = 双方击球次数之和（发球算第 1 拍） |
| **静止期（quiet period）** | 双方停止明显移动、连续 ≥2.5 s 无球无击球的时段 —— **回合间的物理边界**（第 2.2 ④-补） |
| **发球窗口期** | 从"发球准备动作开始"到"球离开拍面"的时间段（业余通常 1.0–2.5 s）；**回合起点须覆盖它** |
| **得分方** | 该回合的胜者（A / B）；由用户三态点选 |
| **发球方（serve side）** | 该回合的发球一方；由上一回合胜者决定（每球得分制）。**v1 不做视觉识别**，由规则推导 |
| **终局比分** | 该局结束时的比分，如 21-18；**可选输入**，用于辅助校验（比分非核心，见 D8） |
| **粗切片** | 仅记录时间索引、不产生视频文件的操作（第 3.3 节） |
| **代理（proxy）** | 为提升预览流畅度而生成的**一支**低码率全片副本，非必需品 |
| **导出** | 唯一真正编码的环节，按设置把选中回合渲染成 MP4 |
| **置信度（confidence）** | 算法对该判定（回合边界、击球、落地）的自评；**不是正确率承诺**，仅用于排序与提示 |

### 16.4 验收测试集与量化指标（此前只有零散指标，未成体系）

**T1. 基准测试集**：**20 段真实业余单局视频**（6–10 分钟），须覆盖：

| 维度 | 覆盖要求 |
|---|---|
| 机位 | **三脚架为主**（用户实际条件）；含 1–2 段半场取景用于验证标定降级路径 |
| **帧率** | **60fps 为主（已确认，D12）**；另含 1–2 段 30fps 用于验证降级路径（不作为主场景） |
| **分辨率** | **全部 4K60（D14）**；建议额外准备 2–3 段 1080p/720p，仅用于验证"硬解不可用 → 转代理"的降级路径 |
| 光照 | 白天自然光 / 夜间灯光 / 逆光 |
| **项目类型** | **单打与双打各半**（用户两者都打；双打只验证"切分与拍数不崩"，不验证空间判定） |
| 失败模式 | 变焦、竖屏、球出画、换球中断 各至少 1 例 |

**T2. 量化指标（双端一致，因为共享核心）**

| 类别 | 指标 | 目标 |
|---|---|---|
| 回合切分 | 召回率 | **≥92%**（60fps + 三脚架条件下恢复原目标；30fps 素材可下调至 ≥85%） |
| | 精确率 | **≥88%** |
| | **边界误差** | 起止点与人工标注误差 **≤0.3 s**（60fps 下可比 30fps 收紧；直接决定观感） |
| | **发球窗口期覆盖** 🆕 | 人工标注的"发球准备动作起点"落在回合片段内的比例 **≥95%**（即不把发球动作切掉） |
| 拍数 | 拍数误差 | ±1 拍以内 ≥85% 的回合 |
| 计分 | 三态标注自洽性 | 若用户填写终局比分，则自动切分结果与之一致的比例 ≥70%（人工修正后 100%） |
| **成片（D13）** 🆕 | **垃圾时间压缩率** | 10 min 素材，标准档成片 **≤7.5 min**（压缩掉 ≥25% 时长） |
| | **成片音频连续性** | 所有拼接点**无可听爆音/咔嗒声**（波形检查 + 人工试听） |
| | **成片边界完整性** | 无回合被"切头去尾"——发球动作与落地反应均完整保留（人工抽检 20 个拼接点） |
| | **精选筛选正确率** | 预设②输出的回合集合与用户标记的"精彩球"**完全一致**（精确匹配，无遗漏无多余） |
| 性能 | 到"可审核"耗时（10 min 素材） | Windows ≤3 min；纯 CPU ≤12 min；手机 720p ≤10 min |
| | 导出耗时（12 个片段，1080p） | ≤60 s（GPU）；**4K60 另计，且必须硬件编码** |
| 交互 | 40 个回合三态标注 | Windows ≤3 min；手机 ≤8 min |
| 稳定性 | 连续处理 | 无崩溃、无内存增长（10 段连跑） |
| 资源 | 内存峰值 | Windows <2.5 GB；手机 <1.2 GB |

**T3. 质量门（Quality Gate）**：每个里程碑结束跑一次 T1 全集，任一指标跌破目标 **10% 以上**即暂停新功能开发，先修回归。

### 16.5 明确不做（v1 反范围蔓延清单）

> 这一节的目的不是限制想象，而是**保护 MVP 能交付**。以下功能在 v1 明确不做，若要加必须走需求变更：

- ❌ **变焦/推拉镜头素材**（业余常见）—— 轨迹会因焦距变化而断裂，v1 不做补偿，只在质量体检时提示
- ❌ **慢动作素材**（120/240fps 录制）—— 时间轴语义不同，需单独处理
- ❌ **多机位 / 双视角同步**
- ❌ **双打的空间判定**（球员归属、责任区、站位分析）—— ⚠️ 注意：**双打本身要支持**（用户既打单打也打双打），但只做"切分 + 拍数 + 三态确认"，**不做任何依赖空间判定的功能**
- ❌ **专业比赛转播源**（比分板 OCR、镜头切换处理）
- ❌ **拍型识别**（吊/杀/高远/网前）与战术统计
- ❌ **跨场次统计**（胜率趋势、站位热图、进步曲线）
- ❌ **批量队列**（一次处理多场）
- ❌ **云同步 / 账号体系 / 跨设备工程同步**
- ❌ **音轨编辑**（降噪、分离人声）
- ❌ **实时分析**（边拍边算）—— 延迟与功耗都不现实
- ❌ **"精彩度"自动评分**（需要专门模型与标注数据）
- ❌ **音频作为主判据**（音画偏移风险，见第 2.2 ④-补二）

### 16.6 数据管理、隐私与"删除"语义（缺失）

- **视频指纹复用**：以"路径 + 文件大小 + 首尾各若干帧的哈希"做指纹。用户**移动/重命名原片后**，程序能重新关联项目，不必重跑分析（避免"白干一场"这种最伤体验的问题）。
- **删除项目的语义（必须明确定义并告知用户）**：删除项目 = 删除**数据库记录 + 代理视频 + 缩略图 + 中间结果**；**绝不删除原始视频**。UI 上把这一点写清楚。
- **隐私**：全程本地、不上传；模型权重从服务器分发时**只下载，不回传任何素材信息**；日志与崩溃报告默认本地留存，不做自动上传（如需上报须显式征得同意）。
- **磁盘守卫**：导出前检查剩余空间并给出预估；空间不足时提示且**不开始导出**，不做"写一半失败"。
- **路径健壮性**：跨盘符、长路径、中文/emoji 路径、UNC 网络路径都要能处理（Windows 260 字符限制是常见坑）。
- **数据模型补全**：第 3.2 节的表结构中**缺少 `project` 表**（一个项目 = 一个视频 + 一条分析记录 + N 个回合 + M 个播放列表），需补上；同时明确 `playlist` 的项目归属外键，避免多项目时串数据。

### 16.7 未覆盖的工程点（补充）

| 点 | 说明 |
|---|---|
| **崩溃恢复** | 分析中途崩溃后，重启能识别"未完成的项目"并从中断点继续（配合 16.6 的中间结果持久化） |
| **编码器不可用的兜底** | 硬件编码器初始化失败（驱动问题很常见）→ 自动回退软编并提示耗时变化，不能直接报错退出 |
| **色彩空间/范围** | BT.601/709、limited/full range 处理不当会导致导出画面发灰或过饱和。分析链路与导出链路需统一约定（建议全程 `yuv420p` + BT.709 limited，导入时做转换） |
| **旋转元数据（rotation）** | 手机竖拍视频常带旋转 flag，FFmpeg 不一定自动应用 → 必须在导入时按其解析出**有效宽高**，否则标定与坐标全错。竖屏场景下这是高概率踩坑项 |
| **文件名与非法字符** | 导出文件名需清洗 Windows/Android 非法字符、控制长度、避免重名覆盖 |
| **自动更新** | 业余工具装一次就不管了，**内置"检查更新"（可选、可关）** 是低成本高收益项 |
| **音画偏移（AV sync）** | 手机录制的音画偏移可达数十至数百毫秒。**已按 D9 规避**：v1 不使用音频，故不阻塞；但将来启用"音频增强档"时，**仍必须先估计并补偿全局偏移**（互相关估计 + 人工微调滑杆），否则音频证据会系统性失准且不报错 |
| **4K 解码与内存** | 4K60 解码缓冲显著大于 1080p（第 12.4 节）。第一遍必须**降采样到 720p 再进模型**，避免在 4K 分辨率上做全帧推理；同时限制解码线程数与窗口帧数，防止内存峰值失控 |
| **双打场景的球员数** | 双打 4 人的姿态检测耗时是单打的约 2 倍。由于姿态只用于"活动量粗判"，可在双打时**降低姿态推理频率**（如每 3 帧一次）来控制成本 |

### 16.8 待确认项汇总

> **完整清单已统一收敛到第 15.2 节**（避免两处清单不同步）。此处仅说明分级原则与当前状态：

| 级别 | 含义 | 当前状态 |
|---|---|---|
| **A 类（阻塞开工）** | 不答就无法定义验收或无法开始算法验证 | ✅ **A1/A2/A3 均已确认**（三态语义 / 接受 M-1 预研 / 素材来源）；⛔ **新增 A6（4K60 硬解可用性）为必做验证**，见第 18.1 节 |
| **B 类（影响精度/工作量）** | 可在 M0–M1 期间并行确认 | **全部已确认**（B1–B5 分别由 D10、D12、D11 回答） |
| **C 类（影响产品形态）** | 不阻塞算法开发 | 5 项待定（精彩球筛选 / 球员姓名 / 烧字内容 / 双端研发顺序 / 检查更新） |

### 16.9 本轮新增的已确认决策（D8–D11）

| # | 决策 | 影响 |
|---|---|---|
| **D8** | **`非回合`的语义确认 = "不计分、回合作废"（三态，非四态）** | 数据模型保持三态；规则引擎"非回合不推进比分、不换发球权"成立；**不需要**四态扩展（第 9.4.3 节） |
| **D9** | **音画偏移问题通过"以轨迹为主证据"规避；音频降为可选增强** | 新增第 2.2 ④-补二；音频-视觉对齐逻辑从 v1 移除；**M-1 决策门只看 P1 与 P3**；粗筛档可完全不解码音频 |
| **D10** | **素材条件**：**三脚架手机拍摄**为主，**单打与双打都有**，**分辨率 1080p–4K 都有**，**球可能出画** | ① 手持稳定化的复杂度**消除**（第 12.3 节的相关工作量可省）；② 静止期检测因固定画面而**更可靠**（第 2.2 ④-补，判据已按第 17.3 节 T5 改为加权）；③ **双打必须支持**，但只做切分/拍数/三态，**不做空间判定**；④ **1080p–4K 都有** → 第一遍统一 720p，第二遍默认整幅；⚠️ "4K-ROI 提精度"已由第 17 节降级为待验证的可选增强；⑤ 球出画是常态 → **"出画即标 UNKNOWN，不猜落点"** 的原则更重要 |
| **D11** | **比分非核心，甚至可以不显示** | 三态标注保留（它是切分质量的校验工具与训练数据来源），但 **UI 默认隐藏比分列**、**烧字默认不烧比分**；第 12.4 节的"终局比分全局校验"从"核心功能"降为**可选辅助**，主校验改为**静止期结构 + 发球节奏自洽性**（见下） |

**D11 的补偿说明（重要）**：去掉"终局比分"这个强约束后，需要一个替代的全局校验手段。建议用这两个**不需要用户输入**的机制：

1. **静止期结构校验**：一段视频内"静止期 → 回合 → 静止期"应严格交替。若出现"静止期缺失"（两个回合直接相连、中间没有静止期），大概率是**漏切/多切**，可高亮提示；
2. **发球节奏自洽性**：业余发球前几乎总有 ≥1.5 s 的静止期，且发球必然是该回合的第 1 拍。若某回合被判定为"无首拍"（直接从连续对拉开始），大概率是**起点定位错误**，可提示复核。

这两条都比"让用户填终局比分"更轻量，也覆盖了绝大部分漏切场景。

### 16.10 第二轮复核修正的问题

| # | 位置 | 问题 | 修正 |
|---|---|---|---|
| 23 | 第 3.1 | 主流程仍写"抽音频""⑤ 发球权与比分自洽性校验"，与 D9/D11 矛盾 | 已重写为"第一遍 720p 粗筛 → 第二遍候选段/4K-ROI 精算"，校验改为静止期结构 + 发球节奏；预处理中的音频步骤移除 |
| 24 | 第 2.2 ④ / 2.3 | 标题仍叫"三证据融合" | 已改为"轨迹为主 + 静止期锚点" / "判据总表" |
| 25 | 第 2.2 ③-补 | 写"宁可高召回 + 后置过滤（这正是三证据融合要解决的问题）" | 已改为指向"两遍处理" |
| 26 | 第 6 节 | 平台表仍用 60 分钟口径；Android 定位为"现场端"；称两端"靠工程包接力" | 已改为单局口径（1.5–3 min / 5–10 min）；Android 定位改为"720p 全流程闭环"；工程包降为**可选** |
| 27 | 第 6.5.1 | 导出项写"生成轻量工程包供传到电脑继续精修" | 已标为**可选功能**（第 10.5 节） |
| 28 | 第 6.5.3 | 仍列"比分板 OCR 交叉校验"为 Windows 独有能力 | 已标注**已砍掉**（D11） |
| 29 | 第 10.1 | 定位/耗时/体积/导出均为 60 分钟旧口径；导出列仍含 FCPXML/EDL | 已全部按单局与 D10/D11 更新（含 4K 档位、工程包"可选"标注） |
| 30 | 第 10.5 | 标题"双端如何配合"暗示工程包是必经流程 | 已改为"可选的工程包接力"，并画清"默认独立闭环 + 可选接力"两条路径 |
| 31 | 第 13 节 M4 | 仍写"终局比分输入与全局一致性校验" | 已改为"静止期结构校验 + 发球节奏自洽性"，比分列标为可选 |
| 32 | 第 4.3 | 时间预算写"解码 + 音频分析" | 已改为"解码 + 静止期检测"（v1 不解码音频） |
| 33 | 第 16.8 / 16.9 | 小节之间缺空行，导致表格与标题粘连 | 已修正格式，并补出本节 16.10 |
| 34 | 第 6.5.2 | 安卓硬约束仍按 60 min 口径（"1.5–3 h"）与"耗电 20–40%" | 已改为单局口径（5–10 min、耗电 10–25%） |
| 35 | 第 8.2 | "服务器能做的轻活"里仍列比分板 OCR 高分辨率 ROI | 已标注 OCR 已砍掉（D11），保留其为纯历史记录 |
| 36 | 第 12.6 / 13 节 | "砍掉 4K 两级 ROI"的措辞会让读者以为该能力被删除 | 已澄清：**4K 两级 ROI 保留**（D10），只是不再作为"全片流程"，仅用于第二遍候选段 |
| 37 | 第 15.1 D5 | 仍写"新增终局比分全局校验为核心功能" | 已标注该结论被 D11 取代 |

| D14 | **素材规格 = 全部 4K60；开发前提供典型素材** | ✅ 已定 | A3 解除；**新增 A6（4K60 硬解验证，最高优先级环境风险）**；硬解成为必需、代理成为默认、导出依赖硬件编码；内存峰值上修；**第一遍策略不变**（见第 18 节） |

### 16.11 第三轮复核：D12（60fps）的传导修正

| # | 位置 | 修正 |
|---|---|---|
| 38 | 第 0 节 / 第 1 节 / 第 15.1 | 帧率从"待确认"改为 **60fps 已确认（D12）**，并加入"素材与规格"行；删去"建议补充确认帧率"的表述 |
| 39 | 第 2.1 | 原文写"30fps 下杀球两帧间飞过半个场地"，与 60fps 事实不符。已改为量化对比：**60fps 下杀球帧间位移约 1.4–2.3 m（可关联）；30fps 下翻倍到 2.8–4.5 m（会退化）** |
| 40 | **第 4.3 节（新增 C 小节）** | 发现并处理了一个**此前被忽略的连带影响**：**60fps 使帧数翻倍、算力成本上升约 1.5–2×**。已补三条提速手段：**① 第一遍跳帧（60→等效 30fps，因帧间位移更小而几乎无损）；② 第二遍只跑候选段 ±2 s；③ 富余帧用于亚帧插值，把边界误差做到优于 1 帧** |
| 41 | 第 12.2 / 12.5 | 时间预算与平台表按 60fps 重算（Android 720p 粗筛由 5–10 min 调整为 **7–15 min**，P4 的通过标准由 ≤10 min 放宽到 **≤12 min**） |
| 42 | 第 12.3 | 帧率行从"常见 30fps、需降级"改为"**60fps 已满足，无需降级**"；并新增一条正面用法：**富余帧数用于轨迹平滑与亚帧定位** |
| 43 | 第 12.8 / R1 | 头号风险从"帧率/抖动"改写为"**光线不足 → 自动快门变慢 → 糊球**"；"单局漏检 2–5 个回合"的悲观预期修正为 **1–3 个**（60fps 条件下） |
| 44 | 第 13 节 M2/M3、第 16.4 T2 | 验收目标全面上调并标注适用条件：**召回 ≥92% / 精确 ≥88% / 边界误差 ≤0.3 s**（60fps + 三脚架）；30fps 或半场取景时按 ≥85% / ≥80% 验收 |
| 45 | 第 16.2 P1–P4 | P1 目标上调为 **召回 ≥90%、最长断裂 <0.5 s**；P2 明确为"增强项，不阻塞"；P3 明确为"降级路径验证"；P4 放宽到 ≤12 min。**决策门收敛为"唯一硬门 = P1"** |
| 46 | 第 16.4 T1 | 测试集帧率从"30/60 各半"改为 **60fps 为主 + 1–2 段 30fps 用于验证降级路径** |
| 47 | 第 15.2 B 类 | B1–B5 **全部已确认**（分别由 D10、D12、D11 回答），B 类清空 |

> **最终复核结论**：全案 16 节 + 附录，累计 **47 处**不一致已全部修正。
> **当前状态**：技术决策已闭环至 **D14**；**A3（基准素材来源）已解除**（D14：开发前提供全 4K60 素材），**新增 A6（4K60 硬解验证）为必做前置**。
> **可以开工**：M-1 预研（1–2 周，纯算法，**唯一硬门 = P1 轨迹召回 ≥90%**）→ M0 需求冻结 → M1 骨架。
> **本轮最重要的发现**：60fps 不是纯利好 —— 它解除了精度上限，但把算力成本推高了约一倍，因此**"第一遍跳帧"从可选优化变成了必需手段**。这一条已写入第 4.3 节 C，是 D12 带来的最大工程影响。

### 16.12 因 D13（切片用途澄清）引起的调整

> 用户明确切片分析的三项用途后，**导出的主次关系被纠正**，并新增了一条低风险保底路径。

| # | 位置 | 调整 |
|---|---|---|
| 48 | **第 9.3 节** | **主产物从"片段包"改为"成片"**。原"每回合一个 MP4 + 可选合并长片"属主次颠倒：用户要的是一条成片，不是 40 个文件。已改为 P0 主产物 = 紧凑成片，P0 辅产物 = 单回合片段 |
| 49 | **新增第 9.5 节** | 三个导出预设（单球 / 精彩集锦 / 垃圾时间压缩）+ **关键结论：三者为同一个"成片生成器"的三个筛选条件，不是三个功能** |
| 50 | **新增第 9.6 节** | 垃圾时间压缩的四个工程要点：① 回合间垃圾可切、**回合内准备不可切**；② **拼接点必须做 80–150 ms 音频交叉淡化**（否则有爆音，成本极低但极易被忽略）；③ 压缩力度三档（保守/标准/激进）；④ **白送的降级路径** —— 压缩只依赖静止期检测，不依赖回合切分 |
| 51 | **新增第 9.7 节** | 连带调整：`is_highlight` 字段、时间线视图升 P0、"精彩球"标记升 P0、暂存列表定位改为"集锦候选池"、取消模糊的"合并长片"概念 |
| 52 | 第 3.1 / 3.2 | 主流程导出步骤改为三预设；数据模型新增 `is_highlight` 与 `quiet_before_ms`/`quiet_after_ms` |
| 53 | 第 5 节 | UI 从四个主界面改为**五个**，新增"**时间线页**"（可视化垃圾时间），并把"连播中一键标精彩球（`H` 键）"写成硬要求 |
| 54 | 第 13 节 M5 | 里程碑从"导出与分享"改为"**成片生成器**"，验收标准改为"1 条集锦成片 ≤10 min 全流程 + 拼接点无可听爆音" |
| 55 | 第 16.4 T2 | 新增四项成片指标：**压缩率、音频连续性、边界完整性、精选筛选正确率** |
| 56 | 第 4.3 C | 补上跳帧的**例外**：横向高速杀球段不能跳帧（与第 17.3 节 T4 呼应） |

> **D13 带来的最有价值的一条**：**"垃圾时间压缩"这个用途只依赖"静止期检测"，不依赖"回合切分是否准确"。** 静止期检测是整条链路里最可靠的一环（球不可见 + 球场帧差低），而回合切分是最难的。这意味着**即使 M-1 预研的 P1（轨迹召回）不达标，产品仍然有可交付的价值** —— 可以降级为"紧凑模式"：只压掉静止段，不做回合划分。**这显著降低了整个项目的失败风险**，建议把这条写成明确的产品降级策略。

### 16.13 因 D14（全 4K60）引起的调整

> 完整复核见 **第 18 节**。此处仅列修正条目。

| # | 位置 | 调整 |
|---|---|---|
| 57 | **新增第 18 节** | 素材条件确定后的整体复核：4K60 的逐项传导（C1–C11）、硬解强制性、第一遍策略不变的论证 |
| 58 | **A3 解除** | 素材来源已确认（开发前提供），从"阻塞项"移出；**但新增 A6：4K60 硬件解码可用性验证** —— **当前最高优先级的环境风险** |
| 59 | **修复编号冲突** 🐞 | 本节新写入时发现：**"素材 A3"与"ROI 消融 A3"重号**（第 16.2 节 vs 第 17.4 节）。已将 ROI 消融改为 **A5**，硬解验证为 **A6** |
| 60 | 第 2.2 ③-补二 | "4K 必须走两级 ROI" → 明确**第一遍必须降到 720p 再缩放**（4K 直接缩 512×288 是灾难）；同时 4K-ROI 仍标注为待验证（A5） |
| 61 | 第 4.3 节 B | 内存峰值上修：Windows **1.5–3 GB → 2.5–4 GB**；Android **700 MB–1.2 GB → 1.2–1.8 GB** |
| 62 | 第 3.3.3 节 | 预览代理从"可选优化"改为 **4K60 场景的默认项**（10 min 4K60 源 2–8 GB，逐段 seek 压力大） |
| 63 | 第 3.3.4 / 9.3 节 | 导出编码明确：**4K60 必须硬件编码**，无硬件时警告（软编 4K60 约 0.1–0.3× 实时） |
| 64 | 第 16.4 T1 | 测试集改为**全部 4K60**，另加 2–3 段低分辨率用于验证代理降级路径 |
| 65 | 第 12.3 节 | 拍摄建议中"1080p 足够"作废 —— 4K60 是既定条件，改为提示"文件较大，预留空间" |
| 66 | 第 15.1 / 15.2 | 新增 D14；A3 标记解除；新增 A6 |
| 67 | 第 16.7 节 | "4K 解码与内存"从补充工程点升级为**主要风险项** |

> **整体复核结论（D14 后）**：
> - **架构不变** —— 第一遍 720p 粗筛、两遍处理、静止期锚点、三态确认、成片生成器，全部维持。理由见第 18.3 节：**4K 降采样后球的信息量与 1080p 基本等价，4K 的价值在导出画质而非第一遍精度。**
> - **变化的三件事**：① **硬解成为强制性前置条件**（新增 A6，最高优先级环境风险）；② **代理生成成为默认**；③ **导出依赖硬件编码**。
> - **待确认项**：C 类 5 项（精彩球筛选 / 球员姓名 / 烧字内容 / 双端研发顺序 / 检查更新）+ 素材的**数量与条件覆盖**需确认（单打/双打比例、光线条件、是否含球出画）。
> - **开工第一件事改了**：拿到素材后**先跑 A6（硬解探测）**，再跑 P1（轨迹召回）。因为硬解不可用会改变整个时间预算的前提，P1 的测试环境也要相应调整（须用代理模式测）。

---

## 17. 可行性与技术复核（独立复核，第三轮）

> 本节不再检查文字一致性（第 16.10/16.11 节已完成），而是**逐条压力测试技术假设**：算清代价、找反例、验证模型适用边界。**复核发现 1 个数量级错误、6 处需要修正的假设、3 项必须新增的验证。**

### 17.1 复核中查出的技术错误

| # | 问题 | 原表述 | 复核后结论 | 影响 |
|---|---|---|---|---|
| **T1** | **第二遍处理（V3/InpaintNet + 4K-ROI）的耗时被低估一个数量级** | "第二遍精算 20–60 s（GPU）" | ❌ **错误**。按 V3 官方性能表 **25.11 FPS**（≈40 ms/帧）实算：一个 12 s 回合 = 720 帧 → **约 29 s/回合**；30 个回合 → **约 14 分钟（GPU）**，而非 20–60 s。差约 **一个数量级** | **必须改设计**：第二遍**不能覆盖整个候选回合**，只能覆盖**事件附近的短窗口**（见 17.2 的修正方案）。否则 Windows 端单局分析从"1.5–3 min"恶化到"15–20 min"，直接影响核心体验目标 |
| **T2** | **ROI 裁剪的尺度与模型训练分布不一致** | 隐含假设"把 4K 裁成 640×640 ROI 喂给 V3 就能提精度" | ⚠️ **未经验证且很可能有害**。TrackNet 训练分布是"**整幅 512×288 球场画面**，球约 2.5 px"。裁成 640×640 球心 ROI 后，球仍约 2–3 px，但**背景上下文完全变了**（整片球场 → 局部特写），属于分布外输入，**精度可能不升反降** | 必须做 ROI 消融实验（17.4 A5）。**在验证通过前，不应把"4K ROI 提精度"写成确定收益**，只能写成"待验证的候选手段" |
| **T3** | **杀球速度取值偏大** | "杀球 300–490 km/h → 60fps 帧间位移 1.4–2.3 m" | ⚠️ 490 km/h 是**极值纪录**，不是典型值。典型杀球 **200–300 km/h** → 60fps 帧间位移 **0.9–1.4 m**；且球**朝向/远离摄像机方向飞行时，图像平面位移远小于此** | 风险比原判断更小，但其**方向依赖**必须写进设计（见 T4） |

### 17.2 因 T1 而必须修正的设计：第二遍到底跑什么

**修正前（错误）**：第二遍 = 对候选回合 → 回原始分辨率 → 全程 V3 + InpaintNet。
**修正后（按代价实算）**：第二遍 = **只对"事件窗口"跑 V3**，而不是对整个回合。

```
第一遍（V2/V4，720p，可跳帧）——全片
  作用：给出轨迹 + 静止期 + **候选事件列表**（每次方向突变、每次落地候选、发球起点）
  开销：36,000 帧 ÷ 160 FPS ≈ 4 min（GPU，按 V4 论文口径）或 ≈ 21 min（按官方 27.7 FPS 口径）
        → 两条口径都需实测确认（17.4 A1）

第二遍（V3 + InpaintNet）——只跑事件窗口
  窗口 = 每个事件 ±0.2 s = 24 帧（匹配 V3 的 3-in/3-out 时序结构 + 上下文）
  假设 30 回合 × 10 个事件 = 300 个窗口 → 7,200 帧 ÷ 25 FPS ≈ **4.8 min（GPU）**
  若收紧到"只精算落地与发球两类关键事件"（约 60 个窗口）→ 1,440 帧 ≈ **1 min（GPU）**
```

**结论与建议**：

1. **第二遍的目标从"提高全回合精度"改为"精确锁定两类关键事件：落地时刻、发球起点"** —— 这两个才是决定回合边界与拍的。
2. **"4K-ROI 精算"降级为可选增强**（第 17.4 A5 验证通过后才启用）；**默认第二遍在 720p/1080p 的整幅上跑 V3**，避免 ROI 分布外风险（T2）。
3. **Windows 端时间预算修正为**：到"可审核"为止 **≈ 3–6 min（GPU）/ 15–30 min（纯 CPU）**；精算档可选开启，开启后 **≈ 5–10 min（GPU）**。
4. **Android 端明确不跑 V3**：25 FPS 的 V3 在手机上意味着 36,000 帧要 24 min 以上，**手机端只跑第一遍（V2/V4 720p）**，4K/精算交 Windows。这一条同时让"手机端 7–15 min"的预算成立（因为不含 V3）。

### 17.3 其余需要修正的技术假设

| # | 假设 | 问题 | 修正 |
|---|---|---|---|
| **T4** | 60fps 下杀球可关联 | 球**朝摄像机方向飞**时图像平面位移很小（反而更容易），但**横向杀球**位移最大。原表述只给了平均概念 | 设计上应**按"图像平面位移"而非物理速度**来定阈值与平滑窗口；对横向杀球单独检查轨迹关联性（第一遍的跳帧策略对横向杀球风险最大 —— 跳帧会让位移翻倍，**所以跳帧只应用于"非高速段"**，高速段保持全帧率） |
| **T5** | 静止期三条件并列（球不可见 + 球员低活动 + 画面帧差低） | **"球员低活动"不是可靠条件**：单打中球员一直在小步调整，双打中 4 人同时在动。若把它当必要条件，真实静止期会被大量漏判 | 改为**加权而非并列**：**主判据 = 球不可见/近乎静止 + 球场区域帧间差分低**；**球员活动量只作弱提示**（用于消歧，不作硬门槛）。固定机位下帧间差分是本场景最可靠的信号 |
| **T6** | 用修复后的轨迹判死球 | InpaintNet 会**修补漏检段**，修补出来的点**不代表真实可见**。若直接拿修复后轨迹算"球何时停下"，会把"看不见"误判成"球在动"或反之 | 必须**分别保留原始 `visibility` 与修复后坐标**：判死球/回合结束用**原始可见性**；轨迹平滑/拍数补齐用**修复后坐标**。数据模型需增加 `visibility_raw` 字段（第 3.2 节） |
| **T7** | 三态标注的粒度 | 未考虑**连续 NON_RALLY**（捡球 + 试挥 + 换球连成一段），会在列表里产生大量无用卡片 | UI 必须把**连续的 NON_RALLY 合并为一条**；并提供"整段标记为非回合"的批量操作；非回合默认**折叠不显示**（可展开） |
| **T8** | 第一遍用 V2/V4 的 FPS | 官方表 V2 = 27.7 FPS（2019 硬件/整脚本口径），V4 论文 V2 = 160+ FPS（口径不可比） | 两个口径差 6 倍，**不能直接采用任一数字做排期**。必须实测（17.4 A1）：在目标硬件上跑 720p 的真实吞吐，并据此决定"是否跳帧、跳几帧" |
| **T9** | 手机端处理 4K60 | 未说明 4K60 在手机上的解码代价 | MediaCodec 可硬解 4K60，但**必须直接输出 720p 供推理**（不要先解成全分辨率再缩放），并**分块处理**（如每 1–2 分钟落盘一次中间结果），避免长时任务被系统回收后全部重来 |

### 17.4 复核新增的验证项（并入 M-1 预研）

| # | 验证项 | 方法 | 通过标准 | 不通过时 |
|---|---|---|---|---|
| **A1** | **第一遍真实吞吐**（T8） | 目标硬件上跑 720p 的 V2/V4，测稳定吞吐（含解码与落盘） | ≥60 FPS（10 min 视频 ≤10 min 跑完第一遍） | 加大跳帧倍率（每 2 帧取 1 帧），并在 UI 明示粗筛档精度 |
| **A2** | **事件窗口方案是否够用**（T1 的修正是否成立） | 对比"只精算事件窗口"与"精算整个回合"的回合边界误差 | 事件窗口方案下 **边界误差仍 ≤0.3 s** | 放宽窗口到 ±0.4 s（代价约翻倍），或放宽边界误差目标到 ≤0.5 s |
| **A5** | **ROI 消融实验**（T2） | 同一素材上对比三种输入：整幅 512×288 / 半场裁剪 / 球心 640×640 ROI，比精度 | ROI 方案精度**不低于**整幅方案 | **彻底放弃 ROI**，第二遍一律整幅推理，"4K 提精度"这一卖点删除 |
| **A6** 🆕 | **4K60 硬件解码可用性**（D14） | 目标机器上探测 NVDEC/QSV/AMF / MediaCodec 能否实时硬解该源；测 CPU 占用与内存峰值 | 硬解可用且不触发软解回退 | 强制转 1080p/720p 代理后再分析，UI 明示 |
| **A4** | **静止期判据的加权方案**（T5） | 按"帧差 + 球不可见为主、球员活动为辅"重做静止期检测，与人工标注比对 | 静止期检出 **召回 ≥95%**（这是切分的第一道锚，必须高） | 加长静止期阈值到 3.5 s（更保守），接受略微多切 |

> **决策门更新**：M-1 的**硬门仍是 P1（轨迹召回）**；**A5（ROI）不通过不影响开工**，只是砍掉一个可选增强项。但 **A1、A2、A4、A6 必须做**，因为它们直接决定时间预算、档位设计与环境前提是否成立。

### 17.5 可行性结论（复核后）

| 维度 | 结论 |
|---|---|
| **技术上是否可行** | ✅ **可行**。核心链路（第一遍轻量粗筛 → 静止期锚点 → 关键事件精算 → 状态机 → 时间索引）在 60fps + 三脚架条件下成立，且**难度比原判断更低**（帧率与抖动两大杀手已被素材条件消除） |
| **性能预算是否成立** | ⚠️ **修正后成立**：Windows 到"可审核" **3–6 min（GPU）**；Android 只跑第一遍 **7–15 min**。**原"1.5–3 min"的乐观数字已作废**（那是把第二遍代价估错一个数量级得出的） |
| **精度目标是否可达** | ⚠️ **召回 ≥92% 是乐观目标**，需 P1 实测确认；**ROI 提精度这一条未经证实，已降级为可选** |
| **最大剩余风险** | ① **光线不足导致糊球**（R1，唯一无法靠素材条件规避的）；② **横向高速杀球的轨迹断裂**（T4，靠"高速段不跳帧"缓解）；③ **静止期在双打中的可靠性**（T5/A4） |
| **是否建议开工** | ✅ **建议**。但**必须先做 M-1 预研（1–2 周）**：**P1 + A1 + A2 + A4 + A6 必做，A5 可选** |

### 17.6 本次复核带出的一个正面发现

**官方数据集本身包含业余比赛素材**：Shuttlecock Trajectory Dataset 的 `Amateur` 目录（match24–match26）已被纳入 TrackNetV3 官方训练集与测试划分。这意味着：

- **预训练权重见过业余画质**，不必从零微调，冷启动风险显著低于预期；
- 可以在 M-1 预研中**直接用官方 `Amateur` 片段做第一轮验证**，不必等用户提供素材 —— **这让 A1/A4/A5 可以在拿到用户素材之前就开跑**，把 A5（ROI 消融）的等待时间省掉。

> 这条修改了第 16.2 节的执行顺序：**M-1 可先用官方业余片段跑通 A1/A3，再用用户素材做 P1 的最终判定。**

---

## 18. 素材条件确定后的整体复核（D14：全 4K60）

> **新增确认（D14）**：**开发前会提供典型素材，且全部为 4K 60fps**；同时确认**实测素材不再是待确认项**。
> 这条把"1080p–4K 都有"收敛为"**只有 4K60**"，把 4K 从"常态"变成"**唯一规格**"。影响面比看起来大 —— 下面逐项复核。

### 18.1 4K60 带来的一个硬性前置条件：硬件解码从"建议"变成"必需"

| 维度 | 1080p60 | **4K60** |
|---|---|---|
| 软解（x264 解码路径）CPU 占用 | 可接受（多核能吃下） | ❌ **不可接受**。4K60 软解会吃掉大部分 CPU，与推理抢核算力，**整体耗时可能翻数倍**，笔记本还会直接降频 |
| 硬解（NVDEC/QSV/AMF/MediaCodec） | 有帮助 | ✅ **必需** |
| 解码带宽压力 | 中等 | 高：4K60 原始帧是 1080p 的 4 倍像素 |
| 内存/显存峰值 | 1.5–3 GB | **上升**（解码缓冲按分辨率增长） |

**结论：必须把"4K60 硬解可用性"提升为一个显式的、开工前必须验证的项目（新增 A6）。** 而且要写成**强制降级**而不是"尽力而为"：

```
导入 4K60 源
   → 探测本机硬解能力（NVDEC / QSV / AMF / MediaCodec）
   → 若硬解可用：直读原片，解码后立即降采样到 720p 送推理
   → 若硬解不可用：**立即转 720p（或 1080p）代理**，后续分析与预览全部基于代理
        UI 明确提示："本机不支持 4K60 硬件解码，已转低分辨率代理以保证速度"
```

**为什么这条重要**：用户的机器可能连"能硬解 4K60"都不满足（老 CPU + 无 iGPU 的 F 系列 + 老显卡）。如果不做这个探测与降级，用户会遇到"分析跑了两个小时"的最坏体验，而且不知道该怪谁。**这是全案唯一一个"可能让绝大多数用户直接失败"的环境问题。**

### 18.2 由 4K60 引起的逐项复核

| # | 位置 | 原表述 | 4K60 下的复核结论 |
|---|---|---|---|
| **C1** | 第 2.2 ③-补二 | "4K 全帧直接缩放到 512×288 丢 92% 像素" | ✅ **结论不变，而且更关键** —— 现在这是**唯一**的素材规格，所以"第一遍必须降到 720p 再缩放"从"优化建议"升级为**架构必需**。直接拿 4K 缩 512×288 是灾难 |
| **C2** | 第 2.2 ③-补二 | "4K-ROI 是正确打开方式" | ⚠️ 已在第 17.1 节 T2 降级为**待验证**（裁剪尺度偏离训练分布）。4K60 下 ROI 的**动机更强**（分辨率优势更大），但**风险不变** → A5 消融实验的优先级**上升** |
| **C3** | 第 4.3 节 B | 内存峰值 1.5–3 GB | ⚠️ **需上修**：4K60 硬解缓冲 + 缩放中间帧，Windows 峰值应估 **2.5–4 GB**；手机 **1.2–1.8 GB**（原本 700 MB–1.2 GB） |
| **C4** | 第 3.3.3 节 | 预览默认"直接播原片"，仅弱机/4K 才生成代理 | ⚠️ **需改为"4K60 默认生成代理"**。10 分钟 4K60 源 2–8 GB，逐段 seek 的随机读压力与解码压力都大得多；代理从"可选优化"变为**4K60 场景的默认项** |
| **C5** | 第 3.3.4 / 9.3 节 | 导出含 4K 原分辨率选项 | ⚠️ **需明确**：4K60 导出**必须走硬件编码**；无硬件时给出明确警告（软编 4K60 约 0.1–0.3× 实时，10 分钟成片要 30–100 分钟） |
| **C6** | 第 4.2 / 12.7 节 | Windows 安装包 200–400 MB | ✅ 不变，但**必须包含硬解支持**（FFmpeg 需带 d3d11va/dxva2 等硬解后端，体积影响小但配置易错） |
| **C7** | 第 12.3 节 | "分辨率 720p–4K 都有" | ✅ 改为"**全部 4K60**"；拍摄建议从"1080p 足够"改为"**4K60 是既定条件，无需讨论；注意 4K60 文件更大、建议预留空间**" |
| **C8** | 第 16.4 T1 | 测试集"720p/1080p/4K 各若干" | ✅ 改为"**全部 4K60**"；但**建议额外准备 2–3 段 1080p/720p**，仅用于验证降级路径（代理模式） |
| **C9** | 第 12.2 节 | 源文件体积 4K60 HEVC 2–4 GB | ✅ 不变。但**20 段测试素材总量会达 40–160 GB**，需提前确认存储与传输方案 |
| **C10** | 第 16.7 节 | "4K 解码与内存"作为补充工程点 | ✅ 升级为**主要风险项**（见 C3/C4/C5），不再只是"补充" |
| **C11** | 第 10.4 节 手机端 | "MediaCodec 可硬解 4K60" | ⚠️ **不能想当然**：需按机型探测。**建议手机端只支持"4K60 硬解可用"的机型**，其余机型引导转代理或引导到 Windows |

### 18.3 由 4K60 引起的一个正面影响

**4K60 让"第一遍跳帧"的收益更大、风险更小。**

- 60fps 下跳帧到 30fps 等效：4K60 的**帧间位移与 1080p60 完全相同**（位移由物理运动和帧率决定，与分辨率无关），所以跳帧的精度损失与分辨率无关；
- 而 4K60 的**解码与缩放成本是主要瓶颈**，跳帧能同时省掉"每两帧只处理一帧"的推理与缩放；
- 更关键的是：**4K 降采样到 720p 后，球的呈现与 1080p 降采样基本等价**（都远大于 512×288 模型输入），所以**"4K 相比 1080p 在球轨迹上几乎没有额外信息增益"** —— 这进一步支持"第一遍统一在 720p 跑"，不因素材是 4K 就改变第一遍策略。

> 也就是说：**4K60 的价值主要在"导出画质"和"可选精算档"，而不是"第一遍粗筛的精度"。** 别为了 4K 在第一遍上多花算力。

### 18.4 复核后的状态

| 项 | 状态 |
|---|---|
| **A3（基准素材来源）** | ✅ **已解除** —— 开发前会提供典型素材（D14） |
| **新增 A6** | ⛔ **新增必做验证**：4K60 硬件解码可用性（第 18.1 节）。**这是新的最高优先级环境风险** |
| **A5（ROI 消融）** | 优先级上升（4K60 下 ROI 动机更强） |
| **其余待确认项** | C 类 5 项待定（精彩球筛选 / 球员姓名 / 烧字内容 / 双端研发顺序 / 检查更新）；**没有新的阻塞项** |
| **建议的下一步** | 拿到素材后**第一件事不是跑 P1，而是先跑 A6**（硬解探测）—— 因为如果硬解不可用，整个时间预算的前提就变了，P1 的测试环境也要相应调整（必须用代理模式测） |

> **一句话结论**：**4K60 不改变架构，但改变三件事**：① 硬解成为强制性前置条件（新增 A6，最高优先级环境风险）；② 代理生成从可选变为默认；③ 导出必须依赖硬件编码。**其余判断（第一遍 720p、两遍处理、静止期锚点、成片生成器）全部不变。**

---

## 附录 A：参考资料

**数据集与论文**
- ShuttleSet: A Human-Annotated Stroke-Level Singles Dataset for Badminton Tactical Analysis (KDD 2023) — 104 局 / 3685 回合 / 36492 拍，含 18 类拍型与双方站位标注 — https://arxiv.org/abs/2306.04948
- BST: Badminton Stroke-type Transformer for Skeleton-based Action Recognition in Racket Sports — https://huggingface.co/buckets/huggingchat/papers-content/tree/2502/2502.21085.md
- 基于改进 YOWO 的羽毛球动作 2D 视觉识别 — https://ieeexplore.ieee.org/document/10695659/
- Benchmarking Badminton Action Recognition with a New Fine-Grained Dataset — https://www.semanticscholar.org/reader/32dfdb2c860d575fd0161595063d0a1d46b0e299
- Badminton Shot Recognition with LSTM Network — https://gz.nstl.gov.cn/paper_detail.html?id=f091f61fffd409245d324af52e943631
- MultiSenseBadminton: Wearable Sensor–Based Biomechanical Dataset（含击球声质量标注思路）— https://www.nature.com/articles/s41597-024-03144-z

**开源实现（可直接借鉴架构）**
- BadmintonTrackNet（TrackNetV3 + 轨迹修复 + 落地/击球事件检测 + 半自动标注 + BounceNet）— https://github.com/ZSHYC/BadmintonTrackNet
- TrackNetV3（球追踪 SOTA 主干，含预训练权重与数据集约定）— https://github.com/qaz812345/TrackNetV3
- Deep-Learning-Based Temporal Analysis of Badminton Gameplay（场地关键点 + 球员位置 + 回合分割 + 拍型分类，CSV 输出）— https://github.com/GirishBagul/Deep-Learning-Based-Temporal-Analysis-of-Badminton-Gameplay
- TrackNetV4: Enhancing Fast Sports Object Tracking with Motion Attention Maps（含对 V2/V3 的独立复现数据，本方案 ③-补 一节的对比表来源）— https://ar5iv.labs.arxiv.org/html/2409.14543
- TrackNetV5: Robust Shuttlecock Tracking via Motion Prompts and Spatiotemporal Attentive Fusion — https://ieeexplore.ieee.org/document/11630406/
- TrackNet: A Deep Learning Network for Tracking High-speed and Tiny Objects in Sports Applications（V1 原始论文，热图回归思路起点）— https://www.semanticscholar.org/paper/TrackNet%3A-A-Deep-Learning-Network-for-Tracking-and-Huang-Liao/33d432a81d5c7ef69e6a64b7e0228e33b3b1a4ce
- TrackNetV2: Efficient Shuttlecock Tracking Network（512×288 输入与 3-in-3-out 的来源）— https://www.computer.org/csdl/proceedings-article/icpai/2020/426200a086/1pZ165Bsj7O

**端侧推理与多媒体**
- Android on-device AI benchmark（LiteRT vs ONNX Runtime vs ExecuTorch 的延迟/内存/精度对比）— https://github.com/sangsoolee/android-edge-ai-benchmark-lab
- Qualcomm MediaPipe-Pose-Estimation（NPU 上 3.1 ms 级姿态推理，含 27 MB 级模型）— https://huggingface.co/qualcomm/MediaPipe-Pose-Estimation
- MediaPipe 模型复杂度选择（lite/full/heavy 的精度-延迟权衡）— https://theneuralbase.com/mediapipe/learn/advanced/model-complexity-selection/
- RTMPose 部署文档（ONNX Runtime / 移动端导出）— https://raw.githubusercontent.com/open-mmlab/mmpose/main/projects/rtmpose/README.md

**导出与工程文件**
- FFmpeg 非关键帧切割行为说明 — https://video.stackexchange.com/questions/37800/how-did-ffmpeg-cut-on-a-non-keyframe-without-re-encoding/37801
- EDL / XML / AAF 兼容性检查（用于验证导出的工程文件）— https://aspect.inc/tools/edl-xml-aaf-compatibility-checker
- ScoreSight（开源比分板 OCR 思路参考）— https://obsproject.com/forum/resources/scoresight-free-open-source-ocr-tool-for-gaming-and-scoreboards.1884/

**浏览器端能力（第 11 节依据）**
- WebGPU 特性与浏览器支持现状（Chrome 144 / Edge 144 / Safari 26 支持，Firefox 未支持，页面覆盖率约 10.9%）— https://web-platform-dx.github.io/web-features-explorer/features/webgpu/
- WebCodecs `VideoDecoder.isConfigSupported()`（解码能力探测与硬件加速回退）— https://developer.mozilla.org/en-US/docs/Web/API/VideoDecoder/isConfigSupported_static
- File System Access API（本地大文件读写、句柄持久化，仅 Chromium 系）— https://developer.chrome.com/docs/capabilities/web-apis/file-system-access
- ONNX Runtime Web 的 WebGPU EP 与显存/加载峰值问题（浏览器端推理的工程坑）— https://github.com/microsoft/onnxruntime/issues/29455

---

## 附录 B：推荐技术栈（可直接开工）

| 层 | Windows | Android |
|---|---|---|
| 语言/核心 | C++20（算法核心）+ Rust（可选，做数据层） | 同左，FFI 复用 |
| UI | Qt6/QML（跨平台，同一套 QML 可迁安卓） | Compose 或 Qt6 for Android |
| 推理 | ONNX Runtime (CPU / DirectML / CUDA EP) | LiteRT + NNAPI / ONNX Runtime Mobile |
| 视频 | FFmpeg（自编译精简版，含硬件编码 NVENC/QSV/AMF） | ffmpeg-kit 或自编译 + MediaCodec |
| 数据库 | SQLite (WAL) | SQLite |
| 数据交换 | JSON + CSV + FCPXML + EDL | 同左 |
| 训练侧 | PyTorch（离线，产出 ONNX；训练不属于交付程序） | — |
| ~~可选 Web UI（第 11 节）~~ | **已排除（D6）**，不做 Web 前端 | — |

> 训练（数据集准备、微调 TrackNet/骨架分类器）建议放在 Python/PyTorch 侧独立完成，程序内只做推理。这样才能保证交付程序体积可控（不含 torch）。
