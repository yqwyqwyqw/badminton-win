# 羽毛球回合分析器 v0.1.0（Windows 便携版）

首个 **C++/ONNX** 版本：轨迹推理内核从「Python + PyTorch 子进程」改为**进程内 ONNX Runtime（DirectML）**，
运行时不再需要 Python、PyTorch、OpenCV 或任何虚拟环境；整个目录即装即用。

## 功能

- 导入本地视频，自动生成 720p 代理，进度条显示真实进度，支持停止与**断点续跑**
- 每识别出一个完整回合就**立即**出现在列表里（增量发布，不必等全片分析完）
- 回合分析页：上一回合 / 下一回合切换，切换即播放并**单回合循环**
- 回合拼接页：选择与拖动排序、显示/隐藏、前后边界微调（±0.5 s 或直接填秒数）、备注
- 单回合导出（720p / 1080p / 原始画质，**带音轨**），以及按顺序拼接导出成片
- 全程本地运行，不联网、不上传任何数据

## 运行要求

- Windows 10 / 11 64 位
- 支持 **DirectX 12** 的显卡（推理走 DirectML，驱动需较新；无 DX12 会直接报错而不是静默变慢）

## 分发形态

发布资产只有一个安装包：

| 资产 | 体积 | 用法 |
|---|---|---|
| `badminton-analyzer-v0.1.0-setup.exe` | **110.1 MB** | 安装包：默认按用户装到 `%LOCALAPPDATA%\Programs\BadmintonAnalyzer`（**不需要管理员**，向导里可切换为全机安装），建开始菜单快捷方式、可选桌面快捷方式，带卸载项 |

需要绿色免安装形态时，可用仓库里的脚本自行生成：`scripts/release-windows.ps1`（解压即用目录）与 `scripts/make-portable-exe.ps1`（自解压单文件）。

## 体积

| 形态 | 体积 |
|---|---|
| 安装后占用 | 360.6 MB |
| **安装包（LZMA2 固体压缩）** | **110.1 MB** |

打包时会剔除未使用的 Qt 模块（只保留 Basic 控件样式，去掉 VirtualKeyboard / Quick3D / Lottie / Pdf / 其余 6 套样式等）。

## 验证

- **与 Python 参考实现逐层对齐**（编译的是产品源码）：中值图与 `cv2` 逐字节一致（0/2764800 差异）；
  整片轨迹可见性差异 1/2823（0.035%），坐标 P50/P95 = 0/0 px、最大 5 px；**回合边界与黄金集完全一致**
- **成品隔离验证**（PATH 只留 `system32`，冷跑）：分析 77.3 s 完成、3 个回合、720p 与 1080p 导出 PASS；
  打包后 GUI 启动 0 个 QML/模块错误
- **安装包全流程验证**：静默安装退出码 0、装出 257 个文件 / 360.6 MB、GUI 在最小 PATH 下正常启动
  （0 条 QML 报错）、卸载**无残留**
- **自动清理验证**：`reset()`（界面上的「新建项目」）删除本项目缓存后，校验工具断言 PASS

## 已知限制

- 回合边界与拍数为自动结果（`confidence=provisional`），建议在拼接页人工确认后再导出
- 出画球的落点由轨迹拐点推断，属"推断"而非直接观测
- 程序未做代码签名：首次运行 SmartScreen 会提示，选「更多信息 → 仍要运行」
- 单回合短片预览无音轨（历史行为）；**导出的视频带音轨**
- 分析缓存位于 `%LOCALAPPDATA%\BadmintonAnalyzer\羽毛球回合分析器\cache\`，按视频自动隔离，删除即可重算

## 第三方组件

Qt 6.11.2（LGPL-3.0，动态链接）、ONNX Runtime 1.24.4（MIT）、DirectML（MIT）、
FFmpeg 9.0.1（**GPL-3.0**，以独立进程调用，未链接 libav\*）、TrackNetV3 预训练权重（MIT）。
详见随包 `THIRD-PARTY-NOTICES.md`。
