# 发布说明

## v0.0.3

这是 Windows 客户端的第一个完整工作流版本，覆盖：

- 本地视频导入、原始日期和视频参数读取、原片预览
- TrackNet 增量回合分析、暂停/恢复和已有结果恢复
- 回合列表预览、单回合画质导出
- 回合选择、隐藏/显示、拖动排序、上下精排和恢复原始顺序
- 回合前后边界微调和三击卡片编辑备注
- 拼接方案推送到视频导出页，并以快照方式确认导出内容
- 720p 快速版和原始画质合集导出，保留可用音轨

## 构建与运行

当前版本仍属于本地验证发布，不是独立安装包。运行分析功能需要准备 Qt 6、FFmpeg、TrackNet 模型和 Python 验证环境。推荐使用：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/release-windows.ps1 -Version 0.0.3
```

构建完成后，发布目录位于 `build/release/`。直接运行时需要把 Qt 的 `bin` 目录加入当前进程的 `PATH`。

## 已知限制

- 项目级的回合排序、边界和备注持久化尚未接入数据库或独立工程文件。
- 当前发布脚本生成的是 Windows 便携 UI 包和源代码包；TrackNet Python 环境不打进压缩包。
- 尚未提供 Android 端、自动计分、拍型识别和竖屏/方形模板。
- 合集导出已验证小规模回合；长视频完整编码仍建议在目标机器上单独验收。
