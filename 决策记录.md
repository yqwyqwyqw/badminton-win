# 决策记录（自主决策）

> 会话：2026-09-15　分支：`deepseekflash`
> 背景：用户出门前授权"有决策问题自己决策"，并指定最后输出本文件。
> 范围：本次只做 **M0（金标准回归集）+ M1（ONNX 导出与对齐）**，**未改动任何产品代码**（`app/`、`validation/` 原样）。
> 证据索引：`onnx-m1-parity-report.md`（完整数据）、`onnx-refactor-plan.md`（方案）、`validation-output/golden/`（产物与日志）。

---

## 0. 用户已确认的四条（我据此执行，不重复决策）

| 编号 | 用户决定 | 我的执行方式 |
|---|---|---|
| D1 | 仅个人 GitHub 发布、完全不商用 | 权重**随包分发**（上游 LICENSE 明文覆盖 checkpoint），不额外做"首启下载"；GPL 义务照履行（附许可 + 源码指引 + **不链接 libav\***） |
| D2 | InpaintNet 不放进本次重构 | 未导出、未接入；但在方案里把"球飞出画面"的处理明确归位到状态机 |
| D3 | V3-8f + ONNX Runtime + DirectML + fp16 | 已按此导出并对齐；fp16 落地细节见 A2/A3 |
| D4 | BounceNet 不放进本次重构 | 未导出；只保留"多模型 = 多 session"的扩展要求 |

---

## A. 执行期自主决策（本次新增）

### A1. 对齐门禁从"单像素极值"改为"决策级" —— ✅ 已执行

- **背景**：fp32 ONNX 与 PyTorch 的热图最大像素差为 **4.41e-3**，超过我最初在方案里写的 1e-3 门禁。
- **我做的诊断**：把 ORT 图优化关掉（`ORT_DISABLE_ALL`）重跑，得到**完全相同的数字**（4.41e-3 / p99.9 2.46e-4）→ 差异来自"导出图 vs 原生 PyTorch 的卷积实现/求和顺序"，**不是** ORT 优化造成的，也无法通过换运行时消除。
- **决策**：门禁改为决策级，并保留极端值报警：
  - 可见性不一致率 ≤ 0.5%（理想 0）
  - 解码坐标 P95 ≤ 1.5 px
  - 热图 p99.9 ≤ 1e-3(fp32) / 5e-3(fp16)；`max` 只作报警门槛（1e-2 / 2e-2）
- **影响**：这是"产品是否可用"的正确判据。实测结果远超门禁（**0/2823 可见性差异、坐标 P95 = 0 px、回合切分完全一致**）。
- **风险**：若将来出现"热图差异恰好落在 0.5 阈值上导致可见性翻转"，门禁会捕获（可见性指标直接反映）。

### A2. 采纳 fp16 模型（21.6 MiB），而非 fp32（43.3 MiB） —— ✅ 已执行

- **依据（实测）**：DirectML 上 fp16 **42.4 fps** vs fp32 38.8 fps（**快 9%**）；体积减半；对齐指标与 fp32 同级（热图 max\|Δ\| 4.72e-3 vs 4.41e-3，可见性 0/2823，坐标 P95 = 0，回合切分一致）。
- **影响**：模型从 43.3 → 21.6 MiB，直接服务于 <500 MB 安装包 / 单 exe 目标。
- **附带决策**：fp16 保持 **fp32 输入输出**（`keep_io_types=True`），C++ 侧永远喂 float32，避免"fp16 模型要 fp16 输入"的第二条代码路径。

### A3. 为 fp16 引入"导出期 int64-sizes 包装" —— ✅ 已执行（本次唯一改图结构之处）

- **问题**：`nn.Upsample(scale_factor=2)` 导出为 ONNX `Resize`，其 `scales` 按规范必须是 **float32**；两个 fp16 转换器都会把它变成 fp16，导致模型非法（`op_block_list=["Resize"]` 也无效；ORT 自带转换器则报拓扑排序错误）。
- **决策**：导出时用一个包装模块把三次上采样改写为 `F.interpolate(size=<skip 形状>)`，图上变成 **int64 `sizes`**，fp16 转换即通过（21.6 MiB，可被 ORT 加载）。
- **风险控制（双重）**：① 导出前断言包装与原生模型**位级一致**（实测 **max\|Δ\| = 0.000e+00**）；② 包装产物同样接受窗口级与全片段管线级对齐检查（均通过）。
- **说明**：vendored 模型定义**未作任何修改**，包装只存在于导出工具中。

### A4. 运行时统一为 `onnxruntime-directml` 单包 —— ✅ 已执行

- **背景**：`onnxruntime`（CPU）与 `onnxruntime-directml` 是**同一包名**，不能共存。安装 DML 包时 CPU 版 1.30.0 被替换为 1.24.4。
- **决策**：采纳 `onnxruntime-directml 1.24.4`（**23.9 MB**，内含 `DmlExecutionProvider` + `CPUExecutionProvider`）作为唯一运行时。
- **理由**：一个包同时覆盖"有 DX12 显卡"和"纯 CPU"两条路径，发布包不增加体积；代价是 CPU EP 版本较旧（见 A5）。
- **影响**：方案 §3 的体积预算里"ORT ≈15 MB(CPU) / ≈25 MB(含 DML)"落实为 **23.9 MB 单包**。

### A5. 推理 batch 上限固定为 8，并要求 C++ 侧实现降批回退 —— ✅ 已执行

- **背景**：换用 DML 包的 CPU EP 后，**batch 32 时申请 32.6 GB im2col 缓冲直接 OOM**（`up_block_3` 的 192×9×288×512 卷积）。
- **决策**：对齐脚本与产品路径统一 **batch ≤ 8**；C++ 侧沿用现有"OOM 就降批（8→4→2→1）"策略（`run_chunked_tracknet.py:432-459` 的既有语义）。
- **附带发现**：CPU EP 吞吐对**线程数**极敏感（8 线程 0.58 fps → 16 线程 3.81 fps），产品侧默认应取满物理核。

### A6. 不导出 InpaintNet（遵循 D2），但保留导出可行性 —— ✅ 已执行

- 未导出、未接入。方案中已记录其规格与**禁用理由**（它的掩码守卫拦不住"顶部飞出再飞回"，会编造画面外轨迹）。

### A7. golden 数据不入库，只提交脚本与报告 —— ✅ 已执行

- **决策**：`validation-output/golden/`（约 350 MB：金标准 205 MB + 模型 111 MB + 报告/轨迹）保持 gitignored；仓库只收 `tools/*.py`（3 个 dev-only 脚本）与两份 markdown。
- **理由**：仓库当前仅 36 个跟踪文件、180 KB，塞入二进制会毁掉 clone 体验；`meta.json` 已记录全部哈希与参数，可一条命令复现（报告 §10）。

### A8. 参考基线用"app 自己的运行结果"，不重跑 —— ✅ 已执行

- **决策**：以 `validation-output/ui-trials/VID20260828212318_1-c1fe12c0ab/tracknet/` 里 app 跑出的 2823 帧轨迹与 3 个回合为参考，固化到 `golden/<clip>/app-run/`。
- **理由**：它就是产品路径的真实产物，且与 `RESULTS.md:51` 记录的已知回归案例（人工 4 回合 vs 自动 3 回合）一致；重跑只会引入不必要的变量。

### A9. 性能口径更正：app 实际是 46.5–51.7 fps，不是 11.62 fps —— ✅ 已记录

- **事实**：`RESULTS.md:41` 的 **11.62 fps** 是 `weight` 模式（步长 1，8 倍窗口数）的数字；app 用的是 **`nonoverlap`**（步长 8）。本次实测 PyTorch CUDA **46.5 fps**、app 自记 **51.7 fps**。
- **影响**：方案的性能章节此前引用了 0.23× 实时作为"CUDA 基线"，**实际基数是 0.93–1.03× 实时**；ONNX+DirectML fp16 的 **0.85× 实时** 是"几乎持平"，而不是"大幅改善"。已在 `onnx-m1-parity-report.md` §6 更正口径。
- **风险提示**：这条更正**削弱**了"必须换运行时才有性能"的论据，但**不改变**换运行时的核心动机（去 Python / 去 CUDA / 体积），反而说明"只换运行时不掉性能"更容易交付。

### A10. 测试素材与回归范围 —— ✅ 已执行

- 首选 `VID20260828212318_1`（4K50 → 720p 代理，2823 帧），因为它有 `RESULTS.md` 的人工复核回归案例。
- `序列 01` 留作 M2 的第二段回归（其前 5 回合边界已被人工确认正确）。

### A11. 不切换到 PyTorch 2.9 的 dynamo 导出器 —— ⏸ 暂缓（已记录）

- 当前用 legacy 导出器（有 `DeprecationWarning`，PyTorch 2.9 起 dynamo 将成为默认）。legacy 导出已验证且图结构稳定；dynamo 会改变图结构，**必须重新跑整套对齐**。故本次不动，列入 M2 之前的待办。

### A12. M2（C++ 推理内核）不在本次会话动工 —— ✅ 已决策

- **理由**：M2 约 10 人日（前处理等价实现、连通域后处理、窗口化/ensemble、状态机移植、FFmpeg 解码、分块续跑），需要一轮专门的开发会话；本次以"**可独立验收的证据 + 可复现的工具 + 决策记录**"收尾，是最有价值的交付。
- **M2 建议纳入的顺手修复**：D-5（背景中值图改为在 512×288 上计算，消除 GB 级内存峰值）—— 它属于推理链路，与 M2 同批改最省事。
- **其余缺陷**（D-2 代理变形 / D-3 短片无声 / D-4 缓存路径 / D-6 硬编码路径 / D-7 NVENC 版本）属于 UI/导出/打包层，建议**单列一轮**，避免一次改动过大。

---

## B. 用户回来后确认的四项（原"待拍板"→ 已决）

| # | 事项 | 用户决定 | 我的落地动作 |
|---|---|---|---|
| B1 | DirectML 在老驱动/非 NVIDIA 上的兼容性 | **无需覆盖老驱动** | 目标收敛为"D3D12 + 较新驱动"；不再安排老驱动矩阵测试；仍保留 EP 探测（失败给明确提示） |
| B2 | 纯 CPU 回退（3.81 fps，整片 ≈12 min） | **不要了** | **删除 CPU 回退路径**：DML 不可用即快速失败并提示硬件要求；`onnxruntime-directml` 里的 CPU EP 仅留作开发期对齐用。方案 §2/§5/§6 已同步 |
| B3 | 模型随包 vs 外置 | **同意随包** | 模型随安装包分发，配 `THIRD-PARTY-NOTICES.md` 署名（MIT © 2024 qaz812345） |
| B4 | 是否修代理非 16:9 变形（D-2） | **变形没事，能识别就行**（切片按时间点、与尺寸无关） | **D-2 标记为不修**（已接受风险）；附带好处：**代理生成逻辑不变 ⇒ M0 golden 集持续有效**；若将来改代理则必须重跑 M0 对齐 |

> 影响的后续项：M2 的实现范围因此**减少**（不需要 CPU 回退与两套 EP 路径），验收标准也从"CPU 也能跑完"改为"**DML ≥0.8× 实时；无 DML 快速失败**"。

## B-补：协作约定（用户要求，2026-09-15）

- **不要频繁提交**：只在出现**阶段性大更新**时提交一次，中间过程留在工作区。
- **提交标题用简短中文描述**（例如"完成金标准对齐与 C++ 推理打通"），不写英文前缀、不堆长正文。
- 分支推送由用户决定时机。


---

## C. M2 第一步（C++ 侧打通）的自主决策

| # | 决策 | 依据 / 影响 |
|---|---|---|
| C1 | C++ SDK 走 **NuGet 预编译包**（`Microsoft.ML.OnnxRuntime.DirectML 1.24.4` + `Microsoft.AI.DirectML 1.15.4`），不自建 | ①**与 Python 侧 wheel 同版本**，避免两侧行为差异；②免去数小时的源码编译；③包内已含 DML EP。解压到 `.tools/onnxruntime-cpp/`（gitignored） |
| C2 | 金标准以**裸 float32 二进制 + dims.txt** 交给 C++ | C++ 不引入 npy/npz 解析依赖（`tools/dump_golden_bin.py`）；`tools/ort_cpp_smoke/` 因此只有一个外部依赖 |
| C3 | 冒烟测试**只测"模型推理"**，不含前处理/后处理 | 目的是把"最大技术未知项"（C++ 能否加载模型 + DML 能否跑 + 数值是否一致）前置验证，与移植解耦 |
| C4 | ⚠️ **不把 84.75 fps 当产品承诺** | 它是仅模型上限；Python 端到端是 42.4 fps，差距来自解码/前处理/ensemble。**产品指标仍以端到端 0.85× 实时为基准，M2 完成后重测** |
| C5 | 产品侧必须加**预热推理** | DML 首次运行有 kernel 编译开销（8 窗口首跑 0.537 s → 稳态 0.09 s）；不预热会让第一段进度条忽慢 |
| C6 | C++ 侧 `provider == cpu` 时**不注册 EP** | DML 版 ORT 的 `AppendExecutionProvider("CPU")` 会抛错（该构建只注册 DML 等 EP，CPU 是隐式兜底）；开发期对照仍可用 CPU |
| C7 | 体积预算按**落盘**口径更正：ORT 运行时 **34.2 MB**（16.5 + 17.7 + 0.02），而非 wheel 的 23.9 MB | 直接进发布包的是 DLL；便携目录目标 220 MB 仍有充足余量 |
| C8 | 冒烟测试程序**不进发布包**，与 M2 正式内核（`app/src/inference/`）分离 | 保持"dev-only 工具"与产品代码的边界，避免测试代码混入交付物 |

---

## D. 本次的净产出

| 类型 | 文件 |
|---|---|
| 工具（入库） | `tools/capture_golden.py`（金标准采集）、`tools/export_onnx.py`（导出 + 窗口级对齐）、`tools/compare_onnx_pipeline.py`（全片段管线级对齐 + 回合对比 + 吞吐实测）、`tools/dump_golden_bin.py`（金标准转裸二进制）、`tools/ort_cpp_smoke/`（C++ + ORT DirectML 冒烟测试） |
| 报告（入库） | `onnx-m1-parity-report.md`（M0/M1/M2-第一步 全部数据与复现命令）、`决策记录.md`（本文件） |
| 方案更新（入库） | `onnx-refactor-plan.md`（D1–D4 + B1–B4 决策、许可结论、出画归位、GPL 约束、体积口径更正、Phase 2 状态） |
| 产物（不入库） | `validation-output/golden/`：金标准轨迹/热图/输入窗口、`app-run/` 权威基线、`models/` 3 个 ONNX 与 `models.json`、`bin/`（C++ 用的裸二进制）、`pipeline-full/`、`pipeline-cpu/` 报告与轨迹 |
| 第三方 SDK（不入库） | `.tools/onnxruntime-cpp/`（ORT DirectML 1.24.4 + DirectML 1.15.4，来自 NuGet） |
| 环境变更 | `.venv-validation` 新增 `onnx 1.22.0`、`onnxruntime-directml 1.24.4`、`onnxconverter-common 1.16.0`（**torch 未动**，仍 2.8.0+cu128） |

**未做**：任何产品代码改动（`app/`、`validation/`、`scripts/` 全部原样）；InpaintNet / BounceNet 导出；C++ 前处理/后处理/状态机移植；打包。
